# **spiderV5 产品需求文档 V3**

## **BFWS + Google OR-Tools CP-SAT 智能排程系统**

---

## **关于 Q2-Q4 的假设与说明**

| 问题 | 假设选择 | 说明 |
|------|---------|------|
| **Q2 排程规模** | 中等规模：10台车型 / 5-8个工位 / 30秒硬时限 | 符合汽车试制项目的典型规模 |
| **Q3 评分-排程耦合** | A. 单向注入：评分 → CP-SAT → 排程 | 先实现较简单的方案，后续可扩展双向耦合 |
| **Q4 规则引擎保留** | C. 可切换："快速模式（规则）" / "精确模式（CP-SAT）" | 给用户选择灵活性，规则引擎也可作为 CP-SAT 的初解加速收敛 |

---

# **第一部分：核心价值与设计理念**

## 1.1 为什么引入 BFWS + CP-SAT

当前系统的"装配顺序推荐"基于**4步启发式规则**：

```
齐套优先 → 瓶颈对齐 → 工位平衡 → 风险车型
```

这类似于 Google Maps 的"推荐路线"功能——给出一条看起来合理的路线，但无法保证：

> ❌ 考虑连锁阻塞效应
> ❌ 证明方案的全局最优性
> ❌ 量化"如果X延迟2天，总工期影响多少小时"
> ❌ 多目标权衡（短工期 vs 低风险 vs 高齐套率）

**引入 BFWS + CP-SAT 后，系统升级为：**

```
BFWS 物理建模（描述阻塞如何产生）
  ↓
CP-SAT 约束求解（搜索全局可行解）
  ↓
甘特图 + 关键路径分析（可视化结果）
  ↓
鲁棒性报告（如果...会怎样？）
```

**核心收益比喻**：从"凭经验估算的人工排程"升级为"类似航班调度的数学精确排程"。

---

## 1.2 BFWS 场景匹配说明

### 汽车试制装配线的典型结构

```
[车型A] → 工位1(底盘装配) → 工位2(动力总成) → 工位3(内外饰) → 工位4(电气检测) → 工位5(终检下线)
[车型B]
[车型C]
...
```

**为什么是 BFWS 而不是经典 FSP：**

| 现象 | 说明 | BFWS 对应约束 |
|------|------|--------------|
| 🚗 **工位间无缓冲** | 车型A在工位2未完成，车型B无法进入工位2（物理空间限制） | `NoOverlap` 约束 |
| 🔄 **阻塞传递** | 工位2因车型A缺料暂停 → 工位1已完成的车型C阻塞在工位1 | `start[j][m+1] >= end[j][m]` |
| ⏰ **关键件到货时间** | 动力电池模组预计06-25到货 → 所有使用该零件的车型最早开始 = 06-25 | `start[j][0] >= critical_arrival[j]` |
| 📊 **装配工时差异** | E70标准续航版需8工时，G59海外版需12工时 | 每个工件的`processing_time[j][m]`差异化定义 |

---

# **第二部分：功能需求详解**

## 模块 1：关键件智能识别与评分（增强版）

### 1.1 功能概述

当前的4维度评分保持不变，但评分结果**不再仅用于标签展示**，而是作为排程优化的核心输入。

### 1.2 改进点对比

| 维度 | V2（当前） | **V3（BFWS+CP-SAT）** |
|------|-----------|-----------------------|
| **输出形式** | 红色/黄色标签 + 数值评分 | 标签 + 评分 + **工位敏感度系数** |
| **数据流向** | 评分 → 结束 | 评分 → **CP-SAT 目标函数惩罚项** → 影响排程结果 |
| **工位差异化** | 所有工位同一评分 | **按工位拆分评分**：同一零件在工位2(动力)权重 = 1.5，在工位5(终检)权重 = 1.0 |
| **动态调整** | 静态（上传PBOM时计算一次） | **动态重算**：排程结果出来后，延迟装配的车型对应零件评分自动上调 |

### 1.3 新增界面元素

**在当前"关键件评分"页面，新增：**

```
┌───────────────────────────────────────────────────────────┐
│  关键件：动力电池模组   综合评分：4.5  ★★★★☆              │
├───────────────────────────────────────────────────────────┤
│  安全件评分：5.0                                            │
│  大件重件评分：4.2                                          │
│  紧缺件评分：4.8                                            │
│  工艺件评分：4.0                                            │
├───────────────────────────────────────────────────────────┤
│  ◆ 工位敏感度分析（NEW）                                   │
│    工位2(动力总成)：使用该零件 → 敏感度系数 ×1.5            │
│    工位5(终检)：       验证该零件 → 敏感度系数 ×1.0          │
│                                                             │
│  ⚡ 排程影响预测：该零件延迟1天 → 关联3台车型平均延迟 1.8h   │
│     (点击查看详细甘特图对比)                                 │
└───────────────────────────────────────────────────────────┘
```

---

## 模块 2：装配顺序推荐（核心改造模块）

### 2.1 功能概述

这是 V3 版本最大的变化模块。从**启发式推理表格**升级为**CP-SAT 精确排程引擎**。

### 2.2 页面布局（顶部一级导航：项目管理 → 二级 Tab：装配顺序推荐）

```
┌─────────────────────────────────────────────────────────────────────┐
│  🔧 装配顺序推荐                                                     │
│  基于 BFWS 阻塞流水车间模型 + Google OR-Tools CP-SAT 求解器            │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─求解模式选择────────────────────────────────────────────┐         │
│  │  ○ 快速模式（规则引擎）                                   │         │
│  │     · 基于经验的4步推理                                    │         │
│  │     · 响应时间：<1秒                                       │         │
│  │     · 适用：快速预览、小规模项目                             │         │
│  │                                                           │         │
│  │  ● 精确模式（CP-SAT 求解器） ⭐ 默认                      │         │
│  │     · BFWS 数学建模 + 全局优化搜索                         │         │
│  │     · 响应时间：5~30秒（依赖规模）                          │         │
│  │     · 适用：正式排程发布、多车型复杂场景                      │         │
│  │                                                           │         │
│  │  [求解时限：] [ 30 秒]  [ 启用鲁棒性分析 ☑ ]              │         │
│  └───────────────────────────────────────────────────────────┘         │
│                                                                     │
│  ┌─What-if 参数配置─────────────────────────────────────────┐         │
│  │  α 齐套权重  [ 40%] ████████░░░░                         │         │
│  │  β 工时权重  [ 30%] ██████░░░░░░                         │         │
│  │  γ 风险权重  [ 30%] ██████░░░░░░                         │         │
│  │                                                           │         │
│  │  关键件到货时间偏移：                                       │         │
│  │  动力电池模组 [ 06-25 ▼ ]  ± [ 2 天] 不确定性             │         │
│  │  安全气囊ECU   [ 06-27 ▼ ]  ± [ 1 天] 不确定性             │         │
│  │                                                           │         │
│  │  [🧮 开始精确排程计算]   [📊 导出排程方案]                │         │
│  └───────────────────────────────────────────────────────────┘         │
│                                                                     │
│  ┌─排程结果：甘特图（核心输出）───────────────────────────────┐         │
│  │  推荐装配顺序：E70-M101 → S597 → E70-M102 → G59-海外版    │         │
│  │  预计总完工时间：2026-06-28 15:00  (较规则模式节省 11h)     │         │
│  │                                                           │         │
│  │  时间轴 ──── 06-23 ──── 06-24 ──── 06-25 ──── 06-26 ──── 06-27 ──▶│
│  │  工位1 ▓▓▓▓▓ E70-M101 ▒▒▒▒ 阻塞 ▓▓▓▓ S597 ...            │         │
│  │  工位2 ░░░░ 等待 ▓▓▓▓▓▓ E70-M101 ░░░ 阻塞 ...            │         │
│  │  工位3 ...                                                 │         │
│  │  工位4 ...                                                 │         │
│  │  工位5 ...                                                 │         │
│  │                                                           │         │
│  │  图例：▓▓▓ 正加工  ▒▒▒ 上游阻塞(工件在本工位等待)           │         │
│  │        ░░░ 工位空闲(下游阻塞/等待进料)  ███ 缺料暂停        │         │
│  └───────────────────────────────────────────────────────────┘         │
│                                                                     │
│  ┌─详细表格（当前V2内容，保留）──────────────────────────────┐         │
│  │  顺序 | 车型         | 关键件到货率 | 状态 | 缺料 | 工时  │         │
│  │   1   | E70-M101...  | 100%         | 可装 |  0   | 8.0h │         │
│  │   2   | S597...      | 96%          | 可装 |  2   | 10.0h│         │
│  │  ...                                                         │         │
│  └───────────────────────────────────────────────────────────┘         │
│                                                                     │
│  ┌─鲁棒性分析报告（NEW）────────────────────────────────────┐         │
│  │  📊 多场景对比分析                                        │         │
│  │                                                           │         │
│  │  场景                | 完工时间    | 延迟 | 风险等级       │         │
│  │  乐观（所有件提前1天）| 06-27 18:00 | -21h | ★ 低          │         │
│  │  基准（按计划到货）  | 06-28 15:00 |  0h  | ★★ 中         │         │
│  │  悲观（关键件延迟2天）| 06-30 11:00 | +44h | ★★★★ 高       │         │
│  │                                                           │         │
│  │  💡 系统建议：将 G59-海外版 调整至最后装配，可将悲观场景    │         │
│  │     完工时间提前至 06-29 14:00（节省 21h）                  │         │
│  │     [点击应用此建议]                                       │         │
│  └───────────────────────────────────────────────────────────┘         │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 2.3 甘特图核心功能说明

**1. 悬停交互**

鼠标悬停在甘特图的任意色块上，弹出浮窗显示：

```
┌────────────────────────────────────────┐
│  车型：E70-M101 标准续航版              │
│  工位：工位2（动力总成）                 │
│  开始时间：2026-06-23 14:30             │
│  结束时间：2026-06-23 22:30             │
│  工时：8.0h                             │
│                                        │
│  🔑 关键件状态：全部到位 ✅              │
│  ⚠️  风险提示：无                        │
│  📈 若延迟1小时 → 后续2台车型平均延迟 0.8h │
└────────────────────────────────────────┘
```

**2. 关键路径高亮**

系统自动计算并高亮**关键路径**上的所有工序（延迟任何一个关键工序，总完工时间必延迟）。关键路径上的工序显示为**深红色边框**，非关键路径为标准色。

**3. 阻塞链追踪**

点击任意"阻塞"色块，系统：
- 高亮显示阻塞来源（上游工位 / 车型）
- 显示阻塞传递链（A阻塞 → B延迟 → C等待）
- 计算消除此阻塞的潜在收益（如：若提前2h完成，总工期节省多少）

---

## 模块 3：到货率看板（增强版）

### 3.1 功能概述

当前的到货率看板是**静态展示**（按车型显示百分比）。V3 版本将其升级为**动态预警系统**：将缺料时间注入 CP-SAT 约束，自动计算影响。

### 3.2 新增界面元素

```
┌─────────────────────────────────────────────────────────────┐
│  📊 关键件到货率看板                                         │
├─────────────────────────────────────────────────────────────┤
│  （保留现有卡片、进度条等元素，新增以下模块）                  │
│                                                             │
│  ┌─排程影响分析（NEW）───────────────────────────────┐       │
│  │  🔴 G59-海外版 到货率 72%                          │       │
│  │  缺料清单：                                        │       │
│  │    · 安全气囊ECU（预计06-27）→ 该车型最早开始: 06-27 │       │
│  │    · 动力电池模组（预计06-25）→ 该车型最早开始: 06-27 │       │
│  │                                                     │       │
│  │  ⚡ 对总排程影响：                                  │       │
│  │     若按计划到货 → 总工期：06-28 15:00              │       │
│  │     若延迟2天到货 → 总工期：06-30 11:00 (+44h)      │       │
│  │                                                     │       │
│  │  🛠  系统建议：                                      │       │
│  │    [加速催料安全气囊ECU]  [将G59移至排程末尾]        │       │
│  └────────────────────────────────────────────────────┘       │
│                                                             │
│  ┌─模拟推演（NEW）─────────────────────────────────────┐       │
│  │  "如果...会怎样？"快速查询                             │       │
│  │  [ 动力电池模组 ] 延迟 [ 3 天] → [🔍 推演]           │       │
│  │  → 影响：E70-M102 延迟开始 3天 → 总工期延迟 2.5天     │       │
│  │      → 建议：将E70-M102与S597交换顺序                 │       │
│  └────────────────────────────────────────────────────┘       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 3.3 与装配顺序推荐的联动

从到货率看板，用户可点击「重算排程」一键跳转到装配顺序推荐页面，并将当前缺料数据自动带入 What-if 参数配置区域。

---

## 模块 4：项目列表（增强版）

### 4.1 功能概述

在项目列表中，新增**CP-SAT 排程状态列**，让用户一眼了解每个项目的排程计算状态和结果。

### 4.2 新增列

```
┌────────┬────────────┬──────────┬────────────┬────────────────┬────────┐
│ 项目号  │ 项目名称    │ 车型     │ 零件数    │ CP-SAT 排程    │ 操作   │
├────────┼────────────┼──────────┼────────────┼────────────────┼────────┤
│ E70-...│ E70换代... │ E70/S73f │ 142       │ ✅ 已排程       │ [查看] │
│        │            │          │           │ 完工: 06-28    │ [重算] │
│        │            │          │           │ 关键件风险: 中  │        │
├────────┼────────────┼──────────┼────────────┼────────────────┼────────┤
│ S597-..│ S597改款...│ S597     │ 98        │ ⏳ 计算中       │ [取消] │
│        │            │          │           │ (12s / 30s)    │        │
├────────┼────────────┼──────────┼────────────┼────────────────┼────────┤
│ G59-...│ G59海外版  │ G59      │ 165       │ ❌ 未排程      │ [开始] │
│        │            │          │           │ 数据不完整      │        │
└────────┴────────────┴──────────┴────────────┴────────────────┴────────┘
```

---

# **第三部分：技术实现方案**

## 3.1 技术选型总结

| 组件 | 选择 | 说明 |
|------|------|------|
| **求解器** | Google OR-Tools CP-SAT（本地 Python 库） | `pip install ortools` |
| **排程模型** | BFWS 阻塞流水车间 | 匹配装配线物理约束 |
| **后端集成** | FastAPI 同步调用 | <30秒硬时限，用户可接受等待 |
| **前端可视化** | ECharts Gantt + 自定义甘特图 | 与现有技术栈一致 |
| **鲁棒性分析** | 多场景 CP-SAT 并行求解 | 3个场景（乐观/基准/悲观） |

## 3.2 后端架构设计

```
spiderV5/
├── main.py                    # FastAPI 入口（现有）
├── api/
│   ├── project.py             # 项目接口（现有）
│   ├── parts.py               # 零件/PBOM 接口（现有）
│   ├── critical.py            # 关键件评分（现有）
│   ├── dedup.py               # 数据去重（现有）
│   └── schedule.py            # ⭐ 排程接口（新增）
│
├── services/
│   ├── part_scoring.py        # 4维度评分服务（现有）
│   ├── data_dedup.py          # 去重合并服务（现有）
│   └── scheduling/            # ⭐ 排程服务目录（新增）
│       ├── __init__.py
│       ├── bfw_model.py       # BFWS 模型构建器
│       ├── cp_sat_solver.py   # CP-SAT 求解器封装
│       ├── gantt_formatter.py # 甘特图数据格式化
│       └── robustness.py      # 鲁棒性分析
│
├── models/
│   ├── project.py             # 项目数据模型（现有）
│   ├── parts.py               # 零件数据模型（现有）
│   └── schedule.py            # ⭐ 排程数据模型（新增）
│
└── requirements.txt
    ├── fastapi
    ├── uvicorn
    ├── sqlalchemy
    ├── openpyxl
    └── ortools==9.10.*        # ⭐ 新增依赖
```

## 3.3 核心 API 设计（新增）

### `POST /api/schedule/compute`

计算指定项目的装配排程

**Request:**
```json
{
  "project_id": "E70-2026-001",
  "mode": "cpsat",
  "time_limit_seconds": 30,
  "weights": {
    "makespan": 40,
    "processing_time": 30,
    "risk": 30
  },
  "robustness_analysis": true,
  "part_delay_overrides": [
    { "part_code": "Z920001S7301", "delay_days": 2 }
  ]
}
```

**Response:**
```json
{
  "status": "completed",
  "computation_time_ms": 18432,
  "recommended_sequence": ["E70-M101", "S597", "E70-M102", "G59"],
  "makespan": "2026-06-28T15:00:00",
  "makespan_vs_rule_improvement_hours": 11.0,
  "gantt_data": {
    "timeline_start": "2026-06-23T08:00:00",
    "timeline_end": "2026-06-28T15:00:00",
    "stations": [
      {
        "station_id": "station_1",
        "name": "工位1（底盘装配）",
        "tasks": [
          { "model": "E70-M101", "start": "...", "end": "...", "status": "processing" }
        ]
      }
    ]
  },
  "detailed_table": [],
  "robustness_report": {
    "optimistic": {},
    "baseline": {},
    "pessimistic": {}
  }
}
```

## 3.4 BFWS + CP-SAT 核心建模代码（设计）

```python
# services/scheduling/bfw_model.py
from ortools.sat.python import cp_model
from datetime import datetime, timedelta
from typing import List, Dict

class BFWSSchedulingModel:
    """阻塞流水车间排程模型构建器"""

    def __init__(self, project_data: Dict):
        self.models = project_data['assembly_models']
        self.stations = project_data['stations']
        self.part_arrivals = project_data['part_arrivals']
        self.critical_scores = project_data['critical_scores']

    def build_model(self, weights: Dict) -> cp_model.CpModel:
        """构建 CP-SAT 模型"""
        model = cp_model.CpModel()
        num_models = len(self.models)
        num_stations = len(self.stations)

        # ====== 1. 计算时间跨度（Horizon）======
        total_processing = sum(
            model['processing_times'][s]
            for model in self.models
            for s in range(num_stations)
        )
        horizon = total_processing * 2

        # ====== 2. 创建决策变量 ======
        start = {}
        end = {}
        intervals = {}

        for m in range(num_models):
            for s in range(num_stations):
                suffix = f'm{m}_s{s}'
                start[(m, s)] = model.NewIntVar(0, horizon, f'start_{suffix}')
                end[(m, s)] = model.NewIntVar(0, horizon, f'end_{suffix}')
                intervals[(m, s)] = model.NewIntervalVar(
                    start[(m, s)],
                    self.models[m]['processing_times'][s],
                    end[(m, s)],
                    f'interval_{suffix}'
                )

        # ====== 3. BFWS 核心约束 ======
        for m in range(num_models):
            for s in range(num_stations - 1):
                model.Add(start[(m, s + 1)] >= end[(m, s)])

        for s in range(num_stations):
            station_intervals = [intervals[(m, s)] for m in range(num_models)]
            model.AddNoOverlap(station_intervals)

        for m in range(num_models):
            model_key = self.models[m]['model_key']
            latest_arrival = self._get_latest_critical_arrival(model_key)
            if latest_arrival > 0:
                model.Add(start[(m, 0)] >= latest_arrival)

        # ====== 4. 目标函数：加权多目标优化 ======
        makespan = model.NewIntVar(0, horizon, 'makespan')
        model.AddMaxEquality(
            makespan,
            [end[(m, num_stations - 1)] for m in range(num_models)]
        )

        weighted_wait_penalty = []
        for m in range(num_models):
            arrival = self._get_latest_critical_arrival(self.models[m]['model_key'])
            score = self._get_avg_critical_score(self.models[m]['model_key'])
            wait_time = start[(m, 0)] - arrival
            weighted_wait_penalty.append(wait_time * int(score * 100))

        total_penalty = model.NewIntVar(0, horizon * 100, 'total_penalty')
        model.Add(total_penalty == sum(weighted_wait_penalty))

        alpha = weights.get('makespan', 40)
        gamma = weights.get('risk', 30)
        model.Minimize(alpha * makespan + gamma * total_penalty)

        return model

    def _get_latest_critical_arrival(self, model_key: str) -> int:
        arrivals = [
            p['arrival_offset_hours']
            for p in self.part_arrivals
            if p['used_by_model'] == model_key and p['critical_score'] >= 3.5
        ]
        return max(arrivals) if arrivals else 0

    def _get_avg_critical_score(self, model_key: str) -> float:
        scores = [
            p['critical_score']
            for p in self.part_arrivals
            if p['used_by_model'] == model_key
        ]
        return sum(scores) / len(scores) if scores else 3.0
```

## 3.5 前端改动清单

### 文件：`index.html`

**新增代码段：**
1. 甘特图渲染函数：`renderGanttChart(data)` — 使用 ECharts Gantt 或自定义 SVG 实现交互式甘特图
2. CP-SAT 状态轮询：`pollScheduleStatus(request_id)` — 轮询后端计算状态
3. 鲁棒性报告渲染：`renderRobustnessReport(data)` — 展示多场景对比

**现有函数改动：**

| 函数 | 改动 |
|------|------|
| `renderAssemblyPlan()` | 新增模式选择卡片、What-if 参数配置区、甘特图区域、鲁棒性报告区域 |
| `renderDashboard()` | 新增排程影响分析模块、模拟推演模块 |
| `renderProjectList()` | 新增 CP-SAT 排程状态列 |

---

# **第四部分：落地实施路径**

## 4.1 开发优先级（分阶段）

```
阶段 1：核心排程引擎（1-2周）
├── 搭建后端排程服务骨架（services/scheduling/）
├── 实现 BFWS 基础模型构建（bfw_model.py）
├── 实现 CP-SAT 求解器封装（cp_sat_solver.py）
├── 实现排程 API（api/schedule.py）
└── 单元测试：用标准 BFWS benchmark 验证正确性

阶段 2：前端可视化（1周）
├── 装配顺序推荐页面重构（甘特图 + 表格）
├── 求解模式选择 UI
├── What-if 参数配置交互
└── 排程结果渲染与状态管理

阶段 3：鲁棒性与联动（1周）
├── 实现多场景鲁棒性分析
├── 到货率看板联动改造
├── 项目列表排程状态
└── 关键件评分页面增强

阶段 4：优化与打磨（1周）
├── CP-SAT 性能调优（初始解注入 + 搜索策略配置）
├── 错误处理与边界情况（求解失败/超时）
├── 用户体验优化（加载动画、进度条、取消计算）
└── 集成测试与文档完善
```

## 4.2 风险评估

| 风险 | 影响 | 应对策略 |
|------|------|---------|
| **OR-Tools 安装兼容性** | 中 | 使用 pinned 版本 `ortools==9.10.4067` |
| **CP-SAT 求解超时** | 高 | 1. 30秒硬时限；2. Anytime 特性；3. 规则引擎初解加速 |
| **甘特图复杂度** | 中 | 先用 ECharts Gantt 简化实现 |
| **用户理解成本** | 中 | 保留"快速模式"，CP-SAT 模式提供说明对比 |

## 4.3 性能预期

| 场景 | 车型数 | 工位数 | CP-SAT 求解时间 | 规则引擎 |
|------|--------|--------|----------------|---------|
| 小型 | 4 | 5 | 2-5s | <1s |
| 中型 | 8 | 5 | 8-15s | <1s |
| 大型 | 15 | 8 | 20-30s | <2s |
| 超大型 | 25+ | 10+ | 30s（返回当前最优） | <5s |

---

*文档版本 V3 · 生成于 2026-06-23 · BFWS + CP-SAT 排程系统设计*