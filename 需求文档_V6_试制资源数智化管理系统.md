# spiderV6 — 试制资源数智化管理系统 — 需求文档 V6.0

> **编写日期：2026-07-29**
> **版本：V6.0**
> **核心价值：通过设备台账、人员看板、任务管理、排程调度、异常预警等模块，实现试制车间全资源实时监控与智能调配。**

---

# 第一部分：系统概述与定位

## 1.1 产品定位

| 维度 | 说明 |
|------|------|
| **产品类型** | 工业制造 Web 管理平台（资源管理子系统） |
| **目标用户** | 试制车间管理员、工艺主管、调度员 |
| **业务领域** | 试制车间设备管理、人员调度、任务跟踪、排程优化 |
| **界面语言** | 中文 |
| **技术栈** | 前端：React + TypeScript + Tailwind CSS + ECharts 5 · 后端：FastAPI + MariaDB |
| **设计风格** | 深色科技风（Dark Theme），品牌色 #1677FF 科技蓝 |

## 1.2 核心模块概览

| 序号 | 模块名称 | 原型页面 | 核心功能 |
|------|---------|---------|---------|
| 1 | 综合驾驶舱 | 综合驾驶舱.html | 全局KPI、设备状态分布、车间实时布局、异常告警 |
| 2 | 设备台账 | 设备台账.html | 设备信息管理、状态监控、维护记录 |
| 3 | 园区地图 | 园区地图.html | 车间平面图、区域分布、设备位置可视化 |
| 4 | 资源占用看板 | 资源占用看板.html | 实时资源占用率、利用率趋势 |
| 5 | 人员看板 | 人员看板.html | 人员实时位置、状态、技能、工作负载 |
| 6 | 甘特图排程 | 甘特图排程.html | 任务甘特图、时间线调度、资源冲突检测 |
| 7 | 人效分析 | 人效分析看板.html | 人员效率统计、工时分析、绩效报表 |
| 8 | 异常预警中心 | 异常预警中心.html | 设备异常、人员异常、任务异常实时告警 |
| 9 | 任务管理 | 任务管理.html | 零星/ABC类/试制岛任务的CRUD、状态追踪 |

## 1.3 与 spiderV5 的关系

本模块为 **spiderV5_optimized** 的扩展子系统，定位为**试制资源管理**领域，与现有**到货数据管理**模块形成互补：

| 对比项 | spiderV5（现有） | spiderV6（新增） |
|--------|----------------|-----------------|
| 业务领域 | 仓库到货、PBOM匹配 | 车间资源、人员调度 |
| 核心实体 | 项目、零件、到货单 | 设备、人员、任务、区域 |
| 数据源 | 360系统到货接口 | 360系统、MES、运营表 |
| API前缀 | `/api/projects/*` | `/api/resource/*` |
| 前端路由 | `/projects/*` | `/resource/*` |

## 1.4 导航结构（新增）

### 一级导航（新增"试制资源"菜单组）

| 一级导航 | 二级Tab页面 | 说明 |
|---------|-----------|------|
| **试制资源** | 综合驾驶舱 | 全局概览仪表盘 |
| | 设备台账 | 设备管理与监控 |
| | 园区地图 | 车间布局可视化 |
| | 资源占用 | 实时占用率看板 |
| | 人员看板 | 人员位置与状态 |
| | 甘特排程 | 任务甘特图调度 |
| | 人效分析 | 人员效率统计 |
| | 异常预警 | 实时告警中心 |
| | 任务管理 | 任务CRUD管理 |

### 面包屑层级

```
试制资源 › 综合驾驶舱
试制资源 › 设备台账
试制资源 › 人员看板
试制资源 › 任务管理
试制资源 › 异常预警
...
```

---

# 第二部分：数据模型设计

## 2.1 数据库表设计

### 2.1.1 设备表 `equipment`

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INT | PRIMARY KEY, AUTO_INCREMENT | 自增主键 |
| equipment_code | VARCHAR(50) | NOT NULL, UNIQUE | 设备编号（如 SZC-01） |
| equipment_name | VARCHAR(100) | NOT NULL | 设备名称 |
| equipment_type | ENUM('lift','island','station') | NOT NULL | 设备类型：举升机/试制岛/工位 |
| zone_code | VARCHAR(50) | NOT NULL | 所属区域编码（外键 → zones） |
| status | ENUM('idle','busy','error','maintenance') | NOT NULL, DEFAULT 'idle' | 设备状态：空闲/占用/故障/维护 |
| current_task_id | INT | NULL, FK → tasks.id | 当前占用的任务ID |
| current_operator | VARCHAR(100) | NULL | 当前操作员姓名 |
| last_update_time | DATETIME | ON UPDATE CURRENT_TIMESTAMP | 最后更新时间 |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE | 更新时间 |

### 2.1.2 区域表 `zones`

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INT | PRIMARY KEY, AUTO_INCREMENT | 自增主键 |
| zone_code | VARCHAR(50) | NOT NULL, UNIQUE | 区域编码（如 SZC、SZA、SZB） |
| zone_name | VARCHAR(100) | NOT NULL | 区域名称（如 仓库装配区） |
| zone_type | ENUM('assembly','island','prototype','external') | NOT NULL | 区域类型 |
| position_x | INT | NULL | 地图X坐标 |
| position_y | INT | NULL | 地图Y坐标 |
| grid_columns | INT | DEFAULT 1 | 网格列数 |
| grid_rows | INT | DEFAULT 1 | 网格行数 |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE | 更新时间 |

### 2.1.3 人员表 `personnel`

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INT | PRIMARY KEY, AUTO_INCREMENT | 自增主键 |
| personnel_code | VARCHAR(50) | NOT NULL, UNIQUE | 工号 |
| name | VARCHAR(50) | NOT NULL | 姓名 |
| avatar_text | VARCHAR(10) | NULL | 头像文字（姓氏） |
| department | VARCHAR(100) | NULL | 人力来源 |
| status | ENUM('working','idle','offline') | NOT NULL, DEFAULT 'offline' | 当前状态：工作中/空闲/离线 |
| current_zone | VARCHAR(50) | NULL, FK → zones.zone_code | 当前所在区域 |
| current_task_id | INT | NULL, FK → tasks.id | 当前任务ID |
| entry_time | DATETIME | NULL | 上班时间 |
| last_update_time | DATETIME | ON UPDATE CURRENT_TIMESTAMP | 最后更新时间 |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE | 更新时间 |

### 2.1.4 任务表 `tasks`

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INT | PRIMARY KEY, AUTO_INCREMENT | 自增主键 |
| task_code | VARCHAR(50) | NOT NULL, UNIQUE | 任务编号 |
| task_name | VARCHAR(200) | NOT NULL | 任务名称 |
| task_type | ENUM('sporadic','abc','island') | NOT NULL | 任务类型：零星/ABC类/试制岛 |
| project_code | VARCHAR(100) | NULL | 关联项目编号 |
| vehicle_code | VARCHAR(100) | NULL | 车号 |
| vehicle_model | VARCHAR(100) | NULL | 车型 |
| priority | ENUM('high','medium','low') | NOT NULL, DEFAULT 'medium' | 优先级 |
| status | ENUM('pending','in_progress','completed','overdue') | NOT NULL, DEFAULT 'pending' | 状态：待开始/进行中/已完成/逾期 |
| zone_code | VARCHAR(50) | NULL, FK → zones.zone_code | 占用区域 |
| equipment_code | VARCHAR(50) | NULL, FK → equipment.equipment_code | 占用设备 |
| planner | VARCHAR(50) | NULL | 负责人（可多人逗号分隔） |
| plan_start_time | DATETIME | NULL | 计划开始时间 |
| plan_end_time | DATETIME | NULL | 计划结束时间 |
| plan_work_hours | DECIMAL(8,2) | NULL | 计划工时 |
| actual_work_hours | DECIMAL(8,2) | DEFAULT 0 | 实际工时 |
| progress | DECIMAL(5,2) | DEFAULT 0 | 进度百分比 |
| source | ENUM('operation','manual','mes') | NOT NULL, DEFAULT 'manual' | 数据来源：360运营表/手工/MES |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE | 更新时间 |

### 2.1.5 异常预警表 `alerts`

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INT | PRIMARY KEY, AUTO_INCREMENT | 自增主键 |
| alert_code | VARCHAR(50) | NOT NULL, UNIQUE | 预警编号 |
| alert_type | ENUM('equipment_timeout','unmanned_operation','personnel_abnormal','task_delay') | NOT NULL | 预警类型 |
| level | ENUM('critical','warning','info') | NOT NULL | 级别：严重/警告/信息 |
| title | VARCHAR(200) | NOT NULL | 预警标题 |
| description | TEXT | NULL | 预警描述 |
| related_equipment | VARCHAR(50) | NULL, FK → equipment.equipment_code | 关联设备 |
| related_task | INT | NULL, FK → tasks.id | 关联任务 |
| related_personnel | VARCHAR(50) | NULL, FK → personnel.personnel_code | 关联人员 |
| status | ENUM('pending','processing','resolved') | NOT NULL, DEFAULT 'pending' | 处理状态 |
| raised_at | DATETIME | NOT NULL | 触发时间 |
| resolved_at | DATETIME | NULL | 解决时间 |
| handler | VARCHAR(50) | NULL | 处理人 |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE | 更新时间 |

### 2.1.6 人员效率统计表 `personnel_efficiency`

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INT | PRIMARY KEY, AUTO_INCREMENT | 自增主键 |
| personnel_code | VARCHAR(50) | NOT NULL, FK → personnel.personnel_code | 工号 |
| stat_date | DATE | NOT NULL | 统计日期 |
| total_work_hours | DECIMAL(8,2) | DEFAULT 0 | 总工时 |
| effective_work_hours | DECIMAL(8,2) | DEFAULT 0 | 有效工时 |
| task_count | INT | DEFAULT 0 | 完成任务数 |
| equipment_usage_rate | DECIMAL(5,2) | DEFAULT 0 | 设备使用率 |
| zone_code | VARCHAR(50) | NULL | 主要工作区域 |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| UNIQUE KEY | (personnel_code, stat_date) | 联合唯一索引 |

### 2.1.7 设备维护记录表 `equipment_maintenance`

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INT | PRIMARY KEY, AUTO_INCREMENT | 自增主键 |
| equipment_code | VARCHAR(50) | NOT NULL, FK → equipment.equipment_code | 设备编号 |
| maintenance_type | ENUM('routine','repair','inspection') | NOT NULL | 维护类型：例行/维修/巡检 |
| start_time | DATETIME | NOT NULL | 开始时间 |
| end_time | DATETIME | NULL | 结束时间 |
| operator | VARCHAR(50) | NULL | 维护人员 |
| description | TEXT | NULL | 维护描述 |
| status | ENUM('in_progress','completed','cancelled') | NOT NULL, DEFAULT 'in_progress' | 状态 |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

---

# 第三部分：功能模块详细规格

## 3.1 模块一：综合驾驶舱

### 3.1.1 功能描述

全局概览仪表盘，实时展示试制车间核心运营指标，支持快速定位异常。

### 3.1.2 页面组成

```
┌─────────────────────────────────────────────────────────────┐
│  顶部标题栏：Logo · 系统标题 · 实时时间 · 车间环境 · 用户    │
├─────────────────────────────────────────────────────────────┤
│  KPI行：设备总数 | 设备占用率 | 在线人员 | 进行中任务 |       │
│        今日完工 | 异常告警                                    │
├────────────────┬────────────────────────────┬───────────────┤
│ 左栏：         │  中间：车间实时布局              │ 右栏：       │
│ · 设备状态分布 │  (网格布局，显示所有              │ · 异常告警列表│
│   (饼图/环形)  │  区域相对位置和设备状态)          │ · 当前任务	 │
│ · 人员状态     │  · 设备颜色标识：               │   (进度条)    │
│   (条形图)     │    空闲/占用/故障/维护          │ · 利用率趋势 │
│                │                             │   (折线图)    │
├────────────────┴────────────────────────────┴───────────────┤
│  底部状态栏：系统状态 · 数据同步状态 · 最后更新时间          │
└─────────────────────────────────────────────────────────────┘
```

### 3.1.3 数据需求

| 数据项 | 数据来源 | 刷新频率 |
|--------|---------|---------|
| 设备总数/状态分布 | equipment 表 | 实时（30秒） |
| 人员状态统计 | personnel 表 | 实时（30秒） |
| 进行中任务数 | tasks 表（status=in_progress） | 实时 |
| 今日完工数 | tasks 表（今日 completed） | 实时 |
| 异常告警列表 | alerts 表（status=pending） | 实时 |
| 车间环境数据 | IoT/传感器（预留接口） | 每分钟 |

### 3.1.4 交互要求

1. 设备格子悬浮显示详情 tooltip（编号、状态、关联任务、操作员）
2. 点击设备格子可跳转至设备台账详情
3. 异常告警可点击"处理"按钮
4. 进度条根据完成百分比显示不同颜色

---

## 3.2 模块二：设备台账

### 3.2.1 功能描述

设备全生命周期管理，包括台账信息维护、状态实时监控、维护记录追踪。

### 3.2.2 页面组成

```
┌─────────────────────────────────────────────────────────────┐
│  页面标题 + 筛选器（区域/类型/状态） + 搜索框 + 新增按钮     │
├─────────────────────────────────────────────────────────────┤
│  KPI统计卡片：设备总数 | 空闲 | 占用 | 故障 | 维护中         │
├─────────────────────────────────────────────────────────────┤
│  设备列表表格：                                            │
│  编号 | 名称 | 类型 | 区域 | 状态 | 当前任务 | 操作员 |       │
│  最后更新 | 操作（详情/编辑/维护记录）                       │
├─────────────────────────────────────────────────────────────┤
│  分页 + 批量操作                                           │
└─────────────────────────────────────────────────────────────┘
```

### 3.2.3 功能清单

| 功能 | 说明 |
|------|------|
| 设备列表查询 | 支持按区域、类型、状态筛选，支持关键词搜索 |
| 设备详情查看 | 展示设备基本信息、当前状态、历史任务、维护记录 |
| 新增设备 | 录入设备基本信息 |
| 编辑设备 | 修改设备信息 |
| 状态更新 | 手动更新设备状态（或通过扫码自动更新） |
| 维护记录 | 查看/新增维护记录 |
| 批量操作 | 批量修改状态、批量删除 |

### 3.2.4 数据需求

- 主表：`equipment`
- 关联表：`zones`、`equipment_maintenance`、`tasks`

---

## 3.3 模块三：园区地图

### 3.3.1 功能描述

园区地图由两个 Tab 视图构成：

- **Tab 1 · 车间实时布局（示意图）**：车间 SVG 平面图可视化，展示各区域分布、设备布局、人员位置（人形图标），支持设备层/人员层切换与状态筛选，图例固定显示于平面图左下角。
- **Tab 2 · 资源俯瞰总览图（卫星鸟瞰）**：基于园区真实卫星/鸟瞰图，在对应车间建筑位置上叠加状态指示标签（脉冲环 + 车间名称 + 设备数/人员数微标），直接从宏观视角俯瞰全园区车间运行状态（正常运行 / 存在预警 / 有故障设备 / 空闲轻载），支持缩放、拖拽与点击跳转到对应车间的详细布局。

### 3.3.2 页面组成

```
┌─────────────────────────────────────────────────────────────┐
│  Tab：  [■ 车间实时布局]  [  资源俯瞰总览图  ]              │
├─────────────────────────────────────────────────────────────┤
│ Tab 1 · 车间实时布局（SVG 示意图）：                       │
│  ┌────────┬────────────┬────────────┐                       │
│  │ SZC    │ SZA 试制岛  │ SZB        │                       │
│  │ 仓库   │            │ 装配二期   │                       │
│  ├────────┼────────────┼────────────┤                       │
│  │ JP1    │ 		     │ JP2        │                       │
│  │ 竞品一区│            │  竞品二区   │                       │
│  ├────────┴────────────┴────────────┤                       │
│  │          LH 联合装配区             │                       │
│  ├──────────────────────────────────┤                       │
│  │          创高科技园区	          │                       │
│  └──────────────────────────────────┘                       │
│  · 设备格子按状态着色                                       │
│  · 人员显示为"人形"小图标（小人形状），按状态着色           │
│  · 图例固定显示于 SVG 平面图左下角，与车间平面图区分        │
│  · 支持缩放、拖拽                                          │
├─────────────────────────────────────────────────────────────┤
│ Tab 2 · 资源俯瞰总览图（卫星鸟瞰 + 状态指示）：             │
│  ┌───────────────────────────────────────────────────────┐  │
│  │   园区卫星鸟瞰底图                │  │
│  │   · 各车间建筑位置交互元素：多个蓝色圆点标记悬浮在对应建筑    │  │
│  │        🔵 绿环（good） 正常运行                         │  │
│  │        🟡 黄环（warn） 存在预警                         │  │
│  │        🔴 红环（bad）  有故障设备                        │  │
│  │        ⚪ 灰环（idle） 空闲/轻载                        │  │
│  │   · 脉冲环下方：车间名称标签 + 设备数/人员数微标         │  │
│  │   · 顶部悬浮 KPI 条：车间区域 | 设备总数 | 在岗人员 |   │  │
│  │                              异常车间                   │  │
│  │   · 右下角：状态图例（4 色）                            │  │
│  │   · 右上角：缩放/缩小/重置/全屏 工具按钮                │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

### 3.3.3 交互要求

1. 车间布局视图（Tab 1）：
   1. 点击区域可高亮显示该区域内所有设备
   2. 点击人形图标显示人员详情 tooltip
   3. 点击设备格子显示设备详情
   4. 支持按状态筛选（空闲/占用/故障/维护）
   5. 支持人员/设备层切换
   6. 左下角固定显示设备状态 + 人员状态 图例（与平面图视觉区分）
2. 资源俯瞰总览图（Tab 2）：
   1. 鼠标悬停车间标记 → 显示 tooltip（车间名、设备数、运行/空闲/预警/故障、人员数）
   2. 点击车间标记 → 自动切换到"车间实时布局"Tab 并高亮对应车间区域
   3. 支持图片缩放（滚轮）、平移（拖拽）、工具按钮（放大/缩小/重置/全屏）
   4. 顶部 KPI 条实时汇总：车间区域、设备总数（运行/空闲）、在岗人员（作业/空闲）、异常车间数
   5. 右下角状态图例：正常运行、存在预警、有故障设备、空闲轻载

---

## 3.4 模块四：资源占用看板

### 3.4.1 功能描述

以"试制排程矩阵"为核心，按场地（装配区 SZA/SZC/SZB、竞品区 JP1/JP2、联合区 LH、外委区 CX1/CX2）逐行列出**各场地的试制能力、单元机台/设备配置、具体项目与预算采购进度、以及按周展开的排程甘特（颜色区分 已完成/进行中/待排/排定中）**，支撑 AB 类项目优先落试制车间内的管理原则。

### 3.4.2 页面组成

```
┌───────────────────────────────────────────────────────────────────────────────────────────────┐
│   KPI 条：场地总数 | 项目排程 | 设备/机台数 | 预算总额 | 累计采购 | 场地占用率                │
├───────────────────────────────────────────────────────────────────────────────────────────────┤
│  [图例] 已完成 深绿 | 进行中 浅绿描边 | 待排 黄 | 排定中 红     | 优先级：A类/ B类 / C类       │
├───────────────────────────────────────────────────────────────────────────────────────────────┤
│  左侧固定信息列         │ 周度排程（颜色甘特格，10 周+）                                       │
│ 试料阵地│地点/举升机输了       │试制能力            │项目 │样车数量│特殊装车数量│已完成  │占用资源│ 7-W29 │ ... │  │
│──────────────────────────────────────────────────────────────────────────────────────────── │
│  SZA  │装配一期 试制岛产线  │B类40台/2项目       │ S3JET0 B类│71│ 71│ 3/71  │ 2台举升机│ [绿3台]│     │  │
│  SZC  │仓库装配 20台  │B类20台 /10台举升   │ DH3    C类│56│ 10│ 8/10   │7月10日2台│ [绿8台]│     │  │
│  SZB  │装配二期 24台  │B类24台/6台举升    │ S599841 B │ 8│ 10│ 3/10   │4台举升机 │ [浅绿] │ [黄10台] │ │
│  (多项目并行) │              │                 │ 乐风翼子 C │ 7│ 12│12/12    │2台举升机 │      │ [绿12台]  │
│  JP1  │竞品一区 12台  │B类12台/3台举升     │ DHTMPV A  │11│ 4 │ 4/4    │4台举升机 │      │ [绿4台]   │
│  (多项目并行) │              │                 │ 7J38E10 C │26│ 26│ 0/26   │2台举升机 │      │ [浅绿]    │
│  JP2  │竞品二区 16台  │B类16台/4台举升     │ N1F1   B  │ 7│ 7 │ 0/7    │3台举升机 │      │      │ [红月14日]│
│  (多项目并行) │              │                 │ N356   A  │33│ 33│ 0/33   │6台举升机 │      │      │ [黄33台] │
│  LH   │联合装配区 12台│B类12台/3-5台举升   │ M18MPV B  │ 6│ 10│ 7/10   │8台举升机 │ [浅绿7/10]│   │
│  CX1  │外委畅行 52台  │B类52台/13台举升    │ 300HEV B  │ 7│ 13│ 7/13   │7月3日3台 │      │ [浅绿]    │
│  (多项目并行) │              │                 │ BD515  C  │ 3│ 3 │ 0/3    │3台举升机 │      │ [黄3台]   │
│  CX2  │外委交石 44台  │B类44台/11台举升    │ 200HEV B  │ 7│ 13│ 0/13   │7月30日7台│      │      │ [红月30日]│
│  (多项目并行) │              │                 │ E70702 C  │ 3│ 7 │ 0/7    │1台举升机 │      │      │ [黄7台]  │
└───────────────────────────────────────────────────────────────────────────────────────────────┘
```

### 3.4.3 场地试制能力与设备配置

| 场地ID | 场地名称         | 设备类型 | 举升机数 | 试制能力 (B类当量)        | 单元机台/设备说明                  |
|--------|------------------|--------|----------|---------------------------|-------------------------------------|
| SZA    | 装配一期车间    | 试制岛    | 0        | 2个项目 或 单项目B类40台  | 试制岛产线（串岛）             |
| SZC    | 仓库装配区      | 举升机  | 5        | 单项目B类当量 20台        | 单排 5台 + 两排 5台举升机（共10台） |
| SZB    | 装配二期车间    | 举升机  | 6        | 2个项目 或 单项目B类24台  | 6台举升机                           |
| JP1    | 竞品装配一区    | 举升机  | 3        | 单项目B类当量 12台        | 3台举升机                           |
| JP2    | 竞品装配二区    | 举升机  | 4        | 单项目B类当量 16台        | 4台举升机                           |
| LH     | 联合装配区      | 举升机  | 5        | 单项目B类当量 12台        | 3-5台举升机                         |
| CX    | 外委(畅行创高新科) | 举升机 | 50      | 2个项目 或 单项目B类200台 | M18-300HEV/BD-515 合计13台举升     |

> **管理原则**：A/B 类项目优先排产在 SZA/SZC/SZB/LH 等车间内；外委区 CX承担批量/剩余项目。

### 3.4.4 交互要求

1. 左侧 8 列（试料料号 / 单元机台设备 / 试制能力 / 项目 / 零件费总预算 / 采购数量 / 累计已完成 / 心明资源）**滚动时固定**，右侧周度甘特格可横向滚动
2. 周度排程单元格颜色语义：
   - 深绿（done）：已完成，显示"X台"
   - 浅绿 + 描边（doing）：进行中，显示"已完成/总数量"
   - 黄（wait）：待排，显示"剩余 X 台"
   - 红（plan）：排定中，显示预计开始日期
3. 顶部快捷筛选：全部场地 / 装配车间 / 竞品区 / 外委区 / A类项目 / B类项目
4. 左侧侧栏：场地类型筛选 / 项目类别筛选 / 排程状态筛选 + 筛选统计（场地/项目/预算合计）
5. 工具条：全宽显示（隐藏侧栏）/ 导出 Excel / 刷新 / 新增排程
6. KPI 条：本周占用率 / 下周占用率 等滚动指标

---

## 3.5 模块五：人员看板

### 3.5.1 功能描述

实时监控所有试制人员的位置、状态、所在区域与工作负载，**装配区人员引入"人员来源"维度（商用车 / 乘用车 / 柳汽 / 中智 / 外协 / 内调 等）**，用于灵活用工调配。**取消团队分组与技能分组作为筛选维度**。

### 3.5.2 分组维度说明

| 分组维度     | 是否保留 | 适用范围           | 可选项                                      |
|--------------|----------|--------------------|---------------------------------------------|
| 状态分布     | ✅ 保留  | 全部人员           | 工作中 / 空闲 / 离线 / 预警                |
| 区域筛选     | ✅ 保留  | 全部人员           | SZA / SZB / SZC / JP1 / JP2 / LH / CX |
| **人员来源** | ✅ **新增** | 全部人员 | 自有 / 商用车 / 乘用车 / 柳汽 / 中智 / 外协 |

### 3.5.3 页面组成

```
┌─────────────────────────────────────────────────────────────┐
│  页面标题 + 人员调配/排班表/刷新按钮                        │
├─────────────────────────────────────────────────────────────┤
│  KPI卡片：人员总数 | 在岗 | 工作中 | 空闲可调配 | 今日异常  │
├──────────────────────┬──────────────────────────────────────┤
│ 左侧侧栏筛选：       │ 主视图（ECharts + 表格列表）：        │
│ · 状态分布           │  上：人员来源分布饼图（装配区）        │
│ · 区域筛选           │       图例：商用车/乘用车/柳汽/中智   │
│ · 人员来源(装配区)   │            /外协/内调/非装配区        │
│   商用车/乘用车/柳汽 │  中：装配区 来源统计柱状图            │
│   中智/外协/内调     │       按来源显示 人员数量 / 平均工时  │
│                      │  下：人员明细表                       │
│                      │   姓名 | 工号 | 位置(区域) | 来源     │
│                      │   状态 | 今日工时 | 工作中任务         │
│                      │   （装配区才显示"来源"列，非装配区留白）│
└──────────────────────┴──────────────────────────────────────┘
```

### 3.5.4 数据需求

- 主表：`personnel`
- 关键字段新增：
  - `source`：人员来源（`自有 / 商用车 / 乘用车 / 柳汽 / 中智 / 外协`）
- 关联表：`zones`、`tasks`

### 3.5.5 交互要求

1. 人员列表支持折叠/展开，支持搜索
2. 点击人员可查看/编辑详情，详情面板"基本信息"包含 **人员来源高亮字段
3. 空闲人员可拖拽分配任务
4. 人员位置实时更新（30秒刷新）
5. 左侧"人员来源"筛选仅作用于装配区；勾选某来源时保留其他来源与非装配区可复选

---

## 3.6 模块六：甘特图排程

### 3.6.1 功能描述

以甘特图形式展示所有任务的时间线排程，支持拖拽调整、资源冲突检测。

### 3.6.2 页面组成

```
┌─────────────────────────────────────────────────────────────┐
│  页面标题 + 时间粒度切换（日/周/月）+ 新增任务 + 筛选       │
├─────────────────────────────────────────────────────────────┤
│  甘特图区域：                                              │
│  左侧：任务列表（名称/类型/优先级/负责人）                 │
│  右侧：时间线（按天/周显示）                               │
│  · 任务条按类型着色（试制岛/ABC类/零星）                    │
│  · 进度条显示完成百分比                                    │
│  · 关键路径高亮                                           │
│  · 资源冲突标记                                           │
├─────────────────────────────────────────────────────────────┤
│  资源冲突提示面板                                          │
└─────────────────────────────────────────────────────────────┘
```

### 3.6.3 功能清单

| 功能 | 说明 |
|------|------|
| 甘特图渲染 | 按时间粒度（日/周/月）展示任务排程 |
| 拖拽调整 | 支持拖动任务条调整开始/结束时间 |
| 资源冲突检测 | 同一设备/人员在同一时间被多个任务占用时高亮提示 |
| 关键路径 | 自动计算并高亮关键路径上的任务 |
| 任务筛选 | 按类型、状态、优先级、负责人筛选 |

---

## 3.7 模块七：人效分析

### 3.7.1 功能描述

人员效率统计分析，以**装配区人员来源为核心分组维度**（自有 / 商用车 / 乘用车 / 柳汽 / 中智 / 外协），取消团队分组与技能分组，支持按来源统计工时、任务完成率、效率评分与综合效率指数。

### 3.7.2 分组维度调整

| 分组维度     | 是否保留 | 适用范围           | 可选项                                                       |
|--------------|----------|--------------------|--------------------------------------------------------------|
| 团队分组     | ❌ 取消  | —                  | —                                                            |
| 技能分组     | ❌ 取消  | —                  | —                                                            |
| 日期范围     | ✅ 保留  | 全部人效数据       | 近7日 / 近30日 / 自定义                                      |
| 区域筛选     | ✅ 保留  | 全部人员           | SZA / SZB / SZC / JP1 / JP2 / LH / CX全部          |
| **人员来源** | ✅ **新增** |  | 自有 / 商用车 / 乘用车 / 柳汽 / 中智 / 外协 |
| 团队效率对比 | 🧩 重构 | 全部               | 原"团队效率对比" → **来源效率对比**（按来源汇总效率评分/完成率/总工时） |

### 3.7.3 页面组成

```
┌─────────────────────────────────────────────────────────────┐
│  页面标题 + 搜索框 + 日期范围筛选 + 刷新/导出                │
├─────────────────────────────────────────────────────────────┤
│  KPI卡片：总工时 | 人均工时 | 任务完成率 | 平均效率评分      │
│            | 返工率  | 人均产出                               │
├─────────────────────────────────────────────────────────────┤
│ 左侧侧栏筛选：                                              │
│ · 日期范围（自定义 + 快捷：近7/30/90天）                    │
│ · 区域筛选                                                  │
│ · 人员来源（装配区）：商用车 / 乘用车 / 柳汽 / 中智 / 外协 / │
│               内调 / 非装配区                                │
├─────────────────────────────────────────────────────────────┤
│  图表区域：                                                │
│  · 工时分布柱状图 → 按"人员来源"分组，叠加来源内各区域分布   │
│  · 任务完成率对比条形图 → 按"人员来源"排序比较               │
│  · 来源效率对比图 → 三维并行条：效率评分 / 完成率 / 总工时   │
│  · 效率排行榜（Top 20 人员，支持搜索）                      │
├─────────────────────────────────────────────────────────────┤
│  明细数据表：                                              │
│   姓名 | 工号 | 来源 | 区域 | 今日工时 | 完成任务 |         │
│   进行中 | 完成率 | 效率评分 | 排名 | 详情                   │
│  （装配区显示来源；非装配区"来源"列为"—"，但仍参与排名）     │
└─────────────────────────────────────────────────────────────┘
```

### 3.7.4 详情弹窗调整

点击人员进入详情面板：
1. **效率指标**：综合效率指数 / 今日工时 / 完成任务 / 进行中 / 返工率 / 人均产出
2. **任务明细**：最多展示 5 条任务（进度状态着色）
3. **基本信息**：工号 / 所在区域 / 人员来源 
4. 移除原"所属团队"字段。

---

## 3.8 模块八：异常预警中心

### 3.8.1 功能描述

实时监控并展示试制车间各类异常情况，支持分级处理。

### 3.8.2 页面组成

```
┌─────────────────────────────────────────────────────────────┐
│  页面标题 + 级别筛选 + 类型筛选 + 状态筛选 + 统计卡片       │
├─────────────────────────────────────────────────────────────┤
│  异常列表：                                               │
│  序号 | 类型 | 级别 | 标题 | 关联对象 | 触发时间 |         │
│  状态 | 处理人 | 操作（详情/处理/关闭）                     │
├─────────────────────────────────────────────────────────────┤
│  统计图表：按类型/级别/状态分布图                         │
└─────────────────────────────────────────────────────────────┘
```

### 3.8.3 预警类型

| 类型 | 说明 | 示例 |
|------|------|------|
| 设备占用超时 | 设备被占用超过预定时间 | SZB-03 已占用8h，预估4h |
| 无人作业异常 | 设备占用但长时间无人员扫码 | SZC-02 超过30分钟无人员扫码 |
| 人员离岗异常 | 人员状态异常离岗超过阈值 | 某人员离岗超过2小时 |
| 任务延迟预警 | 任务有延期风险 | 任务进度低于预期 |

---

## 3.9 模块九：任务管理

### 3.9.1 功能描述

统一管理三类任务（零星/ABC类/试制岛），支持多数据源同步和手工维护。

### 3.9.2 页面组成

```
┌─────────────────────────────────────────────────────────────┐
│  页面标题 + 新建任务/批量导入/同步360/同步运营表按钮         │
├─────────────────────────────────────────────────────────────┤
│  数据来源状态行：                                          │
│  · 零星任务汇总表(360系统) - 同步时间 + 立即同步按钮       │
│  · 试制运营表(ABC类) - 同步时间 + 立即同步按钮             │
│  · MES系统(试制岛) - 同步时间 + 立即同步按钮               │
├─────────────────────────────────────────────────────────────┤
│  统计卡片：待开始 | 进行中 | 今日完成 | 逾期                │
├─────────────────────────────────────────────────────────────┤
│  Tab切换：零星任务 | ABC类任务 | 试制岛任务                 │
│  筛选：状态筛选 + 优先级筛选 + 搜索框                      │
├─────────────────────────────────────────────────────────────┤
│  任务表格：                                               │
│  勾选 | 序号 | 任务名称 | 项目编号 | 车号 | 车型 |         │
│  优先级 | 状态 | 占用资源 | 负责人 | 计划工时 | 已用工时 |   │
│  进度 | 来源 | 操作                                      │
├─────────────────────────────────────────────────────────────┤
│  分页 + 批量操作                                           │
└─────────────────────────────────────────────────────────────┘
```

### 3.9.3 任务来源

| 来源 | 说明 | 同步方式 |
|------|------|---------|
| 零星任务汇总表（360系统） | 零星小任务 | 定时同步 / 手动同步 |
| 试制运营表（ABC类） | ABC类试制任务 | 定时同步 / 手动同步 |
| MES系统（试制岛） | 试制岛专属任务 | 定时同步 / 手动同步 |
| 手工录入 | 手动创建的特殊任务 | 手工维护 |

### 3.9.4 数据需求

- 主表：`tasks`
- 关联表：`equipment`、`personnel`、`zones`

---

# 第四部分：API 接口设计

## 4.1 接口规范

| 项目 | 约定 |
|------|------|
| API前缀 | `/api/resource/*` |
| 数据格式 | JSON |
| 认证方式 | API Key（复用现有机制） |
| 响应格式 | `{ code, message, data }` |
| 时间格式 | ISO 8601（`YYYY-MM-DDTHH:mm:ss`） |

## 4.2 设备相关接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/resource/equipment` | 获取设备列表（支持筛选分页） |
| GET | `/api/resource/equipment/{code}` | 获取单个设备详情 |
| POST | `/api/resource/equipment` | 新增设备 |
| PUT | `/api/resource/equipment/{code}` | 更新设备信息 |
| DELETE | `/api/resource/equipment/{code}` | 删除设备 |
| PUT | `/api/resource/equipment/{code}/status` | 更新设备状态 |
| GET | `/api/resource/equipment/{code}/maintenance` | 获取设备维护记录 |
| POST | `/api/resource/equipment/{code}/maintenance` | 新增维护记录 |
| GET | `/api/resource/equipment/stats` | 获取设备统计数据（驾驶舱用） |

## 4.3 区域相关接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/resource/zones` | 获取所有区域列表 |
| GET | `/api/resource/zones/{code}` | 获取区域详情（含设备、人员汇总） |
| POST | `/api/resource/zones` | 新增区域 |
| PUT | `/api/resource/zones/{code}` | 更新区域信息 |
| DELETE | `/api/resource/zones/{code}` | 删除区域 |
| GET | `/api/resource/zones/map` | 获取地图数据（所有区域+设备位置） |

## 4.4 人员相关接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/resource/personnel` | 获取人员列表（支持筛选） |
| GET | `/api/resource/personnel/{code}` | 获取单个人员详情 |
| POST | `/api/resource/personnel` | 新增人员 |
| PUT | `/api/resource/personnel/{code}` | 更新人员信息 |
| DELETE | `/api/resource/personnel/{code}` | 删除人员 |
| GET | `/api/resource/personnel/status` | 获取人员状态统计（驾驶舱用） |
| GET | `/api/resource/personnel/map` | 获取人员位置分布数据（地图用） |
| GET | `/api/resource/personnel/efficiency` | 获取人效统计数据 |
| GET | `/api/resource/personnel/{code}/efficiency` | 获取单人效率详情 |

## 4.5 任务相关接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/resource/tasks` | 获取任务列表（支持筛选分页） |
| GET | `/api/resource/tasks/{code}` | 获取单个任务详情 |
| POST | `/api/resource/tasks` | 新增任务 |
| PUT | `/api/resource/tasks/{code}` | 更新任务信息 |
| DELETE | `/api/resource/tasks/{code}` | 删除任务 |
| PUT | `/api/resource/tasks/{code}/status` | 更新任务状态 |
| PUT | `/api/resource/tasks/{code}/progress` | 更新任务进度 |
| GET | `/api/resource/tasks/stats` | 获取任务统计数据 |
| GET | `/api/resource/tasks/gantt` | 获取甘特图数据 |
| POST | `/api/resource/tasks/sync/{source}` | 从指定来源同步任务 |
| GET | `/api/resource/tasks/conflicts` | 获取资源冲突列表 |

## 4.6 异常预警接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/resource/alerts` | 获取预警列表（支持筛选） |
| GET | `/api/resource/alerts/{id}` | 获取预警详情 |
| POST | `/api/resource/alerts` | 手动创建预警 |
| PUT | `/api/resource/alerts/{id}/handle` | 处理预警 |
| PUT | `/api/resource/alerts/{id}/resolve` | 解决预警 |
| GET | `/api/resource/alerts/stats` | 获取预警统计数据 |
| POST | `/api/resource/alerts/check` | 触发预警检测 |

## 4.7 驾驶舱综合接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/resource/dashboard/kpi` | 获取所有KPI数据（单次请求） |
| GET | `/api/resource/dashboard/equipment-status` | 设备状态分布数据 |
| GET | `/api/resource/dashboard/personnel-status` | 人员状态分布数据 |
| GET | `/api/resource/dashboard/resource-utilization` | 资源利用率数据 |
| GET | `/api/resource/dashboard/alerts-recent` | 最近预警列表 |

---

# 第五部分：外部系统集成

## 5.1 数据源集成

### 5.1.1 360系统（零星任务）

| 项目 | 说明 |
|------|------|
| 数据内容 | 零星任务汇总表 |
| 同步方式 | API爬虫（复用现有spider模式） |
| 同步频率 | 每30分钟 / 手动触发 |
| 映射到 | tasks 表（task_type=sporadic, source=operation） |

### 5.1.2 MES系统（试制岛任务）

| 项目 | 说明 |
|------|------|
| 数据内容 | 试制岛任务列表 |
| 同步方式 | API对接 |
| 同步频率 | 每15分钟 / 手动触发 |
| 映射到 | tasks 表（task_type=island, source=mes） |

### 5.1.3 试制运营表（ABC类任务）

| 项目 | 说明 |
|------|------|
| 数据内容 | ABC类试制任务 |
| 同步方式 | API对接 / Excel导入 |
| 同步频率 | 每60分钟 / 手动触发 |
| 映射到 | tasks 表（task_type=abc, source=operation） |

## 5.2 预警检测规则

| 规则 | 说明 | 阈值 |
|------|------|------|
| 设备占用超时 | 设备连续占用超过阈值 | 可配置，默认8小时 |
| 无人作业异常 | 设备占用但无人员扫码超过阈值 | 可配置，默认30分钟 |
| 人员离岗异常 | 人员状态为working但实际离岗 | 可配置，通过定位数据判断 |
| 任务延迟预警 | 任务进度低于预期进度超过阈值 | 可配置，默认20% |

---

# 第六部分：前端实现规范

## 6.1 技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 框架 | React 18 + TypeScript | 复用现有webui_ref技术栈 |
| 构建工具 | Vite | 前端工程化构建 |
| UI组件 | 自定义组件库（复用现有ui/组件） | |
| 图表库 | ECharts 5.5 | 所有图表统一使用ECharts |
| 图标库 | Lucide Icons | 统一图标库 |
| 样式 | Tailwind CSS v4 | 原子化CSS |

## 6.2 设计规范（深色科技风）

### 配色体系

| 角色 | 色值 | CSS变量 |
|------|------|---------|
| 主背景 | `#0A0E1A` | `--background` |
| 卡片背景 | `#111827` | `--card` |
| 品牌色 | `#1677FF` | `--brand` |
| 成功/正常 | `#10B981` | `--state-success` |
| 警告 | `#F59E0B` | `--state-warning` |
| 错误/危险 | `#EF4444` | `--state-error` |
| 信息 | `#3B82F6` | `--state-info` |

### 区域状态色

| 状态 | 边框色 | 背景色 |
|------|--------|--------|
| 空闲 | `--state-success` | `rgba(16,185,129,0.1)` |
| 占用 | `--brand` | `rgba(59,130,246,0.1)` |
| 故障 | `--state-error` | `rgba(239,68,68,0.1)` |
| 维护 | `--ink-3` | `rgba(100,116,139,0.1)` |

## 6.3 页面路由

在现有路由基础上新增以下路由：

| 路由路径 | 页面组件 | 说明 |
|---------|---------|------|
| `/resource/dashboard` | ResourceDashboardPage | 综合驾驶舱 |
| `/resource/equipment` | EquipmentLedgerPage | 设备台账 |
| `/resource/zones` | CampusMapPage | 园区地图 |
| `/resource/utilization` | ResourceBoardPage | 资源占用看板 |
| `/resource/personnel` | PersonnelBoardPage | 人员看板 |
| `/resource/gantt` | GanttSchedulePage | 甘特图排程 |
| `/resource/efficiency` | PersonnelEfficiencyPage | 人效分析 |
| `/resource/alerts` | AlertCenterPage | 异常预警中心 |
| `/resource/tasks` | TaskManagementPage | 任务管理 |

## 6.4 组件结构

```
webui_ref/client/src/pages/
├── ResourceDashboardPage/
│   ├── ResourceDashboardPage.tsx
│   ├── KpiCardsSection.tsx
│   ├── EquipmentStatusChart.tsx
│   ├── PersonnelStatusChart.tsx
│   ├── WorkshopLayoutSection.tsx
│   ├── AlertListSection.tsx
│   └── TaskProgressSection.tsx
├── EquipmentLedgerPage/
│   ├── EquipmentLedgerPage.tsx
│   ├── EquipmentTableSection.tsx
│   ├── EquipmentDetailDialog.tsx
│   └── MaintenanceRecordSection.tsx
├── CampusMapPage/
│   ├── CampusMapPage.tsx
│   ├── ZoneMapSection.tsx
│   ├── EquipmentOverlay.tsx
│   └── PersonnelOverlay.tsx
├── ResourceBoardPage/
│   ├── ResourceBoardPage.tsx
│   ├── UtilizationMatrix.tsx
│   └── UtilizationTrendChart.tsx
├── PersonnelBoardPage/
│   ├── PersonnelBoardPage.tsx
│   ├── PersonnelMapSection.tsx
│   ├── PersonnelListSection.tsx
│   └── PersonnelDetailDialog.tsx
├── GanttSchedulePage/
│   ├── GanttSchedulePage.tsx
│   ├── GanttChartSection.tsx
│   └── ConflictAlertSection.tsx
├── PersonnelEfficiencyPage/
│   ├── PersonnelEfficiencyPage.tsx
│   ├── EfficiencyChartSection.tsx
│   └── EfficiencyTableSection.tsx
├── AlertCenterPage/
│   ├── AlertCenterPage.tsx
│   ├── AlertListSection.tsx
│   ├── AlertStatsChart.tsx
│   └── AlertDetailDialog.tsx
└── TaskManagementPage/
    ├── TaskManagementPage.tsx
    ├── TaskTableSection.tsx
    ├── TaskSyncStatus.tsx
    └── TaskDetailDialog.tsx
```

---

# 第七部分：部署与配置

## 7.1 数据库配置

在现有 MariaDB 数据库中新增以下表：
- `equipment`
- `zones`
- `personnel`
- `tasks`
- `alerts`
- `personnel_efficiency`
- `equipment_maintenance`

## 7.2 初始化数据

需要准备以下初始化数据：
1. 区域基础数据（SZC、SZA、SZB、JP1、JP2、LH、WW）
2. 设备基础数据（所有举升机、试制岛、工位）
3. 人员基础数据（工号、姓名、技能等）
4. 预警检测规则配置

## 7.3 定时任务

| 任务 | 频率 | 说明 |
|------|------|------|
| 零星任务同步 | 每30分钟 | 从360系统拉取最新零星任务 |
| ABC类任务同步 | 每60分钟 | 从运营表同步ABC类任务 |
| 试制岛任务同步 | 每15分钟 | 从MES系统同步试制岛任务 |
| 预警检测 | 每5分钟 | 检查设备超时、人员异常等 |
| 人员效率统计 | 每日凌晨 | 聚合前一天的人效数据 |
| 数据清理 | 每日凌晨 | 清理已解决超过30天的预警记录 |

---

# 第八部分：风险与待确认问题

| 序号 | 问题 | 风险等级 | 待确认 |
|------|------|---------|--------|
| 1 | 深色主题与现有Mint Lime风格冲突，需确认设计风格统一方案 | 低 | 待用户确认 |
| 2 | 预警检测规则的具体阈值需要业务方确认 | 中 | 待业务方确认 |
| 3 | MES系统和360系统的接口文档尚未获取 | 高 | 需获取接口文档 |
| 4 | 人员定位数据来源（RFID/蓝牙/手动签到）待确认 | 中 | 待技术方案确认 |
| 5 | 是否需要数据权限控制（区域主管只能看本区域数据） | 中 | 待确认 |
| 6 | 甘特图拖拽调整是否需要与调度员确认交互方式 | 低 | 待UI评审 |

---

# 附录：字段映射说明

## A. 设备编号规则

| 前缀 | 区域 | 说明 |
|------|------|------|
| SZC | 仓库装配区 | 举升机，编号01-06 |
| SZA | 装配一期（试制岛） | 试制岛+举升机，编号01-05 |
| SZB | 装配二期 | 举升机，编号01-06 |
| JP1 | 竞品装配一区 | 举升机，编号01-06 |
| JP2 | 竞品装配二区 | 举升机，编号01-06 |
| LH | 联合装配区 | 工位，编号01-02 |
| WW | 外委装配区 | 工位，编号01-08 |

## B. 人员命名规则

人员使用百家姓姓氏作为头像文字（张三→张、李四→李），后序可扩展为真实头像。

## C. 任务类型编码

| 类型 | 编码 | 来源 |
|------|------|------|
| 零星任务 | sporadic | 360系统 |
| ABC类任务 | abc | 运营表 |
| 试制岛任务 | island | MES系统 |
