# **spiderV5 — PBOM 智能装配与智能排程系统 — 需求与前端设计综合文档 V4**

> **编写日期：2026-06-23**
> **版本：V4.0（综合版）
>
> **核心价值：基于 PBOM 智能解析、多源到货数据融合、关键件风险识别与 BFWS+CP-SAT 智能排程，实现整车试制全流程数字化管理。

---

# **第一部分：产品概述与导航结构**

## **1.1 产品定位

| 维度 | 说明 |
|------|------|
| **产品类型 | 工业制造 Web 管理平台 |
| **目标用户 | 整车试制技术室工艺主管、物料管理员、专业师 |
| **核心模块 | 项目管理 / PBOM 解析 / 关键件评分 / BFWS 排程 / 数据处理 / 系统设置 |
| **界面语言 | 中文 |
| **技术选型 | 前端：HTML5 + ECharts 5 · 后端：FastAPI + MySQL |

## **1.2 导航结构

### **一级导航（顶部 Tab）
| 一级导航 | 说明 | 二级 Tab 页面 |
|---------|------|---------------|
| **项目管理 | 项目全流程核心业务操作 | 项目列表 / PBOM 清单 / 关键件评分 / 装配顺序推荐 / 到货率看板 |
| **数据处理 | 多源数据去重合并与配置列检测 | 多源去重合并 / 三层配置列检测 |
| **系统设置 | 凭证管理与爬虫控制 | 爬虫凭证管理 |

### **面包屑层级：
```
项目管理 › 项目列表
项目管理 › E70换代车型试制 › PBOM清单
项目管理 › E70换代车型试制 › 关键件评分
项目管理 › E70换代车型试制 › 装配顺序推荐 (BFWS CP-SAT)
项目管理 › E70换代车型试制 › 到货率看板
数据处理 › 多源去重合并
数据处理 › 三层配置列检测
系统设置 › 爬虫凭证管理
```

---

# **第二部分：设计规范（Mint Lime 薄荷荧光）

## **2.1 配色体系**

| 角色 | 色值 | CSS 变量 | 用途 |
|------|------|---------|------|
| 页面底色 bg | `#f0f2f1 | `var(--bg) | 全局页面背景 |
| 卡片表面 surface | `#ffffff | `var(--surface) | 白色内容卡片 |
| 主卡片 header 薄荷青 | `#d6e9e9 | `var(--mint) | 重点区块背景 |
| 主文字 text | `#1a1a1a | `var(--text) | 标题与关键数值 |
| 辅助文字 muted | `#94a3b8 | `var(--muted) | 描述文案、次要标签 |
| 强调色 accent | `#e2f163 | `var(--accent) | 荧光黄绿徽章（每屏 ≤3 处） |
| 边框 border | `#f1f5f9 | `var(--border) | 卡片描边 |

### **语义状态色（状态色体系）：
| 状态 | 色值 | 变量 | 用途 |
|------|------|--------|
| 安全/正常 | `#10b981 | `var(--emerald) | 到货率 ≥95%、入库完成、可装 |
| 预警 | `#f59e0b | `var(--amber) | 到货率 80-95%、待检、部分齐套 |
| 危险/阻塞 | `#fb7185 | `var(--rose) | 到货率 <80%、不合格、阻塞 |
| 信息 | `#6366f1 | `var(--indigo) | 已检待入库、CP-SAT 模式 |

### **图表配色序列：
`#6366f1 → #10b981 → #f59e0b → #3b82f6 → #ec4899 → #8b5cf6

## **2.2 字体排版**

| 用途 | 字体栈 | 字号 |
|------|--------|------|
| 全局 | `Inter, SF Pro Display, PingFang SC, Microsoft YaHei, sans-serif` | 13-14px |
| 数据/零件号 | `SF Mono, JetBrains Mono, Consolas, monospace` | 12.5px |
| 页面主标题 | — | text-2xl (26px, font-bold 800) |
| 区块标题 | — | text-lg (15px, font-semibold 600) |
| 卡片标题 | — | 13px (font-bold 700) |
| 正文 | — | 13px (normal 400) |
| 辅助说明 | — | text-xs (11px) |
| KPI 大数字 | — | 28px (font-bold 800) |

## **2.3 圆角系统**

| 类型 | 值 | CSS | 用途 |
|------|-----|-----|------|
| 页面主卡片 | `2rem` (32px) | `border-radius: 2rem` | 重点展示区域 |
| 标准卡片 | `1rem` (16px) | `border-radius: 1rem` | 普通内容卡片 |
| 按钮/交互 | `1rem` (16px) | `border-radius: 1rem` | 按钮、输入框 |
| 徽章/标签 | `9999px` (full) | `border-radius: 9999px` | 状态标签、胶囊 |

## **2.4 间距系统**

| 层级 | 值 | 用途 |
|------|-----|------|
| 内容区最大宽度 | `1400px` | 主容器 max-width |
| 页面内边距 | `2rem` | 主内容区域 padding |
| 区块间距 | `1.5rem` | Section 之间 margin |
| 卡片间距 | `1.25rem` | 网格间距 gap |
| 卡片内边距 | `1.5rem` | 标准卡片 padding |

## **2.5 组件规范**

### **按钮 Button
| 类型 | 样式 |
|------|------|
| 主按钮 Primary | `background:#1a1a1a` | color:white` | `border-radius:1rem` |
| 次按钮 Outline | `background:#ffffff` | `border:1px solid #f1f5f9 | color:#1a1a1a` |
| 幽灵按钮 Ghost | 透明背景 hover 时 `background:#f8fafc` |

### **卡片 Card
| 类型 | 样式 |
|------|------|
| 标准卡片 | `background:#ffffff` | `border:1px solid #f1f5f9` | `border-radius:1rem` | `padding:1.5rem` |
| 薄荷青重点卡片 | `background:#d6e9e9` | `border:none` | `border-radius:1rem` | `padding:1.5rem` |

### **状态徽章 Badge
| 状态 | 样式 |
|------|------|
| 安全/正常 | `background:rgba(16,185,129,0.1) | `color:#10b981` | `border-radius:9999px` |
| 预警 | `background:rgba(245,158,11,0.1)` | `color:#f59e0b | ... |
| 危险/阻塞 | `background:rgba(251,113,133,0.1)` | `color:#fb7185` | ... |
| 荧光强调 | `background:#e2f163` | `color:#1a1a1a` |

### **数据表格 Table
| 部分 | 样式 |
|------|------|
| 容器 | `background:#ffffff` | `border:1px solid #f1f5f9 |`border-radius:1rem |
| 表头 | `background:#f0f2f1` | `color:#94a3b8` | `font-size:11px` | `font-weight:600` |
| 单元格 | `padding:0.85rem 1rem` | `border-bottom:1px solid #f1f5f9` |
| 行悬停 | `hover:background:#f0f2f1` |

### **AI 解释框 AI Box**

**样式：`background:linear-gradient(135deg, #d6e9e9 0%, #f0f9f4 100%) | `border-left:3px solid #e2f163 | `border-radius:12px` | `padding:1.25rem

---

# **第三部分：功能模块详细规格

## **模块 1：项目列表（项目管理 › 项目列表

### **1.1 页面结构
```
Section 1: 4 张 KPI 统计卡片（项目总数 · 平均到货率 · 关键件齐套率 · 高风险项目数

Section 2: 项目清单表格

- 字段：状态徽章 · 项目号（等宽字体）· 项目名称 · 车型 · 试制单号 · 零件数 · 到货率（带进度条）· 关键件齐套（带进度条）· 创建时间
- 顶部操作栏：+ 新建项目按钮 · 导出 Excel · 搜索框
```

### **1.2 KPI 卡片规格
| 卡片 | 数值类型 | 示例 |
|------|---------|------|
| 项目总数 | 整数 | 6 |
| 平均到货率 | 百分比 | 85.6% |
| 关键件齐套率 | 百分比 | 79.7% |
| 高风险项目 | 整数 | 2 |

每张 KPI 卡片左侧带色条（indigo / emerald / amber / rose）

---

## **模块 2：PBOM 清单（项目管理 › PBOM 清单

### **2.1 页面结构
```
Section 1：项目级 KPI（零件总数 · 已入库 · 关键件 · 到货率）

Section 2：PBOM 文件上传区
- 拖拽上传区（支持 .xlsx/.xls
- 解析进度显示
- 数据源状态：飞书共享表 / 仓库 WMS / 采购系统（3 张卡片显示匹配状态）

Section 3：三层递进配置列检测（AI 辅助）
- 规则引擎排除法（Step 1）命中率显示
- LLM 补刀（Step 2）识别数量
- 用户确认兜底（Step 3）待确认列数
- 识别配置列总数（M101-M124 等）

Section 4：零件清单表格（Top N）
- 字段：零件号 · 零件名 · 需求量 · 已入库数量 · 到货率（带进度条）· 数据源 · 匹配级别 · 状态徽章
- 支持按状态筛选、搜索零件号/名称
```

### **2.2 三层配置列检测逻辑
| 步骤 | 方法 | 说明 | 视觉标识 |
|------|------|------|-----------|
| Step 1 规则引擎排除法 | 正则匹配 + 已知列模式排除 | 自动识别非配置列 | 🟢 高命中 |
| Step 2 LLM 补刀 | GPT-4o-mini 语义识别 | 对歧义列进行语义判定 | 🔵 AI辅助 |
| Step 3 用户确认兜底 | 人工确认对话框 | 置信度 <0.6 列弹出人工确认 | 🟡 待确认 |

---

## **模块 3：关键件识别与智能评分（项目管理 › 关键件评分

### **3.1 页面结构
```
Section 1：关键件到货率仪表盘（ECharts 仪表盘）
- 42 件关键件实时齐套监控
- 实时到货率环形显示

Section 2：4 维度评分权重配置（可拖拽滑块调节）
- 🚗 安全件权重（默认 30%
- 📦 大件重件权重（默认 20%
- ⏳ 紧缺件权重（默认 30%
- 🔧 工艺件权重（默认 20%
- 重置默认按钮

Section 3：关键件清单表格（加权总分排序
- 字段：序号 · 零件号 · 零件名 · 安全件评分(1-5) · 大件评分 · 紧缺评分 · 工艺评分 · 加权总分 · 分类徽章 · 状态徽章 · 预计到港日期
- 分类徽章：🔴高风险(4.0+) / 🟡中风险(3.0-3.9) / 🟢低风险(<3.0)
- 支持导出 CP-SAT 输入数据
```

### **3.2 评分模型
```
最终加权总分 = 安全件×0.3 + 大件×0.2 + 紧缺件×0.3 + 工艺×0.2（满分 5 分）
```

---

## **模块 4：装配顺序推荐 · BFWS 阻塞流水车间调度（项目管理 › 装配顺序推荐

### **4.1 页面结构
```
Section 1：4 张 KPI 统计卡片
- 配置总数 · 可装配数 · 部分齐套数 · 阻塞/高风险数

Section 2：BFWS 调度甘特图（ECharts 甘特图
- 工位 1 → 工位 2 → 工位 3
- 显示各配置的装配时间线
- 阻塞状态高亮标识

Section 3：多目标权重调节滑块
- α 齐套优先权重（默认 40%
- β 工时均衡权重（默认 30%
- γ 风险分散权重（默认 30%

Section 4：CP-SAT 求解器状态卡片
- 最优 makespan: XX.X 小时
- 求解时间: X.X 秒
- 关键件约束数
- 全局下界 LB (gap = X.X%

Section 5：推荐装配顺序列表
- 顺序编号 · 配置代码/名称 · 齐套率 · 综合得分 · 缺件说明 · 状态徽章

Section 6：AI 推理白盒解释（线性渐变 AI 框
- 排序逻辑说明：齐套优先 → 瓶颈对齐 → 工位平衡 → 风险车型滞后
```

### **4.2 BFWS 阻塞流水车间调度模型说明
| 概念 | 说明 |
|------|------|
| 模型目标 | 最小化 makespan（总完工时间），考虑关键件到货约束 |
| 求解器 | Google OR-Tools CP-SAT |
| 工位间无缓冲 | 车型 A 在工位 2 未完成，车型 B 无法进入工位 2（物理空间限制 |
| 阻塞传递 | 工位 2 因车型 A 缺料暂停 → 工位 1 已完成的车型 C 阻塞在工位 1 |
| 关键件到货时间 | 动力电池模组预计 06-25 到货 → 所有使用该零件的车型最早开始时间 = 06-25 |
| 装配工时差异 | E70 标准续航版需 8 工时，G59 海外版需 12 工时 |

### **4.3 多目标优化函数
```
目标函数 = α × 齐套率 + β × 工时均衡 + γ × 风险分散
α=0.4, β=0.3, γ=0.3（默认值）
```

### **4.4 求解状态徽章
| 状态 | 说明 | 颜色 |
|------|------|------|
| 可装配 | 关键件齐全，可直接装配 | 🟢 emerald |
| 部分齐套 | 部分关键件在途，预计近期可装 | 🟡 amber |
| 阻塞 | 关键件延迟，需等待到货 | 🔴 rose |

---

## **模块 5：到货率看板（项目管理 › 到货率看板

### **5.1 页面结构
```
Section 1：各项目到货率对比柱状图（ECharts Bar Chart

Section 2：近 14 天到货趋势折线图（ECharts Line Chart

Section 3：关键件到货状态卡片矩阵
- 按车型/配置分组显示
- 关键件总数
- 已到件数
- 在途件数
- 到货率（带进度条
- 缺件清单明细
- 预计到货日期
```

---

## **模块 6：多源到货数据去重与合并（数据处理 › 多源去重合并

### **6.1 页面结构
```
Section 1：4 张 KPI 统计卡片
- 原始记录数
- 强匹配合并数
- 弱匹配核验数
- 唯一源记录数

Section 2：去重匹配明细表格
- 字段：合并 ID（等宽字体）· 零件号 · 数量 · 批次号 · 供应商 · 到货日期 · 匹配源 · 匹配级别徽章 · 最终入库量
- 匹配级别：🟢强匹配（零件号+数量+批次+供应商+到货日期完全一致）
- 🟡弱匹配（零件号+数量+到货日期±3天）
- 🔴模糊匹配（零件号+数量一致但日期差异大）
- ⚪唯一源（仅一源登记）
```

### **6.2 5 维匹配键 × 4 级分级
| 匹配级别 | 判定条件 | 处理方式 | UI 标识 |
|---------|---------|---------|---------|
| 🟢 强匹配 | 零件号 + 数量 + 批次 + 供应商 + 到货日期 5 维完全一致 | 自动合并 | 双重登记 |
| 🟡 弱匹配 | 零件号 + 数量 + 到货日期 ±3 天 | 标黄 + 人工二次确认 | 疑似重复 |
| 🔴 模糊匹配 | 零件号 + 数量一致但日期差异大 | 标红 + 提示差异 | 待核实 |
| ⚪ 唯一源 | 仅一源登记 | 直接入库 | 仅一源 |

---

## **模块 7：三层递进配置列检测（数据处理 › 三层配置列检测

### **7.1 页面结构
```
Section 1：三步检测流程卡片矩阵（3 张薄荷青卡片
- Step 1：规则引擎排除法（命中 86%）
- Step 2：LLM 语义补刀（识别 12 列）
- Step 3：用户确认兜底（待确认 3 列）

Section 2：PBOM 列头识别结果表格
- 字段：序号 · 原始列名 · 识别类型 · 识别方式 · 置信度百分比 · 处理操作按钮
- 置信度色条：高(emerald / 中amber / 低rose
- 支持导出 JSON 结果
```

---

## **模块 8：爬虫凭证管理（系统设置 › 爬虫凭证管理

### **8.1 页面结构
```
Section 1：数据源状态卡片矩阵
- 🟢 仓库 WMS 状态卡片
- 🟢 飞书共享表状态卡片
- 🟡 采购系统状态卡片

Section 2：运行日志面板（深色背景终端风格
- 时间戳 · 级别标签（INFO/WARN/ERROR/OK)
- 日志行彩色标识
- 复制全部按钮

Section 3：系统参数配置区域（可选
- 关键件评分阈值设置
- 到货率阈值设置
```

---

# **第四部分：核心业务逻辑

## **4.1 零件入库判断逻辑
```
入库完成判定：IN_NUM >= ORDER_NUM（入库数量 >= 订单数量）
```

## **4.2 关键件到货率公式
```
关键件到货率 = 已到关键件数 / 应到关键件数 × 100%
```

## **4.3 MRP 净需求公式
```
净需求 = 总需求 - 现有库存 - 在途到货
```

## **4.4 装配顺序推荐逻辑
```
Step 1：齐套优先 → Step 2：瓶颈对齐 → Step 3：工位平衡 → Step 4：风险车型滞后
```

## **4.5 BFWS + CP-SAT 建模
```python
# 决策变量：每个工件在每个工位的开始时间和结束时间
# 约束：
# 1. 工序顺序约束（同一工件的工序间顺序
# 2. 工位能力约束（同一工位同一时间只能加工一个工件）
# 3. 关键件到货时间约束（工件最早开始时间 >= 关键件到货日期
# 目标函数：最小化 makespan + 加权等待时间
```

---

# **第五部分：数据模型

## **5.1 项目（Project）
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | INT | 是 | 主键 |
| project_code | VARCHAR(100) | 是 | 项目号 |
| project_name | VARCHAR(200) | 否 | 项目名称 |
| apply_code | VARCHAR(100) | 是 | 试制申请单号 |
| model_type | VARCHAR(100) | 否 | 车型（如 E70, G59, S597) |
| parts_count | INT | 是 | PBOM 零件总数 |
| delivery_rate | DECIMAL(5,2) | 是 | 到货率 (0-100) |
| critical_ready_rate | DECIMAL(5,2) | 是 | 关键件齐套率 (0-100) |
| status | VARCHAR(20) | 是 | 项目状态（正常/预警/阻塞） |
| created_at | DATETIME | 是 | 创建时间 |
| updated_at | DATETIME | 是 | 更新时间 |

## **5.2 零件（Part）
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | INT | 是 | 主键 |
| project_id | INT | 是 | 关联项目 ID |
| part_code | VARCHAR(200) | 是 | 零件号 |
| part_name | VARCHAR(200) | 是 | 零件名 |
| demand_quantity | INT | 是 | 需求量 |
| received_quantity | INT | 是 | 已入库数量 |
| safety_score | TINYINT | 否 | 安全件评分 1-5 |
| size_score | TINYINT | 否 | 大件评分 1-5 |
| scarcity_score | TINYINT | 否 | 紧缺件评分 1-5 |
| process_score | TINYINT | 否 | 工艺件评分 1-5 |
| critical_level | DECIMAL(3,2) | 否 | 加权总分 0.00-5.00 |
| is_critical | BOOLEAN | 是 | 是否关键件（总分 >=3.0 |
| eta_date | DATE | 否 | 预计到货日期 |
| created_at | DATETIME | 是 | 创建时间 |

## **5.3 到货记录（Delivery）
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | INT | 是 | 主键 |
| project_id | INT | 是 | 关联项目 ID |
| part_code | VARCHAR(200) | 是 | 零件号 |
| part_name | VARCHAR(200) | 是 | 零件名 |
| order_qty | INT | 是 | 订单数量 |
| received_qty | INT | 是 | 收货数量 |
| in_qty | INT | 是 | 入库数量 |
| warehouse | VARCHAR(100) | 否 | 到货仓库 |
| supplier | VARCHAR(200) | 否 | 供应商 |
| batch_no | VARCHAR(100) | 否 | 批次号 |
| arrival_date | DATE | 否 | 到货日期 |
| source | VARCHAR(50) | 是 | 数据源（warehouse/feishu/purchase) |
| match_level | VARCHAR(20) | 否 | 匹配级别（strong/weak/fuzzy/unique) |
| state | VARCHAR(50) | 是 | 状态（入库完成/已检待入库/待检/不合格/其他) |
| professional | VARCHAR(100) | 否 | 专业师 |
| created_at | DATETIME | 是 | 创建时间 |

## **5.4 配置（Config）
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | INT | 是 | 主键 |
| project_id | INT | 是 | 关联项目 ID |
| config_name | VARCHAR(50) | 是 | 配置名称（如 M101, M102 等 |
| display_name | VARCHAR(100) | 否 | 配置显示名（如标准续航版） |
| key_parts_total | INT | 是 | 关键件总数 |
| key_parts_ready | INT | 是 | 已就绪关键件数 |
| ready_rate | DECIMAL(5,2) | 是 | 齐套率 (0-100) |
| status | VARCHAR(20) | 是 | 状态（ready/warning/blocked) |
| created_at | DATETIME | 是 | 创建时间 |

## **5.5 装配计划（AssemblyPlan）
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | INT | 是 | 主键 |
| project_id | INT | 是 | 关联项目 ID |
| plan_name | VARCHAR(200) | 否 | 计划名称 |
| sequence_order | INT | 是 | 装配顺序号 |
| config_id | INT | 是 | 配置 ID |
| ready_rate | DECIMAL(5,2) | 是 | 齐套率 |
| composite_score | DECIMAL(5,2) | 是 | 综合得分 |
| missing_parts | TEXT | 否 | 缺件说明 |
| status | VARCHAR(20) | 是 | 状态（ready/warning/blocked) |
| processing_time_hours | DECIMAL(5,2) | 是 | 预计工时 |
| start_time | DATETIME | 否 | 计划开始时间 |
| end_time | DATETIME | 否 | 计划结束时间 |
| station | INT | 是 | 工位号 |
| created_at | DATETIME | 是 | 创建时间 |

---

# **第六部分：动效与交互规范

## **6.1 动画规范
| 场景 | 方案 | 参数 |
|------|------|------|
| 页面入场 | fadeIn | opacity 0→1, translateY 8px→0, 300ms ease |
| Tab 切换 | fadeIn + translateY | 同页面入场动画 |
| 进度条填充 | width transition | width 0→N%, 500ms easeOut, delay 100ms |
| 卡片悬停 | box-shadow + border | transition-shadow / transition-colors 200ms |
| 徽章状态变化 | background color transition | 200ms ease |

## **6.2 关键交互逻辑

### **导航切换逻辑
- 点击顶部一级导航 → 切换大类（项目管理/数据处理/系统设置）
- 切换大类后，自动显示该类的第一个二级 Tab 页面
- 面包屑动态更新当前页面路径

### **Tab 切换逻辑
- 二级 Tab 点击切换页面
- 当前激活 Tab 底部荧光色下划线标记（accent color #e2f163)

### **图表交互
- 饼状图扇区点击 → 可跳转到对应状态的详细列表
- 甘特图条形悬停 → 显示详细信息浮窗（车型、工位、开始/结束时间、工时、关键件状态、风险提示）
- 柱状图/折线图悬停 → 显示数据 tooltip

### **表格交互
- 行悬停高亮（背景色变化
- 进度条实时显示百分比

---

# **第七部分：技术栈与实现路径

## **7.1 前端技术栈
| 类别 | 技术 | 版本 |
|------|------|------|
| 核心框架 | HTML5 + CSS3 + Vanilla JavaScript | ES6+ |
| 图表库 | ECharts | 5.4.3 |
| 样式方案 | 原生 CSS 变量系统（CSS Variables) | — |
| 响应式 | 原生 CSS Grid / Flexbox | — |
| 字体栈 | Inter / JetBrains Mono | — |

## **7.2 核心实现要点
1. **CSS 变量系统：统一管理配色、圆角、间距等设计 Token
2. **单页面应用（SPA）风格：通过 JavaScript 控制页面切换与状态管理
3. **ECharts 图表集成：饼图、仪表盘、甘特图、柱状图、折线图
4. **响应式布局：支持桌面端（>1100px) 与移动端自适应
5. **组件化思维：KPI 卡片、表格、徽章、AI 解释框等可复用组件