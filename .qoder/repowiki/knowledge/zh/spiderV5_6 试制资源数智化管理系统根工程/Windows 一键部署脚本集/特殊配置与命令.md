运行前需确保 Python 3.8+ 与 MySQL 5.7/8.0 已安装且 `mysql` 在 PATH 中；推荐直接双击 `windows_setup\0_一键部署.bat` 或 `run_all.bat` 完成全部五步；也可按顺序单独执行 `step1_create_mysql_db.bat` ~ `step5_start_server.bat`。首次部署后需编辑 `backend\.env` 中的 DB_* 与 SERVER_PORT。