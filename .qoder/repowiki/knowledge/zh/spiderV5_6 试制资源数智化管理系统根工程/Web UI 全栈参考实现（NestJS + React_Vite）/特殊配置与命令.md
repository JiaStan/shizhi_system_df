Node ≥22、npm ≥10；安装依赖后执行 `npm run prepare` 自动配置 `.githooks/pre-commit`；开发启动 `npm run dev`（脚本调用 `scripts/dev.sh` 同时启动 NestJS 与 Vite），或分别运行 `dev:server`、`dev:client`；数据库 schema 通过 `npm run gen:db-schema` 从 Lark AaaS 同步至 `server/database/schema.ts`；构建命令 `npm run build:prod` 会先编译服务端再打包客户端。