运行前需将 `backend/.env.example` 复制为 `backend/.env` 并填写 DB_HOST/DB_PORT/DB_USER/DB_PASSWORD/DB_DATABASE 等数据库凭据，否则连接池初始化会抛出带排错提示的异常；服务通过 `python -m uvicorn backend.main:app --host <SERVER_HOST> --port <SERVER_PORT>` 或直接执行 `backend/main.py` 启动，调试模式由 `DEBUG=true` 开启热重载。