- 后端新模块遵循 `router.py` + `services/` + `schemas.py` 三段式结构，并在 `backend/main.py` 中挂载路由
- 数据库变更统一以 SQL 脚本形式放在 `backend/sql/` 下，保持建表与初始化数据分离
- 前端新增页面置于 `webui_ref/client/src/pages/` 对应目录，并通过代理访问后端 `/api` 接口
- 演示页新增 HTML 文件放入 `demo/`，复用 `demo/assets/css/theme.css` 主题样式