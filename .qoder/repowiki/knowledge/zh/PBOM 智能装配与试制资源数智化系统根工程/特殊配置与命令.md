根级启动流程：先执行 `python backend/scripts/init_db.py` 初始化基础库，再执行 `python backend/sql/init_resource_db.py` 初始化试制资源表，最后通过 `python start_server.py`（Windows）或 `python backend/main.py` 直接启动；凭证占位通过 `python insert_credentials.py` 写入 `spider_credentials` 表。