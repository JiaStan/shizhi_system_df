# spiderV5 — PBOM 智能装配与 BFWS 智能排程系统 — 需求与前端设计综合文档 V5.0

> **编写日期：2026-06-23**
> **版本：V5.0（综合版）**
> **核心价值：基于 PBOM 智能解析、多源到货数据融合、关键件风险识别与 BFWS+CP-SAT 智能排程，实现整车试制全流程数字化管理。**

---

# 第一部分：产品概述与导航结构

## 1.1 产品定位

| 维度 | 说明 |
|------|------|
| **产品类型** | 工业制造 Web 管理平台 |
| **目标用户** | 整车试制技术室工艺主管、物料管理员、专业师 |
| **核心模块** | 项目管理 / PBOM 解析 / 关键件评分 / BFWS 排程 / 数据处理 / 系统设置 |
| **界面语言** | 中文 |
| **技术选型** | 前端：HTML5 + CSS3 + Vanilla JavaScript + ECharts 5 · 后端：FastAPI + MySQL |
| **设计风格** | Mint Lime 薄荷荧光 — 杂志编辑式排版 + 大圆角卡片 + 荧光色点缀 |

## 1.2 导航结构

### 一级导航（顶部 Tab）

| 一级导航 | 说明 | 二级 Tab 页面 |
|---------|------|---------------|
| **项目管理** | 项目全流程核心业务操作 | 项目列表 / PBOM 清单 / 关键件评分 / 装配顺序推荐 / 到货率看板 |
| **数据处理** | 多源数据去重合并与配置列检测 | 多源去重合并 / 三层配置列检测 |
| **系统设置** | 凭证管理与爬虫控制 | 爬虫凭证管理 / 系统参数 |

### 面包屑层级

```
项目管理 › 项目列表
项目管理 › E70换代车型试制 › PBOM清单
项目管理 › 关键件识别与智能评分
项目管理 › 装配顺序推荐 · BFWS 阻塞流水车间调度
项目管理 › 到货率看板
数据处理 › 多源数据去重与合并
数据处理 › 三层递进配置列检测
系统设置 › 爬虫凭证管理
系统设置 › 系统参数配置
```

---

# 第二部分：设计规范（Mint Lime 薄荷荧光）

## 2.1 配色体系

| 角色 | 色值 | CSS 变量 | 用途 |
|------|------|---------|------|
| 页面底色 bg | `#f0f2f1` | `--bg` | 全局页面背景 |
| 卡片表面 surface | `#ffffff` | `--surface` | 白色内容卡片 |
| 主卡片 header 薄荷青 | `#d6e9e9` | `--mint` | 重点区块背景 |
| 主文字 text | `#1a1a1a` | `--text` | 标题与关键数值 |
| 辅助文字 muted | `#94a3b8` | `--muted` | 描述文案、次要标签 |
| 强调色 accent | `#e2f163` | `--accent` | 荧光黄绿徽章（每屏 ≤3 处） |
| 边框 border | `#e2e8f0` | `--border` | 卡片描边 |
| 边框次级 | `#f1f5f9` | `--border-soft` | 次要分隔线 |

### 语义状态色（状态色体系）

| 状态 | 色值 | 变量 | 用途 |
|------|------|--------|------|
| 安全/正常 | `#10b981` | `--emerald` | 到货率 ≥95%、入库完成、可装 |
| 预警 | `#f59e0b` | `--amber` | 到货率 80-95%、待检、部分齐套 |
| 危险/阻塞 | `#fb7185` | `--rose` | 到货率 <80%、不合格、阻塞 |
| 信息 | `#6366f1` | `--indigo` | 已检待入库、CP-SAT 模式 |

### 图表配色序列

```
#6366f1 → #10b981 → #f59e0b → #3b82f6 → #ec4899 → #8b5cf6
```

## 2.2 字体排版

| 用途 | 字体栈 | 字号/字重 |
|------|--------|-----------|
| 全局 | `Inter, SF Pro Display, PingFang SC, Microsoft YaHei, sans-serif` | 13-14px, 400-700 |
| 数据/零件号 | `JetBrains Mono, SF Mono, Consolas, monospace` | 12.5px |
| 页面主标题 | — | 22-26px, 800 |
| 区块标题 | — | 13-15px, 700 |
| 卡片标题 | — | 13px, 700 |
| 正文 | — | 13px, 400 |
| 辅助说明 | — | 11px, 400-600 |
| KPI 大数字 | — | 28px, 800 |
| 徽章标签 | — | 10.5-11px, 700 |

## 2.3 圆角系统

| 类型 | 值 | CSS | 用途 |
|------|-----|-----|------|
| 页面主卡片 | `2rem` (32px) | `border-radius: 2rem` | 重点展示区域 |
| 标准卡片 | `1rem` (16px) | `border-radius: 1rem` | 普通内容卡片 |
| 按钮/交互 | `1rem` (16px) | `border-radius: 1rem` | 按钮、输入框 |
| 徽章/标签 | `9999px` (full) | `border-radius: 9999px` | 状态标签、胶囊 |
| 进度条圆角 | `9999px` | 内部填充元素 |

## 2.4 间距系统

| 层级 | 值 | 用途 |
|------|-----|------|
| 内容区最大宽度 | `1400px` | 主容器 max-width |
| 页面内边距 | `2rem` | 主内容区域 padding |
| 区块间距 | `1.5rem` | Section 之间 margin |
| 卡片间距 | `1.25rem` | 网格间距 gap |
| 卡片内边距 | `1.5rem` | 标准卡片 padding |
| 小间距 | `0.75rem` | 元素间距 |

## 2.5 组件规范

### 按钮 Button

| 类型 | 样式 | 使用场景 |
|------|------|---------|
| 主按钮 Primary | `background:#1a1a1a; color:white; border-radius:1rem; padding:0.55rem 1.1rem; font-weight:700` | 主要操作、提交操作 |
| 次按钮 Outline | `background:#ffffff; border:1px solid var(--border); color:var(--text); border-radius:1rem; padding:0.55rem 1.1rem; font-weight:700` | 次要操作、取消操作 |
| 幽灵按钮 Ghost | `background:transparent; hover:background:var(--border-soft); color:var(--text); border-radius:1rem; padding:0.55rem 1.1rem` | 表格行内操作、链接按钮 |

### 卡片 Card

| 类型 | 样式 | 使用场景 |
|------|------|---------|
| 标准卡片 | `background:#ffffff; border:1px solid var(--border); border-radius:1rem; padding:1.5rem` | 普通内容容器 |
| 薄荷青重点卡片 | `background:#d6e9e9; border:none; border-radius:1rem; padding:1.5rem` | KPI 统计、重点信息展示 |
| 数据展示卡片 | `background:#ffffff; border:1px solid var(--border); border-radius:1rem; padding:1.5rem` | 表格、图表容器 |
| KPI 卡片 | `background:#ffffff; border:1px solid var(--border); border-radius:1rem; padding:1.5rem; position:relative; overflow:hidden` | 左侧 4px 彩色条 + 大数字 |

### 状态徽章 Badge

| 状态 | 样式 | 颜色变量 |
|------|------|---------|
| 安全/正常 | `background:rgba(16,185,129,0.1); color:var(--emerald); border-radius:9999px; padding:3px 10px; font-size:10.5px; font-weight:700` | emerald |
| 预警 | `background:rgba(245,158,11,0.1); color:var(--amber); ...` | amber |
| 危险/阻塞 | `background:rgba(251,113,133,0.1); color:var(--rose); ...` | rose |
| 信息 | `background:rgba(99,102,241,0.1); color:var(--indigo); ...` | indigo |
| 荧光强调 | `background:var(--accent); color:var(--text); border-radius:9999px; padding:3px 10px; font-size:10.5px; font-weight:700` | accent |
| 中性/唯一源 | `background:var(--bg); color:var(--muted); ...` | muted |

### 数据表格 Table

| 部分 | 样式 |
|------|------|
| 容器 | `background:#ffffff; border:1px solid var(--border); border-radius:1rem; overflow-x:auto` |
| 表头 | `background:var(--bg); color:var(--muted); font-size:11px; font-weight:700; letter-spacing:0.3px` |
| 单元格 | `padding:0.85rem 1rem; border-bottom:1px solid var(--border-soft); font-size:12.5px` |
| 行悬停 | `hover:background:var(--border-soft)` |
| 对齐规则 | 数值/零件号右对齐并使用等宽字体，文本左对齐 |

### AI 解释框 AI Box

**样式：**
```
background: linear-gradient(135deg, #d6e9e9 0%, #f0f9f4 100%);
border-left: 3px solid var(--accent);
border-radius: 12px;
padding: 1.25rem 1.5rem;
```

**结构：** AI标签（左上角荧光黄圆角矩形）+ AI内容（标题 + 要点列表/说明文本）

### 进度条 Progress Bar

**样式：**
- 外层容器：`width: 100px; background: var(--bg); border-radius: 9999px; height: 6px`
- 内部填充：`height: 100%; border-radius: 9999px; transition: width 0.4s ease`
- 颜色规则：`>= 90% emerald` / `80-90% amber` / `< 80% rose`
- 右侧百分比文本：`font-size: 11px; color: var(--muted); margin-left: 8px; font-weight: 600`

### 终端日志面板 Terminal Panel

**样式：**
```
background: var(--text);
border-radius: 1rem;
padding: 1.25rem;
font-family: JetBrains Mono, Consolas, monospace;
font-size: 11.5px;
line-height: 2;
color: #94a3b8;
max-height: 420px;
overflow-y: auto;
```

**日志级别高亮：**
- INFO: `color: var(--accent)`
- WARN: `color: var(--amber)`
- ERROR: `color: var(--rose)`
- OK: `color: var(--emerald)`

---

# 第三部分：功能模块详细规格

## 模块 1：项目列表（项目管理 › 项目列表）

### 1.1 页面结构

```
Section 1: 4 张 KPI 统计卡片（项目总数 · 平均到货率 · 关键件齐套率 · 高风险项目数）

Section 2: 项目清单表格

字段：状态徽章 · 项目号（等宽字体）· 项目名称 · 车型 · 试制单号 · 零件数（右对齐）· 到货率（带进度条）· 关键件齐套（带进度条）· 创建时间

顶部操作栏：+ 新建项目按钮 · 导出 Excel · 搜索框
```

### 1.2 KPI 卡片规格

| 卡片 | 数值类型 | 示例 | 左侧色条颜色 |
|------|---------|------|-------------|
| 项目总数 | 整数 | 6 | indigo |
| 平均到货率 | 百分比 | 85.6% | emerald |
| 关键件齐套率 | 百分比 | 79.7% | amber |
| 高风险项目 | 整数 | 2 | rose |

每张 KPI 卡片：
- 主标签（顶部小字，灰色，字重600）
- 大数字（28px，800）
- 底部说明（11px，灰色，描述趋势或补充说明）

---

## 模块 2：PBOM 清单（项目管理 › PBOM 清单）

### 2.1 页面结构

```
Section 1：项目级 KPI（零件总数 · 已入库 · 关键件数 · 待检/待入库）
→ 4 张 KPI 卡片，带彩色左边条

Section 2：PBOM 文件上传区
→ 拖拽上传区（支持 .xlsx/.xls）
→ 数据源状态矩阵（3 张薄荷青卡片显示匹配状态）：
  • 仓库 WMS（正常/延迟/异常）
  • 飞书共享表（正常/延迟/异常）
  • 采购系统（正常/延迟/异常）

Section 3：三层递进配置列检测（AI 辅助）
→ AI 解释框说明检测方法与结果
→ Step 1 规则引擎命中率（86%）
→ Step 2 LLM 补刀识别数量（12列）
→ Step 3 用户确认兜底列数（3列）
→ 总计识别配置列数（M101-M124等）

Section 4：零件清单表格（Top N）
→ 字段：零件号（等宽）· 零件名 · 需求量（右对齐）· 已入库（右对齐）· 到货率（进度条）· 数据源 · 匹配级别徽章 · 状态徽章
→ 支持按状态筛选、搜索零件号/名称
```

### 2.2 三层配置列检测逻辑

| 步骤 | 方法 | 说明 | 视觉标识 |
|------|------|------|-----------|
| Step 1 规则引擎排除法 | 正则匹配 + 已知列模式排除 | 自动识别非配置列 | 🟢 高命中 |
| Step 2 LLM 补刀 | GPT-4o-mini 语义识别 | 对歧义列进行语义判定 | 🔵 AI辅助 |
| Step 3 用户确认兜底 | 人工确认对话框 | 置信度 <0.6 列弹出人工确认 | 🟡 待确认 |

---

## 模块 3：关键件识别与智能评分（项目管理 › 关键件评分）

### 3.1 页面结构

```
Section 1：关键件到货率仪表盘（ECharts 仪表盘）
→ 42 件关键件实时齐套监控
→ 实时到货率环形显示，三色分段（rose/amber/emerald）
→ 中心显示当前百分比数值

Section 2：4 维度评分权重配置（可拖拽滑块调节）
→ 🚗 安全件权重（默认 30%）
→ 📦 大件重件权重（默认 20%）
→ ⏳ 紧缺件权重（默认 30%）
→ 🔧 工艺件权重（默认 20%）
→ 重置默认按钮
→ 实时显示总权重百分比

Section 3：关键件清单表格（按加权总分排序）
→ 字段：序号 · 零件号 · 零件名 · 安全件评分(1-5分) · 大件评分 · 紧缺评分 · 工艺评分 · 加权总分（indigo色加粗）· 分类徽章 · 状态徽章 · 预计到港日期（amber色）
→ 分类徽章颜色：🔴rose(高风险4.0+) / 🟡amber(中风险3.0-3.9) / 🟢emerald(低风险<3.0)
→ 支持导出 CP-SAT 输入数据
```

### 3.2 评分模型

```
最终加权总分 = 安全件×0.30 + 大件×0.20 + 紧缺件×0.30 + 工艺×0.20（满分 5.0 分）
```

---

## 模块 4：装配顺序推荐 · BFWS 阻塞流水车间调度（项目管理 › 装配顺序推荐）

### 4.1 页面结构

```
Section 1：4 张 KPI 统计卡片
→ 配置总数（indigo）· 可装配数（emerald）· 部分齐套数（amber）· 阻塞/高风险数（rose）

Section 2：BFWS 模型说明（AI 解释框样式）
→ 阻塞流水车间调度概念说明
→ 工位间无缓冲：车型 A 在工位 2 未完成 → 车型 B 无法进入工位 2
→ 阻塞传递：工位 2 因车型 A 缺料暂停 → 工位 1 已完成的车型 C 阻塞在工位 1
→ 关键件到货约束：动力电池模组预计 06-25 到货 → 所有使用该零件的车型最早开始时间 = 06-25
→ 装配工时差异：E70 标准续航版需 8 工时，G59 海外版需 12 工时

Section 3：BFWS 调度甘特图（ECharts 甘特图）
→ 工位 1 → 工位 2 → 工位 3
→ Y轴显示工位名称，X轴显示时间（小时）
→ 显示各配置的装配时间线，使用不同颜色标识状态
→ emerald: 正常 / amber: 部分齐套 / rose: 阻塞 / indigo: 在工位流转
→ 阻塞状态高亮标识，甘特条形图悬停显示详细信息

Section 4：多目标优化权重调节滑块
→ α 齐套优先权重（默认 40%）
→ β 工时均衡权重（默认 30%）
→ γ 风险分散权重（默认 30%）
→ 权重滑块实时响应，滑块 accent 色

Section 5：CP-SAT 求解器状态卡片
→ 3×1 小统计矩阵：最优 makespan（52.8h）· 求解时间（1.8s）· 约束数（186）
→ 右下角显示 Gap: 2.3%

Section 6：推荐装配顺序列表
→ 顺序编号（黑色方形徽章，42×42px）· 配置代码/名称（等宽字体）· 齐套率 · 综合得分 · 缺件说明 · 状态徽章
→ 每行独立排列，带下分隔线
→ 支持拖拽调整顺序（可选功能）

Section 7：AI 推理白盒解释（线性渐变 AI 框）
→ 排序逻辑说明：齐套优先 → 瓶颈对齐 → 工位平衡 → 风险车型滞后
→ 推理依据说明与参数配置提示
```

### 4.2 BFWS 阻塞流水车间调度模型说明

| 概念 | 说明 |
|------|------|
| 模型目标 | 最小化 makespan（总完工时间），考虑关键件到货约束 |
| 求解器 | Google OR-Tools CP-SAT（约束规划求解器） |
| 工位间无缓冲 | 车型 A 在工位 2 未完成，车型 B 无法进入工位 2（物理空间限制） |
| 阻塞传递 | 工位 2 因车型 A 缺料暂停 → 工位 1 已完成的车型 C 阻塞在工位 1 |
| 关键件到货时间 | 动力电池模组预计 06-25 到货 → 所有使用该零件的车型最早开始时间 = 06-25 |
| 装配工时差异 | E70 标准续航版需 8 工时，G59 海外版需 12 工时 |

### 4.3 多目标优化函数

```
目标函数 Score = α × 齐套率 + β × 工时均衡 + γ × 风险分散
α=0.40, β=0.30, γ=0.30（默认值，用户可通过滑块调节）
```

### 4.4 求解状态徽章

| 状态 | 说明 | 颜色变量 |
|------|------|---------|
| 可装配 | 关键件齐全，可直接装配 | emerald |
| 部分齐套 | 部分关键件在途，预计近期可装 | amber |
| 阻塞 | 关键件延迟，需等待到货 | rose |

### 4.5 甘特图颜色编码

| 颜色 | 含义 | 配置示例 |
|------|------|---------|
| `#10b981` | 正常装配，关键件齐全 | M101, M102, M108 |
| `#f59e0b` | 部分齐套，存在在途零件 | M103 |
| `#fb7185` | 阻塞，关键件延迟到货 | M106 |
| `#6366f1` | 在后续工位流转 | 工位2/工位3的M101-M108 |

---

## 模块 5：到货率看板（项目管理 › 到货率看板）

### 5.1 页面结构

```
Section 1：各项目到货率对比柱状图（ECharts Bar Chart）
→ X轴：项目编号（SP-2026-018 等）
→ Y轴：到货率百分比（0-100%）
→ 柱颜色根据到货率自动变化（emerald/amber/rose）
→ 柱顶显示数值标签

Section 2：近 14 天到货趋势折线图（ECharts Line Chart）
→ 多条数据线（E70系列/G59系列/S597系列）
→ 每条线有不同颜色和渐变色填充区域
→ X轴：日期（06-10 至 06-23）
→ Y轴：到货率百分比
→ 顶部图例说明

Section 3：关键件到货状态卡片矩阵
→ 按车型/配置分组显示（网格布局，2-3列）
→ 每张卡片包含：
  • 配置标题（M101 · E70 标准续航版）
  • 关键件总数
  • 已到件数（emerald色）
  • 在途件数（amber色）
  • 未到货件数（rose色）
  • 到货率进度条
  • 缺件清单明细（如果有）
  • 预计到货日期（如果有）
```

---

## 模块 6：多源到货数据去重与合并（数据处理 › 多源去重合并）

### 6.1 页面结构

```
Section 1：4 张 KPI 统计卡片
→ 原始记录数（indigo）· 强匹配合并数（emerald）· 弱匹配核验数（amber）· 唯一源记录数（muted）

Section 2：AI 匹配规则说明（AI 解释框样式）
→ 5 维匹配键 × 4 级分级说明
→ 各匹配级别的判定条件与处理方式

Section 3：去重匹配明细表格
→ 字段：合并ID（等宽）· 零件号 · 数量（右对齐）· 批次号 · 供应商 · 到货日期 · 匹配源 · 匹配级别徽章 · 最终入库量（indigo色加粗，右对齐）
→ 匹配级别：🟢强匹配（零件号+数量+批次+供应商+到货日期完全一致）
→ 🟡弱匹配（零件号+数量+到货日期±3天）
→ 🔴模糊匹配（零件号+数量一致但日期差异大）
→ ⚪唯一源（仅一源登记）

顶部操作栏：执行去重合并按钮 · 导出结果 · 搜索框
```

### 6.2 5 维匹配键 × 4 级分级

| 匹配级别 | 判定条件 | 处理方式 | UI 标识 |
|---------|---------|---------|---------|
| 🟢 强匹配 | 零件号 + 数量 + 批次 + 供应商 + 到货日期 5 维完全一致 | 自动合并 | emerald徽章 |
| 🟡 弱匹配 | 零件号 + 数量 + 到货日期 ±3 天 | 标黄 + 人工二次确认 | amber徽章 |
| 🔴 模糊匹配 | 零件号 + 数量一致但日期差异大 | 标红 + 提示差异 | rose徽章 |
| ⚪ 唯一源 | 仅一源登记 | 直接入库 | muted徽章 |

---

## 模块 7：三层递进配置列检测（数据处理 › 三层配置列检测）

### 7.1 页面结构

```
Section 1：三步检测流程卡片矩阵（3 张薄荷青卡片）
→ Step 1：规则引擎排除法（命中 86%，显示大数字百分比）
→ Step 2：LLM 语义补刀（识别 12 列，显示识别列数）
→ Step 3：用户确认兜底（待确认 3 列，显示待确认数量）
→ 每张卡片：步骤编号（灰色小字体）· 步骤标题（黑色，字重700）· 数值结果（22px，字重800）· 补充说明（11px灰色）

Section 2：PBOM 列头识别结果表格
→ 字段：序号 · 原始列名 · 识别类型 · 识别方式 · 置信度百分比（带进度条）
→ 置信度色条规则：高(emerald, >=0.9) / 中(amber, 0.7-0.9) / 低(rose, <0.7)
→ 支持导出 JSON 结果

顶部操作栏：重新检测按钮 · 导出 JSON
```

---

## 模块 8：爬虫凭证管理（系统设置 › 爬虫凭证管理）

### 8.1 页面结构

```
Section 1：数据源状态卡片矩阵
→ 3 张薄荷青卡片（仓库 WMS / 飞书共享表 / 采购系统）
→ 每张卡片：状态徽章（正常/延迟/异常，顶部）· 数据源名称（黑色标题）· 最后同步时间（灰色小字体）
→ 状态颜色：正常 emerald / 延迟 amber / 异常 rose

Section 2：运行日志面板（深色背景终端风格）
→ 时间戳 · 级别标签（INFO/WARN/ERROR/OK，彩色）· 日志内容
→ 日志行彩色标识：INFO(accent) / WARN(amber) / ERROR(rose) / OK(emerald)
→ 日志行间距 2 倍行高
→ 复制全部按钮（可选，顶部）

Section 3：爬虫控制卡片
→ 自动调度开关（on/off）· 执行频率选择（30min / 1h / 6h / 12h / 24h）· 手动执行按钮

顶部操作栏：刷新凭证 · 复制日志
```

---

## 模块 9：系统参数配置（系统设置 › 系统参数）

### 9.1 页面结构

```
Section 1：关键件评分阈值设置
→ 安全件权重滑块（默认 30）
→ 大件重件权重滑块（默认 20）
→ 紧缺件权重滑块（默认 30）
→ 工艺件权重滑块（默认 20）
→ 实时显示总权重百分比
→ 重置默认按钮

Section 2：到货率阈值设置
→ 安全阈值（emerald，默认 >=95%）滑块输入
→ 预警阈值（amber，默认 80-95%）滑块输入
→ 危险阈值（rose，默认 <80%）滑块输入
→ 实时预览阈值区间图表

Section 3：BFWS 排程参数设置
→ 工位数量设置（默认 3）
→ α 齐套优先权重（默认 40%）
→ β 工时均衡权重（默认 30%）
→ γ 风险分散权重（默认 30%）
→ CP-SAT 求解超时时间（默认 30 秒）

保存按钮（主按钮，黑色背景白色文字）
```

---

# 第四部分：核心业务逻辑与算法说明

## 4.1 零件入库判断逻辑

```
入库完成判定条件：IN_NUM >= ORDER_NUM（入库数量 >= 订单数量）
状态流转：待检 → 已检待入库 → 入库完成
              ↓
           不合格（需人工判定）
```

## 4.2 关键件到货率公式

```
关键件到货率 = 已到关键件数 / 应到关键件总数 × 100%
状态分级：>= 85% 正常（emerald）
          70-85% 预警（amber）
          < 70% 阻塞（rose）
```

## 4.3 MRP 净需求公式

```
净需求 = 总需求 - 现有库存 - 在途到货 + 安全库存
净需求 > 0 → 需采购/生产
净需求 <= 0 → 库存充足
```

## 4.4 装配顺序推荐逻辑

```
Step 1：齐套优先 → 齐套率高的配置优先安排
Step 2：瓶颈对齐 → 识别关键工位，对齐瓶颈资源
Step 3：工位平衡 → 均衡分配各工位负载，避免局部过载
Step 4：风险车型滞后 → 高风险/阻塞配置延后安排，等待关键件到货
```

## 4.5 BFWS + CP-SAT 建模

### 4.5.1 决策变量

```
x_ijk ∈ {0,1}: 工件 j 在工位 k 在时刻 i 是否加工
s_jk: 工件 j 在工位 k 的开始时间（整数变量）
e_jk: 工件 j 在工位 k 的结束时间（整数变量）
makespan: 总完工时间（目标函数）
```

### 4.5.2 约束条件

```
1. 工序顺序约束（同一工件的工序间顺序）：
   s_jk >= e_j(k-1) for all j, k>1

2. 工位能力约束（同一工位同一时间只能加工一个工件）：
   e_jk <= s_lk OR e_lk <= s_jk for all j!=l, all k（互不重叠约束）

3. 关键件到货时间约束：
   s_jk >= part_arrival_date for all 工件j使用的关键件, k=1（最早开工时间）

4. BFWS 阻塞约束（工位间无缓冲，工件完成后阻塞在当前工位）：
   e_jk <= s_j(k+1) for all j, k<m（需等下一工位空闲才能进入）

5. 时间窗口约束：
   s_jk >= earliest_time, e_jk <= latest_time for all j, k
```

### 4.5.3 目标函数

```
minimize: makespan = max(e_jk) for all j, k
subject to: 上述所有约束

多目标扩展版本：
minimize: α × makespan + β × 总等待时间 + γ × 阻塞工时
（α=0.4, β=0.3, γ=0.3，用户可调节）
```

### 4.5.4 CP-SAT 求解流程

```
输入：
→ 配置列表（M101-M124等，24个配置）
→ 每个配置的加工时间（各工位工时）
→ 关键件到货日期
→ 用户设定的权重 α, β, γ

建模与求解步骤：
1. 定义整数变量：每个配置在每个工位的开始/结束时间
2. 添加工序顺序约束（同一配置的工位顺序）
3. 添加工位互斥约束（NoOverlap，同一工位同一时间只加工一个配置）
4. 添加关键件到货约束（最早开工时间 >= 到货日期）
5. 添加 BFWS 阻塞约束（相邻工位无缓冲）
6. 定义目标函数：最小化 makespan + 加权等待时间
7. 调用 CP-SAT 求解器求解
8. 输出最优/可行解：甘特图数据、装配顺序列表、makespan、Gap、求解时间

后处理：
→ 根据用户调节的权重重新排序推荐列表
→ 生成 AI 白盒解释（推理依据说明）
→ 可视化甘特图
```

---

# 第五部分：数据模型

## 5.1 项目（Project）

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | INT | 是 | 主键 |
| project_code | VARCHAR(100) | 是 | 项目号 |
| project_name | VARCHAR(200) | 否 | 项目名称 |
| apply_code | VARCHAR(100) | 是 | 试制申请单号 |
| model_type | VARCHAR(100) | 否 | 车型（如 E70, G59, S597） |
| parts_count | INT | 是 | PBOM 零件总数 |
| delivery_rate | DECIMAL(5,2) | 是 | 到货率 (0-100) |
| critical_ready_rate | DECIMAL(5,2) | 是 | 关键件齐套率 (0-100) |
| status | VARCHAR(20) | 是 | 项目状态（正常/预警/阻塞） |
| created_at | DATETIME | 是 | 创建时间 |
| updated_at | DATETIME | 是 | 更新时间 |

## 5.2 零件（Part）

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | INT | 是 | 主键 |
| project_id | INT | 是 | 关联项目 ID |
| part_code | VARCHAR(200) | 是 | 零件号 |
| part_name | VARCHAR(200) | 是 | 零件名 |
| demand_quantity | INT | 是 | 需求量 |
| received_quantity | INT | 是 | 已入库数量 |
| safety_score | TINYINT | 否 | 安全件评分 1-5 |
| size_score | TINYINT | 否 | 大件评分 1-5 |
| scarcity_score | TINYINT | 否 | 紧缺件评分 1-5 |
| process_score | TINYINT | 否 | 工艺件评分 1-5 |
| critical_level | DECIMAL(3,2) | 否 | 加权总分 0.00-5.00 |
| is_critical | BOOLEAN | 是 | 是否关键件（总分 >=3.0） |
| eta_date | DATE | 否 | 预计到货日期 |
| created_at | DATETIME | 是 | 创建时间 |

## 5.3 到货记录（Delivery）

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | INT | 是 | 主键 |
| project_id | INT | 是 | 关联项目 ID |
| part_code | VARCHAR(200) | 是 | 零件号 |
| part_name | VARCHAR(200) | 是 | 零件名 |
| order_qty | INT | 是 | 订单数量 |
| received_qty | INT | 是 | 收货数量 |
| in_qty | INT | 是 | 入库数量 |
| warehouse | VARCHAR(100) | 否 | 到货仓库 |
| supplier | VARCHAR(200) | 否 | 供应商 |
| batch_no | VARCHAR(100) | 否 | 批次号 |
| arrival_date | DATE | 否 | 到货日期 |
| source | VARCHAR(50) | 是 | 数据源（warehouse/feishu/purchase） |
| match_level | VARCHAR(20) | 否 | 匹配级别（strong/weak/fuzzy/unique） |
| state | VARCHAR(50) | 是 | 状态（入库完成/已检待入库/待检/不合格/其他） |
| professional | VARCHAR(100) | 否 | 专业师 |
| created_at | DATETIME | 是 | 创建时间 |

## 5.4 配置（Config）

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | INT | 是 | 主键 |
| project_id | INT | 是 | 关联项目 ID |
| config_name | VARCHAR(50) | 是 | 配置名称（如 M101, M102 等） |
| display_name | VARCHAR(100) | 否 | 配置显示名（如标准续航版） |
| key_parts_total | INT | 是 | 关键件总数 |
| key_parts_ready | INT | 是 | 已就绪关键件数 |
| ready_rate | DECIMAL(5,2) | 是 | 齐套率 (0-100) |
| status | VARCHAR(20) | 是 | 状态（ready/warning/blocked） |
| processing_time_hours | DECIMAL(5,2) | 否 | 加工工时 |
| created_at | DATETIME | 是 | 创建时间 |

## 5.5 装配计划（AssemblyPlan）

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | INT | 是 | 主键 |
| project_id | INT | 是 | 关联项目 ID |
| plan_name | VARCHAR(200) | 否 | 计划名称 |
| sequence_order | INT | 是 | 装配顺序号 |
| config_id | INT | 是 | 配置 ID |
| ready_rate | DECIMAL(5,2) | 是 | 齐套率 |
| composite_score | DECIMAL(5,2) | 是 | 综合得分 |
| missing_parts | TEXT | 否 | 缺件说明 |
| status | VARCHAR(20) | 是 | 状态（ready/warning/blocked） |
| processing_time_hours | DECIMAL(5,2) | 是 | 预计工时 |
| start_time | DATETIME | 否 | 计划开始时间 |
| end_time | DATETIME | 否 | 计划结束时间 |
| station | INT | 是 | 工位号 |
| solver_gap | DECIMAL(5,2) | 否 | CP-SAT 求解 Gap |
| makespan_hours | DECIMAL(8,2) | 否 | 总完工时间（小时） |
| created_at | DATETIME | 是 | 创建时间 |

---

# 第六部分：动效与交互规范

## 6.1 动画规范

| 场景 | 方案 | 参数 |
|------|------|------|
| 页面入场 | fadeIn + translateY | opacity 0→1, translateY 8px→0, 300ms ease |
| Tab 切换 | fadeIn + translateY | 同页面入场动画 |
| 进度条填充 | width transition | width 0→N%, 500ms easeOut, delay 100ms |
| 卡片悬停 | box-shadow + border + translateY | transition-shadow / transition-colors 200ms, translateY -2px |
| 徽章状态变化 | background color transition | 200ms ease |
| 数值变化 | 数字递增动画 | 从 0 递增到目标值，500ms easeOut |
| 甘特图条形入场 | scaleX 动画 | 从左向右展开，300ms ease |

## 6.2 关键交互逻辑

### 导航切换逻辑
- 点击顶部一级导航 → 切换大类（项目管理/数据处理/系统设置）
- 切换大类后，自动显示该类的第一个二级 Tab 页面
- 面包屑动态更新当前页面路径
- Tab 激活状态：底部荧光色下划线标记（accent color）

### Tab 切换逻辑
- 二级 Tab 点击切换页面
- 当前激活 Tab 使用 accent 背景色（荧光黄）
- Tab 切换时页面内容有淡入动画

### 图表交互
- 饼状图扇区点击 → 可跳转到对应状态的详细列表
- 甘特图条形悬停 → 显示详细信息浮窗（车型、工位、开始/结束时间、工时、关键件状态、风险提示）
- 柱状图/折线图悬停 → 显示数据 tooltip
- 图表随窗口大小自动 resize

### 表格交互
- 行悬停高亮（背景色变化）
- 进度条实时显示百分比
- 数据单元格右对齐使用等宽字体
- 表格支持横向滚动

### 滑块交互
- 关键件权重滑块：实时更新显示值，拖动后总权重自动计算
- 排程权重滑块：α/β/γ 三个滑块联动显示
- 滑块填充颜色：拖动时 accent 色填充已选部分

---

# 第七部分：技术栈与实现路径

## 7.1 前端技术栈

| 类别 | 技术 | 版本 |
|------|------|------|
| 核心框架 | HTML5 + CSS3 + Vanilla JavaScript | ES6+ |
| 图表库 | Apache ECharts（原百度 ECharts） | 5.4.3 |
| 样式方案 | 原生 CSS 变量系统（CSS Variables） | — |
| 响应式布局 | 原生 CSS Grid / Flexbox | — |
| 字体栈 | Inter / JetBrains Mono / PingFang SC | — |

## 7.2 核心实现要点

1. **CSS 变量系统**：统一管理配色、圆角、间距等设计 Token，便于主题切换
2. **单页面应用（SPA）风格**：通过 JavaScript 控制页面切换与状态管理，避免页面刷新
3. **ECharts 图表集成**：仪表盘（gauge）、柱状图（bar）、折线图（line）、甘特图（custom）
4. **响应式布局**：
   - 桌面端（> 1100px）：4 列 KPI 卡片、3 列网格
   - 平板端（768-1100px）：2 列 KPI 卡片、2 列网格
   - 移动端（< 768px）：单列表格、堆叠卡片
5. **组件化思维**：KPI 卡片、表格、徽章、AI 解释框、进度条等可复用组件
6. **状态管理**：使用简单的 JavaScript 对象管理页面状态（subTabs、当前分类、图表实例等）
7. **数据模拟**：内置模拟数据用于前端展示，后端对接时替换为 API 调用

## 7.3 文件结构

```
spiderV5/
├── index.html              # 主页面（所有模块的单页应用）
├── 需求文档_V5_综合设计.md # 本设计文档
├── frontend-reproduction-spec.md # 参考前端设计规范
├── 需求文档_V2_综合版.md   # V2 需求文档（PBOM解析 + 数据融合）
├── 需求文档_V3_BFWS_CPSAT.md # V3 需求文档（BFWS排程 + 关键件评分）
└── build_html.py           # 辅助构建脚本（可选）
```

## 7.4 后端 API 建议（后续对接）

```
GET  /api/projects                    # 获取项目列表
GET  /api/projects/:id                # 获取项目详情
POST /api/projects                    # 创建项目
GET  /api/projects/:id/parts          # 获取项目零件清单
GET  /api/projects/:id/critical-parts # 获取关键件清单
GET  /api/projects/:id/configs        # 获取配置清单
POST /api/schedule/solve              # 提交 BFWS + CP-SAT 排程求解
GET  /api/data/delivery               # 获取多源到货数据
POST /api/data/dedup-merge            # 执行去重合并
POST /api/pbom/detect-columns         # 执行配置列检测
GET  /api/system/credentials          # 获取数据源凭证状态
POST /api/system/credentials/refresh  # 刷新凭证
GET  /api/system/logs                 # 获取运行日志
POST /api/system/params               # 更新系统参数
```

---

# 第八部分：实施与优化建议

## 8.1 优化方向

1. **性能优化**：图表懒加载、数据分页、虚拟滚动表格
2. **交互增强**：拖拽排序、多选操作、快捷键支持
3. **可视化增强**：热力图、桑基图、关系图、3D 甘特图
4. **AI 集成**：白盒解释、自然语言查询、智能推荐
5. **导出功能**：Excel 导出、PDF 报表、图表导出

## 8.2 后续迭代计划

### Phase 1：核心功能完善
- 完成所有 9 个模块的前端实现
- 完善模拟数据，确保每个模块有完整的数据展示
- 优化动画和交互体验

### Phase 2：数据与算法对接
- 接入真实数据源 API
- 实现 CP-SAT 求解器后端对接
- 完善去重合并算法逻辑
- 实现三层配置列检测的 AI 调用

### Phase 3：增强功能开发
- 导出功能（Excel/PDF/图表）
- 用户权限与角色管理
- 项目模板与批量操作
- 移动端适配优化

### Phase 4：高级可视化与 AI
- 高级图表（热力图、桑基图、3D 甘特图）
- AI 白盒解释增强（流式推理展示）
- 自然语言查询接口
- 异常检测与预警推送

---

> **文档结束**
>
> 本文档整合了 V2（PBOM 解析 + 多源数据融合）和 V3（BFWS 阻塞流水车间调度 + CP-SAT 智能排程 + 关键件四维评分）的核心需求，参考 Mint Lime 前端设计规范，定义了完整的页面结构、组件规格、数据模型和交互逻辑，可用于指导前端开发与后续功能迭代。