# 仓库到货数据管理系统 - 需求文档 V2.0（综合版）

> 基于 `spiderV4` 需求文档 + `PBOM智能装配路线图` 整合
> 编写日期：2026-06-23
> 版本：V2.0 综合版

---

## 一、项目概述

### 1.1 项目背景

现有 `warehouse_sync` 项目已实现：
- 从外部接口（`di360.dfmc.com.cn`）定时爬取到货明细数据，存入 MySQL 数据库
- 提供 FastAPI 接口供外部平台查询到货数据

现需在 `spiderV5` 目录下创建一套完整的 **本地Web管理系统**，在 spiderV4 的基础上实现：
- PBOM消耗清单上传与解析（含三层递进配置列识别）
- 与数据库到货数据自动匹配
- 关键件智能识别与评分
- 多源到货数据去重合并（仓库系统 + 飞书共享表）
- 装配顺序智能推荐与 What-if 模拟
- 可视化展示到货状态（饼状图）与关键件到货率看板
- 多项目管理（增/删/改）
- 凭证自动获取与爬虫控制

### 1.2 技术选型

| 层级 | 技术 | 说明 |
|------|------|------|
| 前端框架 | Vue 3 + Element Plus | 组件化UI框架 |
| 构建工具 | Vite | 前端工程化构建 |
| 图表库 | ECharts 5 | 饼状图、看板图表 |
| 后端框架 | FastAPI (Python 3.7+) | 承接 `warehouse_sync` 技术栈 |
| 数据库 | MySQL | 与现有 `warehouse_data` 库共用 |
| 前端部署 | FastAPI 托管静态文件 | 前后端同项目部署 |
| Excel解析 | openpyxl | Python端解析Excel文件 |
| LLM 调用 | GPT-4o-mini API | 配置列识别、关键件评分、装配解释 |
| 飞书集成 | 飞书 OpenAPI | 拉取共享表数据、多源去重 |

### 1.3 整体架构

```
spiderV5/
├── backend/                          # 后端代码
│   ├── main.py                       # FastAPI入口，包含静态文件托管
│   ├── api/                          # API路由
│   │   ├── __init__.py
│   │   ├── projects.py               # 项目管理接口
│   │   ├── delivery.py               # 到货数据接口（承接现有）
│   │   ├── pbom.py                   # PBOM上传与解析接口（含三层检测）
│   │   ├── statistics.py             # 统计数据接口（饼状图等）
│   │   ├── critical_parts.py         # 关键件识别与评分接口（新增）
│   │   ├── dedup.py                  # 多源去重合并接口（新增）
│   │   ├── assembly.py               # 装配顺序推荐接口（新增）
│   │   ├── spider_config.py          # 爬虫凭证管理接口
│   │   └── spider_control.py         # 爬虫手动控制接口
│   ├── models/                       # 数据模型
│   │   ├── __init__.py
│   │   ├── database.py               # 数据库工具类（复用现有）
│   │   ├── project_model.py          # 项目表模型
│   │   ├── part_model.py             # 零件表模型
│   │   ├── config_model.py           # 配置表模型（新增）
│   │   └── delivery_no_order_model.py # 飞书到货表模型（新增）
│   ├── services/                     # 业务逻辑层
│   │   ├── __init__.py
│   │   ├── pbom_service.py           # PBOM解析与三层检测逻辑
│   │   ├── config_detector.py        # 配置列识别（规则引擎 + LLM）（新增）
│   │   ├── match_service.py          # 与数据库匹配逻辑
│   │   ├── critical_service.py       # 关键件评分服务（4维度加权）（新增）
│   │   ├── dedup_service.py          # 多源去重合并服务（5维+4级）（新增）
│   │   ├── assembly_service.py       # 装配顺序推荐 + 多目标优化（新增）
│   │   ├── feishu_service.py         # 飞书API对接服务（新增）
│   │   ├── llm_service.py            # LLM调用封装（新增）
│   │   └── spider_auth_service.py    # 模拟登录获取凭证
│   ├── spider/                       # 爬虫模块（从warehouse_sync迁移适配）
│   │   ├── __init__.py
│   │   └── spider_main.py            # 爬虫核心逻辑
│   ├── common/                       # 公共模块
│   │   ├── __init__.py
│   │   ├── database.py               # 数据库单例
│   │   ├── logger.py                 # 日志工具
│   │   └── cache.py                  # 缓存工具（新增）
│   └── .env                          # 环境配置
├── frontend/                         # 前端代码
│   ├── index.html                    # 入口HTML
│   ├── vite.config.js                # Vite配置
│   ├── package.json                  # 前端依赖
│   ├── src/
│   │   ├── main.js                   # Vue入口
│   │   ├── App.vue                   # 根组件
│   │   ├── router/
│   │   │   └── index.js              # 路由配置
│   │   ├── api/                      # 接口请求封装
│   │   │   ├── request.js            # axios封装
│   │   │   ├── projects.js           # 项目相关接口
│   │   │   ├── delivery.js           # 到货数据接口
│   │   │   ├── pbom.js               # PBOM上传接口
│   │   │   ├── statistics.js         # 统计数据接口
│   │   │   ├── critical.js           # 关键件接口（新增）
│   │   │   ├── assembly.js           # 装配推荐接口（新增）
│   │   │   └── spider.js             # 爬虫配置接口
│   │   ├── views/                    # 页面组件
│   │   │   ├── ProjectList.vue       # 项目列表页
│   │   │   ├── ProjectDetail.vue     # 项目详情页（核心页面）
│   │   │   ├── PendingInspection.vue # 待检子页面
│   │   │   ├── UnqualifiedPending.vue# 不合格待判定子页面
│   │   │   ├── CriticalParts.vue     # 关键件管理页（新增）
│   │   │   ├── AssemblyPlan.vue      # 装配顺序推荐页（新增）
│   │   │   ├── Dashboard.vue         # 关键件到货率看板（新增）
│   │   │   ├── SpiderConfig.vue      # 爬虫配置页
│   │   │   └── LoginConfig.vue       # 凭证管理页
│   │   ├── components/               # 公共组件
│   │   │   ├── PieChart.vue          # 饼状图组件
│   │   │   ├── KpiCard.vue           # KPI指标卡组件（新增）
│   │   │   ├── StatusBadge.vue       # 状态标签组件（新增）
│   │   │   ├── PartsTable.vue        # 零件信息表格
│   │   │   ├── DeliveryTable.vue     # 到货信息表格
│   │   │   ├── ConfigConfirmDialog.vue # 配置列确认对话框（新增）
│   │   │   ├── CriticalScoreCard.vue # 关键件评分卡（新增）
│   │   │   └── WhatIfPanel.vue       # What-if模拟面板（新增）
│   │   └── utils/
│   │       └── index.js              # 工具函数
│   └── dist/                         # 构建输出目录
├── tests/                            # 测试程序
│   ├── backend/                      # 后端测试
│   │   ├── test_projects.py          # 项目管理测试
│   │   ├── test_pbom.py              # PBOM解析测试
│   │   ├── test_config_detector.py   # 配置列检测测试（新增）
│   │   ├── test_critical.py          # 关键件评分测试（新增）
│   │   ├── test_dedup.py             # 多源去重测试（新增）
│   │   ├── test_assembly.py          # 装配推荐测试（新增）
│   │   ├── test_match.py             # 匹配逻辑测试
│   │   ├── test_spider_auth.py       # 模拟登录测试
│   │   └── test_delivery.py          # 到货数据接口测试
│   └── frontend/                     # 前端测试（可选）
├── requirements.txt                  # Python依赖
└── 需求文档_V2_综合版.md              # 本文件
```

### 1.4 系统分层架构图（PBOM智能装配路线图）

```
                         ┌────────────────────────┐
                         │   PBOM Excel 上传       │
                         └──────────┬─────────────┘
                                    ↓
              ┌─────────────────────────────────────────┐
              │  层 1: PBOM 解析（三层递进检测）          │
              │  ① 规则引擎排除法（80% 命中）             │
              │  ② LLM 补刀（5-15% 不确定）              │
              │  ③ 用户确认对话框（兜底）                │
              └──────────┬──────────────────────────────┘
                         ↓
              ┌─────────────────────────────────────────┐
              │  层 2: 关键件识别（4维度加权评分）         │
              │  关键词 + LLM 评分 1-5                   │
              │  4 维度: 安全 30% / 大件 20% / 紧缺 30%  │
              │         / 工艺 20%                       │
              └──────────┬──────────────────────────────┘
                         ↓
┌────────────┐    ┌────────────────────────┐
│ 仓库系统    │───→│                        │
│ (有订单)    │    │  层 3: 多源去重合并      │──→ 统一到货表
└────────────┘    │  5 维匹配 + 4 级分级     │    (带来源标识)
                  │  + 飞书 UNIQUE 兜底      │
┌────────────┐    │                        │
│ 飞书共享表  │───→│                        │
│ (无订单)    │    │                        │
└────────────┘    └──────────┬─────────────┘
                              ↓
                  ┌─────────────────────────────────────────┐
                  │  层 4: 装配顺序推荐（4 步推理 + 多目标）  │
                  │  ① 齐套优先 ② 瓶颈对齐 ③ 工位平衡       │
                  │  ④ 风险车型                              │
                  │  多目标: 齐套 / 工时 / 风险 三者加权       │
                  │  LLM 白盒解释 "为什么这样排"              │
                  │  What-if 模拟：可调整优先级重排           │
                  └──────────┬──────────────────────────────┘
                             ↓
                  ┌─────────────────────────────────────────┐
                  │  看板: 装配建议 + 关键件到货监控          │
                  │  🟢 ≥95% / 🟡 80-95% / 🔴 <80%          │
                  └──────────┬──────────────────────────────┘
                             ↓
                  ┌─────────────────────────────────────────┐
                  │  闭环: 装配执行反馈 → 校准 LLM 评分      │
                  └─────────────────────────────────────────┘
```

---

## 二、功能模块详细需求

### 模块 1：数据库扩展

#### 1.1 新建表：`projects`（项目表）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INT | PRIMARY KEY, AUTO_INCREMENT | 自增主键 |
| project_name | VARCHAR(200) | DEFAULT NULL | 项目名称（选填） |
| project_code | VARCHAR(100) | NOT NULL | 项目号（必填） |
| apply_code | VARCHAR(100) | NOT NULL | 试制申请单号（必填） |
| model_type | VARCHAR(100) | DEFAULT NULL | 车型（如 E70、G59、S597）（新增） |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE | 更新时间 |

> 新增 `model_type` 字段，用于支持多车型的装配顺序推荐

#### 1.2 新建表：`project_parts`（项目零件表）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INT | PRIMARY KEY, AUTO_INCREMENT | 自增主键 |
| project_id | INT | NOT NULL, FK → projects.id | 关联项目 |
| part_name | VARCHAR(200) | NOT NULL | 零件名（从Excel解析） |
| part_code | VARCHAR(200) | NOT NULL | 零件号（从Excel解析） |
| demand_quantity | INT | DEFAULT 0 | 需求量（从Excel解析） |
| critical_level | TINYINT | DEFAULT 0 | 关键件评分（0-5, 0=未评估）（新增） |
| critical_reason | VARCHAR(200) | DEFAULT '' | 关键件判定理由（新增） |
| safety_score | TINYINT | DEFAULT NULL | 安全件维度评分 1-5（新增） |
| size_score | TINYINT | DEFAULT NULL | 大件维度评分 1-5（新增） |
| scarcity_score | TINYINT | DEFAULT NULL | 紧缺维度评分 1-5（新增） |
| process_score | TINYINT | DEFAULT NULL | 工艺维度评分 1-5（新增） |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

> 索引：`(project_id, part_code)` 联合唯一索引，确保同一项目中零件不重复

#### 1.3 新建表：`project_configs`（项目配置表）（新增）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INT | PRIMARY KEY, AUTO_INCREMENT | 自增主键 |
| project_id | INT | NOT NULL, FK → projects.id | 关联项目 |
| config_name | VARCHAR(50) | NOT NULL | 配置名称（如 M101/M102） |
| display_name | VARCHAR(100) | DEFAULT NULL | 配置显示名（如"标准续航版"） |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

> 索引：`(project_id, config_name)` 联合唯一索引

#### 1.4 新建表：`project_part_configs`（零件配置数量表）（新增）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INT | PRIMARY KEY, AUTO_INCREMENT | 自增主键 |
| project_id | INT | NOT NULL | 关联项目 |
| config_id | INT | NOT NULL, FK → project_configs.id | 关联配置 |
| part_code | VARCHAR(200) | NOT NULL | 零件号 |
| config_qty | INT | DEFAULT 0 | 该配置下的零件数量 |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

> 索引：`(config_id, part_code)` 联合唯一索引

#### 1.5 新建表：`delivery_no_order`（飞书无订单到货表）（新增）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INT | PRIMARY KEY, AUTO_INCREMENT | 自增主键 |
| project_code | VARCHAR(200) | NOT NULL | 项目号 |
| apply_code | VARCHAR(200) | NOT NULL | 申请单号 |
| part_code | VARCHAR(200) | NOT NULL | 零件号 |
| part_name | VARCHAR(200) | DEFAULT NULL | 零件名 |
| received_qty | INT | DEFAULT 0 | 到货数量 |
| received_date | DATETIME | DEFAULT NULL | 到货日期 |
| source | VARCHAR(50) | DEFAULT 'feishu' | 来源标识 |
| source_row_id | VARCHAR(100) | DEFAULT NULL | 飞书行ID |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

> 索引：`(source, source_row_id)` 唯一索引，`(project_code, apply_code, part_code)` 联合索引

#### 1.6 新建表：`dedup_log`（去重合并日志表）（新增）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INT | PRIMARY KEY, AUTO_INCREMENT | 自增主键 |
| project_id | INT | NOT NULL | 关联项目 |
| match_level | VARCHAR(20) | NOT NULL | 匹配级别：strong/weak/fuzzy/unique |
| part_code | VARCHAR(200) | NOT NULL | 零件号 |
| source_a | VARCHAR(50) | NOT NULL | 来源A |
| source_b | VARCHAR(50) | DEFAULT NULL | 来源B |
| matched_fields | VARCHAR(500) | DEFAULT NULL | 匹配的字段列表 |
| action | VARCHAR(50) | DEFAULT NULL | 处理动作：auto_merge/manual_confirm |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

#### 1.7 新建表：`assembly_plans`（装配计划表）（新增）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INT | PRIMARY KEY, AUTO_INCREMENT | 自增主键 |
| project_id | INT | NOT NULL, FK → projects.id | 关联项目 |
| plan_name | VARCHAR(200) | DEFAULT NULL | 计划名称 |
| plan_data | JSON | NOT NULL | 装配顺序JSON数据 |
| parameters | JSON | DEFAULT NULL | 优化参数（α, β, γ 权重） |
| created_by | VARCHAR(100) | DEFAULT NULL | 创建人 |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

#### 1.8 新建表：`spider_credentials`（爬虫凭证表）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INT | PRIMARY KEY, AUTO_INCREMENT | 自增主键 |
| authorization_token | TEXT | NOT NULL | NEW_AUTHORIZATION 值 |
| cookie | TEXT | NOT NULL | NEW_COOKIE 值 |
| login_username | VARCHAR(100) | DEFAULT NULL | 登录用户名（用于自动获取） |
| login_password | VARCHAR(100) | DEFAULT NULL | 登录密码（用于自动获取） |
| updated_at | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE | 更新时间 |

#### 1.9 对现有表 `delivery_detail` 的说明

该表由爬虫填充，结构已在 `spider_main.py` 中定义，字段包括：

| 关键字段 | 说明 |
|----------|------|
| DELIVERY_CODE | 送货单号 |
| APPLY_CODE | 试制申请单号（匹配依据） |
| PRO_CODE | 项目号（匹配依据） |
| PRO_NAME | 项目名称 |
| MATTER_CODE | 零件号 |
| MATTER_NAME | 零件名 |
| STATE | 单据状态（核心分类依据） |
| ORDER_NUM | 订单数量 |
| SEND_NUM | 发货数量 |
| RECIVE_NUM | 收货数量 |
| IN_NUM | 入库数量 |
| CANT_NUM | 不合格数量 |
| WH_NAME | 到货仓库 |
| SEND_WH_NAME | 发货仓库 |
| ZYS_USERNAME | 专业师 |
| STYLIST_USERNAME | 设计师 |
| RECIVE_TIME | 收货时间 |

**入库判断逻辑**：`IN_NUM >= ORDER_NUM`（入库数量 >= 订单数量）视为已入库完成

#### 1.10 测试要求

> 测试文件：`tests/backend/test_database.py`
>
> - 测试数据库连接与断线重连
> - 测试 `projects` 表的 CRUD（增删改查）
> - 测试 `project_parts` 表的插入与关联查询
> - 测试 `project_configs` 和 `project_part_configs` 的级联操作
> - 测试 `delivery_no_order` 表的唯一索引防重复
> - 测试 `dedup_log` 表的日志记录
> - 测试 `assembly_plans` 表的 JSON 读写
> - 测试 `spider_credentials` 表的读写
> - 测试与 `delivery_detail` 表的联合查询

---

### 模块 2：PBOM消耗清单上传与解析（增强版）

#### 2.1 功能描述

用户上传 Excel (.xlsx) 格式的 PBOM 消耗清单，系统自动解析并提取零件信息。

**V2.0 增强**：在原有基础上增加三层递进配置列检测，自动识别 M101/M102 等车型配置列。

#### 2.2 Excel 格式约定

- 支持 `.xlsx` 格式
- 从 Excel 中提取的列：**零件名**、**零件号**、**需求量**
- 系统需支持**自动识别**表头位置（从第一行查找匹配的列名）
- 列名可能为：`零件名`/`零件名称`/`part_name`，`零件号`/`物料号`/`part_code`，`需求量`/`需求数量`/`数量`

**PBOM 真实样本参考**（来自 E70 换代车型 S73f M101/M102 宽表）：

```
┌─────┬──────────┬──────────────────────┬──────────────┬───────────────┬────────┬──────────┬──────┬──────┬──────────────┐
│ 序号 │ 线别      │ 安装工序              │ 零件号        │ 零件名称       │ 总消耗 │ 零件接收  │ M101 │ M102 │ 力矩及范围    │
├─────┼──────────┼──────────────────────┼──────────────┼───────────────┼────────┼──────────┼──────┼──────┼──────────────┤
│ 1   │ 底盘一   │ 安装降压DCDC转换器负极  │ Z910080S7311 │ DCDC转换器负极 │ 12     │ (空)    │ 1    │ 1    │ 10 Nm ±10%  │
│ 2   │ 底盘一   │ 安装冷却水管-连接管总成  │ Q602316-0625 │ 六角法兰面螺栓 │ 12     │ (空)    │ 1    │ 1    │              │
│ 3   │ 底盘一   │ 安装冷却水管-连接管总成  │ Q083091-27   │ 钢带式弹性环箍  │ 12     │ (空)    │ 1    │ 1    │              │
└─────┴──────────┴──────────────────────┴──────────────┴───────────────┴────────┴──────────┴──────┴──────┴──────────────┘
```

#### 2.3 三层递进配置列检测（新增核心功能）

```
层 1: 规则引擎排除法
   ↓ 命中 80%
层 2: LLM 补刀（GPT-4o-mini, ~$0.0005/次）
   ↓ 不确定时
层 3: 用户确认对话框（兜底）
```

**层 1 规则引擎实现逻辑**：

```python
KNOWN_COL_PATTERNS = [r'序号', r'线别', r'工序', r'零件号', r'物料编码',
                      r'零件名称', r'总消耗', r'零件接收']
METADATA_COL_PATTERNS = [r'力矩', r'备注', r'自检', r'追溯', r'NM', r'★']
# 排除已知列 + 元数据列 + 数值 < 80% 数字 = 候选配置列
```

**层 2 LLM 补刀**：当规则引擎无法确定剩余列是否为配置列时，调用 LLM 辅助判断。

**层 3 用户确认对话框**：

```
┌──────────────────────────────────────────────┐
│ 检测到以下配置列，请确认：                      │
│ ☑ M101    (134 个零件, 值范围 1-20)           │
│ ☑ M102    (134 个零件, 值范围 1-20)           │
│ ☐ 力矩及范围  ← 已排除（元数据列）              │
│ 配置名可修改：M101 → [标准续航版]              │
│ [确认]  [跳过（无配置模式）]                    │
└──────────────────────────────────────────────┘
```

#### 2.4 零件合并规则

同一项目中，对**相同零件名 + 相同零件号**的零件数据进行合并：
- 需求量相加（SUM）
- 去重保留单条记录

#### 2.5 接口设计

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/projects/{id}/upload-pbom` | 上传Excel文件，解析并存入 `project_parts` 表，返回检测到的配置列 |
| GET | `/api/projects/{id}/parts` | 获取已解析的零件列表 |
| POST | `/api/projects/{id}/confirm-configs` | 确认配置列（用户确认后调用）（新增） |
| GET | `/api/projects/{id}/configs` | 获取项目的配置列表（新增） |

#### 2.6 测试要求

> 测试文件：`tests/backend/test_pbom.py`、`tests/backend/test_config_detector.py`
>
> - 准备一个**测试用Excel文件**（包含零件名、零件号、需求量三列，以及M101/M102等配置列）
> - 测试Excel解析功能：正确提取三列数据
> - 测试三层配置列检测：规则引擎命中率、LLM补刀场景、用户确认流程
> - 测试合并逻辑：相同零件名+零件号的条目正确合并
> - 测试重复上传：同一项目重复上传时覆盖旧数据
> - 测试异常情况：上传非Excel文件、空文件、缺列文件

---

### 模块 3：项目 CRUD 管理

#### 3.1 功能描述

支持对项目进行**创建、查看、编辑、删除**操作。

#### 3.2 项目信息字段

| 字段 | 必填 | 说明 |
|------|------|------|
| 项目名称 | 否 | 用户自定义名称 |
| 项目号 | 是 | 对应 `delivery_detail.PRO_CODE` |
| 试制申请单号 | 是 | 对应 `delivery_detail.APPLY_CODE` |
| 车型 | 否 | 车型型号（如 E70、G59、S597），用于装配顺序推荐（新增） |

#### 3.3 删除逻辑

- 删除项目时，**同时删除**关联的 `project_parts`、`project_configs`、`project_part_configs`、`dedup_log`、`assembly_plans` 表数据
- 不删除 `delivery_detail` 和 `delivery_no_order` 中的数据（该表为公共数据）

#### 3.4 接口设计

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/projects` | 创建项目 |
| GET | `/api/projects` | 获取项目列表（含项目状态概览） |
| GET | `/api/projects/{id}` | 获取单个项目详情 |
| PUT | `/api/projects/{id}` | 编辑项目信息 |
| DELETE | `/api/projects/{id}` | 删除项目（含所有关联数据） |

#### 3.5 测试要求

> 测试文件：`tests/backend/test_projects.py`
>
> - 测试创建项目：必填项检验、选填项默认值
> - 测试项目列表查询：分页、排序
> - 测试编辑项目：修改项目名、项目号、申请单号、车型
> - 测试删除项目：验证所有关联数据也被级联删除
> - 测试重复项目号+申请单号的限制

---

### 模块 4：到货数据匹配与展示（核心模块）

#### 4.1 功能描述

将上传的 PBOM 零件清单与 `delivery_detail` 表中的到货数据进行匹配，匹配依据为：
- **项目号**（`PRO_CODE`）← 项目创建时录入
- **试制申请单号**（`APPLY_CODE`）← 项目创建时录入
- **零件号**（`MATTER_CODE`）← 从Excel解析

#### 4.2 匹配逻辑

1. 用项目号（PRO_CODE）和试制申请单号（APPLY_CODE）从 `delivery_detail` 中筛选数据
2. 用 PBOM 中的零件号（part_code）匹配筛选结果中的 MATTER_CODE
3. 汇总匹配结果，展示到货情况

#### 4.3 展示内容 - 主页面

| 展示项 | 说明 |
|--------|------|
| 单据状态 (STATE) | 饼状图展示所有状态的分布比例 |
| 订单数量 (ORDER_NUM) | 汇总值 |
| 收货数量 (RECIVE_NUM) | 汇总值 |
| 入库数量 (IN_NUM) | 汇总值 |
| 到货仓库 (WH_NAME) | 列表展示 |
| 不合格数量 (CANT_NUM) | 汇总值 |
| 未到货零件 | 需求量 - 已入库数量 > 0 的零件清单 |
| 专业师 (ZYS_USERNAME) | 关联的专业师信息 |

#### 4.4 饼状图需求

- 基于 `STATE` 字段做分类统计
- 分类包括（根据数据库中实际出现的 STATE 值动态生成）：
  - 🟢 **入库完成**（IN_NUM >= ORDER_NUM）
  - 🔵 **已检待入库**（STATE 中包含"已检"或"待入库"）
  - 🟡 **待检**（STATE 中包含"待检"）
  - 🔴 **不合格待判定**（STATE 中包含"不合格"或"待判定"）
  - ⚪ **其他**（其他状态）
- 饼状图展示各状态的数量和占比
- 点击饼状图图例或扇区可筛选下方表格

#### 4.5 子页面需求

**待检子页面** `PendingInspection.vue`
- 路径：`/project/{id}/pending-inspection`
- 展示 STATE 为"待检"的明细数据
- 展示字段：DELIVERY_CODE、MATTER_CODE、MATTER_NAME、ZYS_USERNAME、ORDER_NUM、RECIVE_NUM
- 按专业室（ZYS_USERNAME）分组统计数量

**不合格待判定子页面** `UnqualifiedPending.vue`
- 路径：`/project/{id}/unqualified-pending`
- 展示 STATE 为"不合格待判定"的明细数据
- 展示字段：DELIVERY_CODE、MATTER_CODE、MATTER_NAME、ZYS_USERNAME、CANT_NUM、ORDER_NUM
- 按专业室（ZYS_USERNAME）分组统计数量

#### 4.6 接口设计

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/projects/{id}/delivery-summary` | 获取项目到货汇总数据（含饼状图数据） |
| GET | `/api/projects/{id}/delivery-detail` | 获取项目到货明细列表（支持STATE筛选） |
| GET | `/api/projects/{id}/states/{state}` | 获取特定状态的明细数据（子页面用） |
| GET | `/api/projects/{id}/unmatched-parts` | 获取未匹配到到货数据的零件清单 |

#### 4.7 测试要求

> 测试文件：`tests/backend/test_match.py`
>
> - 测试匹配逻辑：用已知数据验证 PROJECT_CODE + APPLY_CODE 筛选正确
> - 测试零件匹配：验证 PBOM 零件号与 MATTER_CODE 的匹配
> - 测试饼状图数据聚合：各状态分类计数正确
> - 测试入库判断（IN_NUM >= ORDER_NUM）逻辑正确
> - 测试子页面数据按专业室分组

---

### 模块 5：关键件智能识别与评分（新增）

#### 5.1 功能描述

基于 PBOM 零件清单，通过关键词规则 + LLM 评分，自动识别关键件并给出 4 维度评分，支持工艺专家校准。

#### 5.2 评分卡：4 维度 × 加权（1-5 分制）

| 维度 | 权重 | 5 分条件 | 1 分条件 |
|---|---|---|---|
| **安全件** | 30% | 制动/转向/碰撞安全核心 | 一般紧固件 |
| **大件/重件** | 20% | > 50kg 或 > 1m³ | < 5kg 标准件 |
| **紧缺件** | 30% | 历史到货延迟率 > 30% | 到货率 ≥ 95% |
| **关键工艺件** | 20% | 影响总装节点（首批/末批/翻转） | 通用装配位置 |

**最终评分** = 安全×0.3 + 大件×0.2 + 紧缺×0.3 + 工艺×0.2（满分 5 分）

| 评分 | 标签 | 行动 |
|---|---|---|
| 4.0+ | 关键件（红） | 优先备料 / 7 天缓冲 / 每日监控 |
| 3.0-3.9 | 次关键（黄） | 提前催料 / 备替代方案 |
| < 3.0 | 常规（绿） | 周级别监控 |

#### 5.3 LLM Prompt 设计

```
你是一个汽车零部件专家。以下是 PBOM 中的候选关键件，请按 4 维度评分（1-5）：

零件列表：
1. [Z910080S7311] 降压DCDC转换器负极
2. [Q602316-0625] 六角法兰面平端螺栓
...

请对每个零件输出：
{
  "part_code": "Z910080S7311",
  "name": "降压DCDC转换器负极",
  "safety": 4,        // 安全件维度
  "size": 2,          // 大件维度
  "scarcity": 3,      // 紧缺维度
  "process": 5,       // 工艺维度
  "is_critical": true,
  "reason": "DCDC转换器影响电动系统稳定，工艺关键路径件"
}

成本：~$0.001/件（GPT-4o-mini）
```

#### 5.4 交互流程

1. PBOM 上传解析完成后，系统自动触发关键件识别
2. 识别过程分两步：关键词规则初筛 → LLM 精细评分
3. 评分结果以列表展示，支持**工艺专家手动校准**
4. 校准后的评分反馈回系统，用于后续模型优化

#### 5.5 接口设计

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/projects/{id}/critical/identify` | 触发关键件识别（关键词+LLM评分） |
| GET | `/api/projects/{id}/critical/list` | 获取关键件评分列表（可按评分筛选） |
| PUT | `/api/projects/{id}/critical/{part_id}` | 手动校准关键件评分和维度分数 |
| GET | `/api/projects/{id}/critical/summary` | 获取关键件汇总统计（红/黄/绿数量） |

#### 5.6 降级策略

- 当 LLM API 不可用时，自动回退到**纯关键词规则引擎**（基于关键词匹配给出基础评分）
- 规则引擎覆盖的关键词包括：`制动`、`转向`、`安全`、`碰撞`、`气囊`、`DCDC`、`电池`、`电机` 等

#### 5.7 测试要求

> 测试文件：`tests/backend/test_critical.py`
>
> - 测试关键词规则引擎初筛逻辑
> - 测试 LLM 评分接口调用与解析
> - 测试 LLM 不可用时的降级回退
> - 测试工艺专家手动校准接口
> - 测试关键件汇总统计准确性（红/黄/绿分类）

---

### 模块 6：多源到货数据去重合并（新增）

#### 6.1 功能描述

将仓库系统（`delivery_detail`，有订单）和飞书共享表（`delivery_no_order`，无订单）两个来源的到货数据进行去重合并，形成统一的到货视图。

#### 6.2 数据源

| 来源 | 表 | 特点 |
|------|-----|------|
| 仓库系统 | `delivery_detail` | 有订单、权威、结构化 |
| 飞书共享表 | `delivery_no_order` | 无订单、补充、可能非结构化 |

#### 6.3 5 维匹配 × 4 级分级

| 匹配级别 | 判定条件 | 处理方式 | UI 标识 |
|---|---|---|---|
| 🟢 **强匹配** | 零件号 + 数量 + 批次 + 供应商 + 到货日期 **5 维完全一致** | 自动合并 | 双重登记 |
| 🟡 **弱匹配** | 零件号 + 数量 + 到货日期（±3 天） | 标黄 + 人工二次确认 | 疑似重复 |
| 🔴 **模糊匹配** | 零件号 + 数量一致但日期差异大 | 标红 + 提示差异 | 待核实 |
| ⚪ **唯一** | 仅一源登记 | 直接入库 | 仅一源 |

#### 6.4 合并逻辑

```python
deliveries = query_all("""
    SELECT MATTER_CODE as part_code, 'warehouse' as source, 
           RECEIVED_QTY, RECEIVED_DATE 
    FROM delivery_detail WHERE PRO_CODE = %s AND APPLY_CODE = %s
    UNION ALL
    SELECT part_code, 'feishu' as source, received_qty, received_date 
    FROM delivery_no_order WHERE project_code = %s AND apply_code = %s
""")

# 合并: 数量累加 + 取最早到货日 + 标记来源
```

#### 6.5 飞书 UNIQUE 兜底方案

**轻量级方案**：如果飞书共享表数据量小且不需要与 PBOM 关联，可直接使用飞书多维表格的 `UNIQUE()` + `COUNTIF()` 函数做共享表内去重。

| 方案 | 优势 | 劣势 | 推荐 |
|---|---|---|---|
| **A. 拉取到本地 MySQL 处理** | 可控、可追溯、可关联 PBOM | 需要 1 周开发 | ✅ 主推 |
| **B. 飞书多维表格 UNIQUE 公式** | 零开发 / 1 天上线 | 不可关联 PBOM 业务字段 | ✅ 短期/轻量 |

#### 6.6 接口设计

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/projects/{id}/dedup/sync-feishu` | 从飞书拉取共享表数据到本地 |
| POST | `/api/projects/{id}/dedup/run` | 执行多源去重合并 |
| GET | `/api/projects/{id}/dedup/result` | 获取去重合并结果（含各级别匹配详情） |
| PUT | `/api/projects/{id}/dedup/confirm/{log_id}` | 人工确认弱匹配/模糊匹配结果 |
| GET | `/api/projects/{id}/delivery-unified` | 获取合并后的统一到货视图 |

#### 6.7 测试要求

> 测试文件：`tests/backend/test_dedup.py`
>
> - 测试飞书 API 数据拉取（Mock 飞书返回）
> - 测试 5 维匹配逻辑各级别判定正确性
> - 测试自动合并 vs 人工确认的分流逻辑
> - 测试统一到货视图的数据完整性
> - 测试去重日志记录完整性

---

### 模块 7：装配顺序智能推荐（新增）

#### 7.1 功能描述

基于多车型 PBOM 和统一到货数据，通过 4 步推理 + 多目标优化，自动生成装配顺序建议，并支持 LLM 白盒解释和 What-if 模拟。

#### 7.2 4 步推理流程

```
步骤 1: 齐套优先
   ↓ 关键件全部到货的车型 = 优先装配
步骤 2: 瓶颈对齐
   ↓ 关键件缺的车型 = 推后到预计到货日
步骤 3: 工位平衡
   ↓ 相似工序连续装配 = 减少换型时间
步骤 4: 风险车型
   ↓ 外委供应商排程不稳 = 标黄警示
```

#### 7.3 多目标优化函数

```
目标函数 = α × 齐套率 + β × 工时均衡 + γ × 风险分散

α, β, γ 权重可调（默认 α=0.5, β=0.3, γ=0.2）
```

#### 7.4 MRP 净需求公式

> 关键件到货率 = `已到关键件数 / 应到关键件数 × 100%`
> 
> 更严格：**净需求** = 总需求 - 现有库存 - 在途到货

```
净需求 (Net Requirements) = 总需求 (Gross) - 现有库存 (On Hand) - 在途 (Scheduled Receipts)
```

> 装配顺序推荐要基于"净需求"而非"毛需求"——避免重复催料或漏催。

#### 7.5 LLM 白盒解释

```
推荐顺序:
1. E70-配置A（关键件齐套 100%）  🟢 可装
2. E70-配置B（关键件齐套 95%）  🟡 预警
3. G59-海外版（关键件齐套 78%）🔴 阻塞 2 项
   └─ 缺件: 侧围外板总成（预计 6/22 到货）

LLM 解释: "建议先装 E70-配置A，关键件全部到货且工艺连续；
         G59 推后到 6/22 是因为侧围外板依赖外委供应商，
         该供应商历史延迟 18%，加 2 天缓冲更稳妥。"
```

#### 7.6 What-if 模拟

**功能**：用户可调整某车型的优先级权重或到货日期，系统实时重算装配顺序。

**交互设计**：
- 拖拽调整车型优先级
- 修改关键件预计到货日期
- 修改优化权重（α, β, γ）
- 实时预览重排结果

**价值**：让工艺主管能"试错"，而不是"听 AI 的"

#### 7.7 接口设计

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/projects/{id}/assembly/recommend` | 生成装配顺序推荐（可传参数αβγ） |
| GET | `/api/projects/{id}/assembly/plan` | 获取当前装配计划 |
| POST | `/api/projects/{id}/assembly/what-if` | What-if 模拟（传入调整参数，返回模拟结果） |
| POST | `/api/projects/{id}/assembly/plan/save` | 保存装配计划 |
| GET | `/api/projects/{id}/assembly/plans` | 获取历史装配计划列表 |

#### 7.8 测试要求

> 测试文件：`tests/backend/test_assembly.py`
>
> - 测试 4 步推理逻辑正确性
> - 测试多目标优化函数计算
> - 测试净需求公式计算
> - 测试 LLM 解释生成
> - 测试 What-if 模拟的参数调整与重算
> - 测试装配计划的保存与读取

---

### 模块 8：关键件到货率看板（新增）

#### 8.1 功能描述

对已识别为关键件的零件进行到货率监控，通过红绿灯看板直观展示各车型/配置的关键件到货状态。

#### 8.2 看板指标

| 状态 | 阈值（净需求） | 含义 | 行动 |
|---|---|---|---|
| 🟢 | ≥ 95% | 可装 | 正常推进 |
| 🟡 | 80-95% | 预警 | 催缺料 + 备替代方案 |
| 🔴 | < 80% | 阻塞 | 调整装配顺序 / 推后排程 |

#### 8.3 看板展示字段

| 字段 | 说明 |
|------|------|
| 车型/配置 | 车型名称或配置名称 |
| 关键件总数 | 该车型/配置下的关键件总数 |
| 已到数 | 已到货的关键件数量 |
| 在途数 | 在途的关键件数量 |
| 净需求 | 关键件净需求数量 |
| 到货率 | 已到数 / 净需求 × 100% |
| 缺料清单 | 未到货的关键件明细 |
| 预计到货日 | 缺料预计到货日期 |

#### 8.4 接口设计

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/projects/{id}/dashboard/critical-arrival` | 获取关键件到货率看板数据 |
| GET | `/api/projects/{id}/dashboard/shortage-list` | 获取缺料清单明细 |

#### 8.5 测试要求

> 测试文件：`tests/backend/test_dashboard.py`
>
> - 测试看板数据聚合正确性
> - 测试红绿灯阈值判定逻辑
> - 测试缺料清单完整性

---

### 模块 9：凭证管理（自动获取）

#### 9.1 功能描述

由于爬虫依赖的 `NEW_AUTHORIZATION` 和 `NEW_COOKIE` 会过期，需要提供**手动更新**和**自动获取**两种方式。

#### 9.2 自动获取（模拟登录）

- 登录地址：`https://di360.dfmc.com.cn:24664/user/login`
- 发送 POST 请求，携带 `username` 和 `password`
- 从登录响应中提取 `Authorization` 和 `Cookie`
- 更新 `spider_credentials` 表并写入 `.env` 文件

#### 9.3 手动更新

提供 Web 页面表单，允许用户手动输入：
- `NEW_AUTHORIZATION` 值（文本框）
- `NEW_COOKIE` 值（文本框）
- 保存后更新到 `spider_credentials` 表和 `.env` 文件

#### 9.4 凭证在爬虫中的使用

- 爬虫启动时优先从 `spider_credentials` 表读取最新凭证
- 若表中无数据，从 `.env` 文件读取
- 每次爬虫执行前检查凭证有效性

**V2.0 优化**：增加凭证过期预检机制，提前 30 分钟发出预警通知。

#### 9.5 接口设计

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/spider/login` | 模拟登录（用户名+密码）自动获取凭证 |
| GET | `/api/spider/credentials` | 获取当前凭证信息（不显示密码） |
| PUT | `/api/spider/credentials` | 手动更新凭证 |
| POST | `/api/spider/validate` | 验证当前凭证是否有效 |

#### 9.6 测试要求

> 测试文件：`tests/backend/test_spider_auth.py`
>
> - 测试凭证存储和读取：写入 spider_credentials 表并正确读取
> - 测试 .env 文件更新逻辑
> - 测试手动更新接口
> - 测试凭证验证逻辑（发送测试请求验证是否有效）

---

### 模块 10：爬虫控制

#### 10.1 功能描述

在 Web 页面中提供爬虫的手动控制功能，复用 `warehouse_sync` 的爬虫逻辑。

#### 10.2 功能列表

- 查看爬虫运行状态（空闲/运行中/上次执行时间）
- 手动触发一次爬虫任务
- 查看爬虫运行日志

**V2.0 优化**：增加定时任务配置（可配置爬虫执行间隔），增加爬虫失败自动重试机制。

#### 10.3 接口设计

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/spider/trigger` | 手动触发爬虫任务 |
| GET | `/api/spider/status` | 获取爬虫运行状态 |
| GET | `/api/spider/logs` | 获取最近爬虫日志 |
| PUT | `/api/spider/schedule` | 配置定时任务参数（新增） |

#### 10.4 测试要求

> 测试文件：`tests/backend/test_spider_control.py`
>
> - 测试触发爬虫任务接口正常返回
> - 测试状态查询接口
> - 测试日志读取接口

---

## 三、前端页面详细设计

### 3.1 页面路由

| 路径 | 页面 | 说明 |
|------|------|------|
| `/` 或 `/projects` | ProjectList.vue | 项目列表首页 |
| `/project/create` | ProjectDetail.vue | 新建项目 |
| `/project/:id` | ProjectDetail.vue | 项目详情（查看/编辑，含PBOM+饼状图） |
| `/project/:id/pending-inspection` | PendingInspection.vue | 待检子页面 |
| `/project/:id/unqualified-pending` | UnqualifiedPending.vue | 不合格待判定子页面 |
| `/project/:id/critical-parts` | CriticalParts.vue | 关键件管理页（新增） |
| `/project/:id/assembly-plan` | AssemblyPlan.vue | 装配顺序推荐页（新增） |
| `/project/:id/dashboard` | Dashboard.vue | 关键件到货率看板（新增） |
| `/spider/config` | SpiderConfig.vue | 爬虫配置页 |
| `/spider/login` | LoginConfig.vue | 凭证管理页 |

### 3.2 页面原型描述

#### 3.2.1 项目列表页 `ProjectList.vue`

```
+------------------------------------------------------------------+
|  🏭 仓库到货数据管理系统                   [爬虫配置] [凭证管理]      |
+------------------------------------------------------------------+
|  [新建项目]                                                       |
+------------------------------------------------------------------+
|  # | 项目名称 | 项目号 | 车型 | 零件数 | 到货率 | 关键件到货率 | 操作 |
|  1 | 项目A   | P001  | E70  | 120   | 85%   | 🟡 88%      | [..] |
|  2 | 项目B   | P002  | G59  | 80    | 60%   | 🔴 72%      | [..] |
+------------------------------------------------------------------+
|  共 2 条                                                         |
+------------------------------------------------------------------+
```

- 顶部导航栏：系统标题 + 全局功能入口
- 项目列表卡片：展示所有项目及关键摘要信息
- 到货率 = 已入库零件数 / PBOM总需求量
- 关键件到货率：🟢 ≥95% / 🟡 80-95% / 🔴 <80%（新增）
- 操作按钮：查看（进入详情）、关键件、装配计划、看板、编辑、删除

#### 3.2.2 项目详情页 `ProjectDetail.vue`

```
+------------------------------------------------------------------+
|  ← 返回列表  项目详情 - {项目名称}({项目号}) [编辑] [删除]          |
|  [关键件管理] [装配计划] [到货率看板]                                |
+------------------------------------------------------------------+
|  📁 PBOM消耗清单                                                  |
|  [上传Excel文件] [重新上传]                                        |
|  +--------------------------------------------------------------+ |
|  | 零件号    | 零件名    | 需求量 | 关键 | 已入库 | 未到货 | 状态  | |
|  | MAT001   | 螺丝M6   | 100   | 🔴4.2| 80    | 20     | 🟡    | |
|  | MAT002   | 螺母M8   | 200   | 🟢2.1| 200   | 0      | 🟢    | |
|  +--------------------------------------------------------------+ |
+------------------------------------------------------------------+
|  📊 到货状态分布                                                  |
|  [        饼状图区域        ]  入库完成: 45%                       |
|  [   🟢🟡🔴⚪              ]  已检待入库: 25%                      |
|  [                         ]  待检: 15%  ←点击进入子页面           |
|  [                         ]  不合格待判定: 10% ←点击进入子页面      |
|  [                         ]  其他: 5%                            |
+------------------------------------------------------------------+
|  📋 到货明细                                                      |
|  +--------------------------------------------------------------+ |
|  | 送货单号 | 状态 | 订单数量 | 收货数量 | 入库数量 | 仓库 | 专业师 | |
|  | D001    | 已检待入库| 50  | 50   | 0    | 主仓  | 张三  | |
|  +--------------------------------------------------------------+ |
+------------------------------------------------------------------+
```

V2.0 增强：零件表格增加"关键件评分"列，显示红/黄/绿标识。

#### 3.2.3 关键件管理页 `CriticalParts.vue`（新增）

```
+------------------------------------------------------------------+
|  ← 返回项目详情  关键件管理 - {项目名称}                            |
|  [AI识别关键件] [导出清单]                                          |
+------------------------------------------------------------------+
|  关键件汇总: 🔴关键: 12  🟡次关键: 23  🟢常规: 99                   |
+------------------------------------------------------------------+
|  # | 零件号 | 零件名 | 安全 | 大件 | 紧缺 | 工艺 | 总分 | 标签 | 校准 |
|  1 | Z9100 | DCDC  | 4   | 2   | 3   | 5   | 3.7 | 🟡   | [✏️] |
|  2 | Q6023 | 螺栓  | 1   | 1   | 2   | 1   | 1.3 | 🟢   | [✏️] |
+------------------------------------------------------------------+
|  [+] 校准面板（点击展开）:                                         |
|  | 安全: [4] 大件: [2] 紧缺: [3] 工艺: [5]                         |
|  | 理由: [DCDC转换器影响电动系统稳定]                    [保存校准]   |
+------------------------------------------------------------------+
```

#### 3.2.4 装配顺序推荐页 `AssemblyPlan.vue`（新增）

```
+------------------------------------------------------------------+
|  ← 返回项目详情  装配顺序推荐 - {项目名称}                          |
|  [生成推荐] [What-if模拟] [保存计划] [历史计划]                      |
|  优化权重: α(齐套): [0.5] β(工时): [0.3] γ(风险): [0.2]            |
+------------------------------------------------------------------+
|  推荐装配顺序:                                                    |
|  ┌────┬──────────┬──────────┬────────┬────────┬──────┬──────────┐|
|  │ 顺序 │ 车型/配置  │ 关键件到货率 │ 状态   │ 缺料数  │ 工时  │ 风险     │|
|  ├────┼──────────┼──────────┼────────┼────────┼──────┼──────────┤|
|  │ 1  │ E70-配置A │ 100%     │ 🟢 可装 │ 0     │ 8h   │ 低       │|
|  │ 2  │ E70-配置B │ 95%      │ 🟡 预警 │ 2     │ 8h   │ 低       │|
|  │ 3  │ G59-海外  │ 78%      │ 🔴 阻塞 │ 5     │ 12h  │ 高 ⚠️    │|
|  └────┴──────────┴──────────┴────────┴────────┴──────┴──────────┘|
+------------------------------------------------------------------+
|  💡 AI 解释:                                                     |
|  "建议先装 E70-配置A，关键件全部到货且工艺连续；                     |
|   G59 推后到 6/22 是因为侧围外板依赖外委供应商，                      |
|   该供应商历史延迟 18%，加 2 天缓冲更稳妥。"                         |
+------------------------------------------------------------------+
|  🧪 What-if 模拟面板（点击展开）:                                  |
|  | 调整车型优先级: [G59-海外] [↑] [↓]                             |
|  | 修改预计到货日: [侧围外板总成] [2026-06-22] [重算]               |
|  +-- 模拟结果 --------------------------------------------------+ |
|  | 1. G59-海外（预计 6/22 到货后齐套 95%）→ 可提前至 6/24 装配    | |
|  +--------------------------------------------------------------+ |
+------------------------------------------------------------------+
```

#### 3.2.5 关键件到货率看板 `Dashboard.vue`（新增）

```
+------------------------------------------------------------------+
|  ← 返回项目详情  关键件到货率看板 - {项目名称}                       |
+------------------------------------------------------------------+
|  ┌──────────┬──────┬──────┬──────┬────────┬────────┬────────────┐ |
|  │ 车型/配置  │ 关键件 │ 已到  │ 在途  │ 净需求  │ 到货率  │ 状态       │ |
|  ├──────────┼──────┼──────┼──────┼────────┼────────┼────────────┤ |
|  │ E70-标准  │ 15   │ 15   │ 0    │ 15     │ 100%   │ 🟢 可装    │ |
|  │ E70-长续  │ 15   │ 14   │ 1    │ 15     │ 93.3%  │ 🟡 预警    │ |
|  │ G59-海外  │ 18   │ 12   │ 2    │ 18     │ 66.7%  │ 🔴 阻塞    │ |
|  └──────────┴──────┴──────┴──────┴────────┴────────┴────────────┘ |
+------------------------------------------------------------------+
|  🔴 阻塞详情 - G59-海外:                                          |
|  +--------------------------------------------------------------+ |
|  | 零件号      | 零件名        | 需求量 | 已到 | 在途 | 预计到货日   | |
|  | Z9100...   | 侧围外板总成   | 5     | 0   | 0   | 2026-06-22 | |
|  | Q6023...   | 六角法兰面螺栓 | 10    | 3   | 5   | 2026-06-20 | |
|  +--------------------------------------------------------------+ |
+------------------------------------------------------------------+
```

#### 3.2.6 待检子页面 `PendingInspection.vue`

```
+------------------------------------------------------------------+
|  ← 返回项目详情  待检明细 - {项目名称}                              |
+------------------------------------------------------------------+
|  按专业室汇总:                                                    |
|  +--------------------------------------------------------------+ |
|  | 专业室(专业师) | 待检数量 | 送货单数                            | |
|  | 张三         | 15     | 5                                   | |
|  | 李四         | 8      | 3                                   | |
|  +--------------------------------------------------------------+ |
+------------------------------------------------------------------+
|  明细列表:                                                        |
|  +--------------------------------------------------------------+ |
|  | 送货单号 | 零件号 | 零件名 | 专业师 | 订单数量 | 收货数量 |       | |
|  | D001    | MAT001 | 螺丝  | 张三   | 50     | 50     |       | |
|  +--------------------------------------------------------------+ |
+------------------------------------------------------------------+
```

#### 3.2.7 不合格待判定子页面 `UnqualifiedPending.vue`

```
+------------------------------------------------------------------+
|  ← 返回项目详情  不合格待判定明细 - {项目名称}                       |
+------------------------------------------------------------------+
|  按专业室汇总:                                                    |
|  +--------------------------------------------------------------+ |
|  | 专业室(专业师) | 不合格数量 | 送货单数                           | |
|  | 张三         | 30      | 6                                   | |
|  | 李四         | 12      | 4                                   | |
|  +--------------------------------------------------------------+ |
+------------------------------------------------------------------+
|  明细列表:                                                        |
|  +--------------------------------------------------------------+ |
|  | 送货单号 | 零件号 | 零件名 | 专业师 | 不合格数量 | 订单数量 |     | |
|  | D002    | MAT003 | 螺母  | 张三   | 10      | 50     |     | |
|  +--------------------------------------------------------------+ |
+------------------------------------------------------------------+
```

#### 3.2.8 凭证管理页 `LoginConfig.vue`

```
+------------------------------------------------------------------+
|  ← 返回首页  爬虫凭证管理                                          |
+------------------------------------------------------------------+
|  📌 自动获取（模拟登录）                                           |
|  +--------------------------------------------------------------+ |
|  |  用户名: [________________]                                    | |
|  |  密  码: [________________]                                    | |
|  |  [登录获取凭证]                    状态: ✅ 凭证有效/❌ 无效      | |
|  +--------------------------------------------------------------+ |
+------------------------------------------------------------------+
|  📌 手动更新                                                      |
|  +--------------------------------------------------------------+ |
|  |  NEW_AUTHORIZATION:                                            | |
|  |  [________________________________________________________]   | |
|  |  NEW_COOKIE:                                                   | |
|  |  [________________________________________________________]   | |
|  |  [保存凭证] [验证凭证]                                          | |
|  +--------------------------------------------------------------+ |
+------------------------------------------------------------------+
|  上次更新: 2026-06-15 10:30:00                                    |
+------------------------------------------------------------------+
```

#### 3.2.9 爬虫配置页 `SpiderConfig.vue`

```
+------------------------------------------------------------------+
|  ← 返回首页  爬虫控制台                                            |
+------------------------------------------------------------------+
|  运行状态: 🟢 运行中 / ⚪ 空闲 / 🔴 异常                           |
|  上次执行: 2026-06-15 10:00:00                                    |
|  下次执行: 2026-06-15 11:00:00                                    |
|  执行间隔: [60] 分钟  [保存]                                       |
+------------------------------------------------------------------+
|  [手动触发爬虫]                                                    |
+------------------------------------------------------------------+
|  运行日志:                                                        |
|  +--------------------------------------------------------------+ |
|  | 2026-06-15 10:00:00 INFO 开始执行...                         | |
|  | 2026-06-15 10:02:30 INFO 第1页入库 2000条                    | |
|  | 2026-06-15 10:05:00 INFO 完成，共新增 3500条                  | |
|  +--------------------------------------------------------------+ |
+------------------------------------------------------------------+
```

### 3.3 前端通用要求

1. **鉴权处理**：每个请求自动携带 `X-API-Key` 头（从 localStorage 读取）
2. **加载状态**：所有数据请求需显示加载中动画（El-Skeleton / v-loading）
3. **空状态处理**：无数据时显示友好的空状态提示（El-Empty）
4. **错误处理**：接口异常时弹出错误提示（El-Message），对 LLM 调用异常有专门降级提示
5. **响应式**：页面布局适配 1280px+ 分辨率
6. **确认弹窗**：删除等危险操作需二次确认（El-MessageBox）
7. **数据导出**：关键件清单、装配计划等支持导出为 Excel（新增）
8. **实时刷新**：看板页面支持手动刷新和自动轮询（可配置间隔）（新增）

---

## 四、API 接口汇总

### 4.1 现有接口（从 warehouse_sync 迁移）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/delivery/list` | 分页获取到货列表 |
| GET | `/api/delivery/{delivery_code}` | 获取送货单详情 |
| PUT | `/api/delivery/{delivery_code}/status` | 更新单据状态 |
| GET | `/api/delivery/statistics` | 到货数据统计 |
| GET | `/api/delivery/grouped` | 按零件号聚合到货数据 |

### 4.2 新增接口

| 方法 | 路径 | 所属模块 |
|------|------|----------|
| POST | `/api/projects` | 项目CRUD |
| GET | `/api/projects` | 项目CRUD |
| GET | `/api/projects/{id}` | 项目CRUD |
| PUT | `/api/projects/{id}` | 项目CRUD |
| DELETE | `/api/projects/{id}` | 项目CRUD |
| POST | `/api/projects/{id}/upload-pbom` | PBOM上传 |
| GET | `/api/projects/{id}/parts` | PBOM上传 |
| POST | `/api/projects/{id}/confirm-configs` | PBOM配置列确认（新增） |
| GET | `/api/projects/{id}/configs` | 项目配置列表（新增） |
| GET | `/api/projects/{id}/delivery-summary` | 到货匹配 |
| GET | `/api/projects/{id}/delivery-detail` | 到货匹配 |
| GET | `/api/projects/{id}/states/{state}` | 到货匹配 |
| GET | `/api/projects/{id}/unmatched-parts` | 到货匹配 |
| POST | `/api/projects/{id}/critical/identify` | 关键件识别（新增） |
| GET | `/api/projects/{id}/critical/list` | 关键件评分列表（新增） |
| PUT | `/api/projects/{id}/critical/{part_id}` | 手动校准关键件评分（新增） |
| GET | `/api/projects/{id}/critical/summary` | 关键件汇总统计（新增） |
| POST | `/api/projects/{id}/dedup/sync-feishu` | 飞书数据同步（新增） |
| POST | `/api/projects/{id}/dedup/run` | 执行多源去重（新增） |
| GET | `/api/projects/{id}/dedup/result` | 去重结果查询（新增） |
| PUT | `/api/projects/{id}/dedup/confirm/{log_id}` | 人工确认去重结果（新增） |
| GET | `/api/projects/{id}/delivery-unified` | 统一到货视图（新增） |
| POST | `/api/projects/{id}/assembly/recommend` | 生成装配推荐（新增） |
| GET | `/api/projects/{id}/assembly/plan` | 获取装配计划（新增） |
| POST | `/api/projects/{id}/assembly/what-if` | What-if模拟（新增） |
| POST | `/api/projects/{id}/assembly/plan/save` | 保存装配计划（新增） |
| GET | `/api/projects/{id}/assembly/plans` | 历史装配计划（新增） |
| GET | `/api/projects/{id}/dashboard/critical-arrival` | 关键件到货率看板（新增） |
| GET | `/api/projects/{id}/dashboard/shortage-list` | 缺料清单（新增） |
| POST | `/api/spider/login` | 凭证管理 |
| GET | `/api/spider/credentials` | 凭证管理 |
| PUT | `/api/spider/credentials` | 凭证管理 |
| POST | `/api/spider/validate` | 凭证管理 |
| POST | `/api/spider/trigger` | 爬虫控制 |
| GET | `/api/spider/status` | 爬虫控制 |
| GET | `/api/spider/logs` | 爬虫控制 |
| PUT | `/api/spider/schedule` | 定时任务配置（新增） |

---

## 五、成功指标（KPI）

> 参考业内首过接受率 85-90% 基准，设定以下 6 个关键指标：

| # | 指标 | 目标值 | 验证方法 |
|---|---|---|---|
| 1 | **配置识别准确率** | ≥ 95% | 134 个零件全标对 / 人工抽检 20 条 |
| 2 | **多源去重准确率** | ≥ 95% | 人工抽检 20 条重复记录 |
| 3 | **关键件判定命中率** | ≥ 80% | 与工艺专家标注对比 30 个关键件 |
| 4 | **装配建议节省工时** | ≥ 30% | AI 排 vs 人工排 / 1 周实际对比 |
| 5 | **用户确认对话框点击率** | < 5% | 95% 自动识别正确 / 不需要人工干预 |
| 6 | **首过接受率** | ≥ 85% | 装配建议直接被采纳的比例 |

> 验收标准：以上 6 项有 4 项达标即为通过。

---

## 六、实施计划

### 6.1 开发阶段划分（6 周计划）

#### Phase 1（Week 1）— 项目骨架搭建
- 初始化 `spiderV5` 项目结构
- 搭建 FastAPI 后端框架（含数据库模型）
- 建表（`projects`、`project_parts`、`project_configs`、`project_part_configs`、`delivery_no_order`、`dedup_log`、`assembly_plans`、`spider_credentials`）
- 搭建 Vue3 + Element Plus 前端框架
- 实现 FastAPI 托管前端静态文件
- 实现项目 CRUD 接口 + 前端页面
- 测试：验证前后端联调正常

#### Phase 2（Week 2）— PBOM解析 + 配置列检测
- 实现 Excel 解析服务（openpyxl）
- 实现三层递进配置列检测（规则引擎 + LLM + 用户确认对话框）
- 实现零件合并逻辑
- 实现上传接口 + 前端上传组件
- 实现配置列确认对话框（前端）
- 测试：PBOM 解析 + 三层检测全流程

#### Phase 3（Week 3）— 到货匹配 + 关键件识别
- 实现匹配服务逻辑（项目号 + 申请单号 + 零件号）
- 实现汇总统计接口（含饼状图数据）
- 实现子页面数据接口
- 前端饼状图组件（ECharts）+ 前端到货明细展示
- 实现关键件识别服务（关键词规则 + LLM 4维度评分）
- 实现关键件管理页（前端）
- 测试：匹配逻辑 + 统计 + 关键件评分

#### Phase 4（Week 4）— 多源去重 + 装配推荐
- 实现飞书 API 对接服务（拉取共享表数据）
- 实现 5 维匹配 + 4 级分级去重合并
- 实现统一到货视图
- 实现 4 步推理 + 多目标优化装配推荐
- 实现 LLM 白盒解释 + What-if 模拟
- 实现装配推荐页 + 看板页（前端）
- 测试：去重 + 装配推荐全流程

#### Phase 5（Week 5）— 凭证管理 + 爬虫控制
- 实现模拟登录功能
- 实现凭证管理接口 + 定时任务配置
- 实现爬虫控制接口 + 失败重试
- 实现爬虫配置页 + 凭证管理页（前端）
- 测试：凭证管理 + 爬虫控制

#### Phase 6（Week 6）— 集成测试与优化
- 全流程集成测试
- 6 个 KPI 指标验证
- UI 细节优化
- 异常场景处理（LLM 降级、飞书 API 异常等）
- 部署文档完善

### 6.2 资源需求

| 角色 | 人数 | 投入 |
|------|------|------|
| 主导开发 | 1 人 | 全程 |
| 工艺专家 | 1 人 | 每周 2 小时（校准关键件、验证装配推荐） |
| 数据人员 | 1 人 | Phase 3-4 / 数据准备 + 验证 |
| IT 协助 | 1 人 | Phase 4 / 飞书 API + 仓库系统对接 |

---

## 七、风险与降级路径

| 风险 | 影响 | 降级路径 |
|------|------|----------|
| **飞书共享表结构不统一** | 解析失败、去重不准 | 轻量级：飞书 UNIQUE 公式兜底；重量级：数据治理 + 统一字段模板 |
| **仓库系统数据延迟** | 实时性差、到货率不准 | 与 IT 协调实时 API（Phase 1 启动前并行）；本地缓存 + 定时刷新 |
| **关键件定义主观** | 评分不准、信任度低 | 工艺专家定期校准（每周 2h）+ 反馈闭环持续优化 |
| **外委供应商波动大** | 排程失效 | 加 7 天缓冲 + 风险预警标签 + What-if 重排能力 |
| **LLM 幻觉** | 错误建议影响决策 | 关键决策保留人工确认 + LLM 白盒解释可审计 + 降级到规则引擎 |
| **LLM API 不可用** | 评分卡/配置检测失效 | 回退到纯规则引擎（关键词匹配 + 规则排除法） |
| **飞书 API 5xx** | 同步失败、数据缺失 | 队列重试机制 + 手动导出 Excel 兜底 + 错误日志告警 |
| **凭证过期未及时发现** | 爬虫数据中断 | 增加凭证过期预检机制，提前 30 分钟预警 |
| **数据库连接中断** | 全系统不可用 | 连接池自动重连 + 健康检查接口 |

---

## 八、人机协同闭环

```
AI 出初稿 → 工艺专家校准 → 关键决策人工确认 → 装配执行
   ↑                                              ↓
   └──────── 反馈数据回流校准 LLM 评分 ←───────────┘
```

**设计原则**：
- AI 出 100% 准确的初稿 = 不可能（业内首过接受率 85-90%）
- 工艺专家**只校准 10-15% 边界情况** = 高效
- 关键决策**必须人工确认** = 安全（装配顺序、关键件标签）
- 反馈数据回流 = 模型越用越准

---

## 九、工业参考（避免闭门造车）

### 9.1 AutoAssembler（Assembly Intelligence）
- **能力**：EBOM → MBOM → BOP 自动规划，AI 推理装配顺序
- **优势**：基于 CAD 几何 + 工艺约束 + 物理仿真
- **限制**：SaaS / 不在本地 / 价格未知
- **价值**：作为方案思路参考，**不直接采购**

### 9.2 鼎捷 PLM 2026 趋势
- **AI 原生 BOM 多视图管理**
- EBOM → MBOM 转换错误率 < 0.5%
- 适合**全公司级**推广，不适合**科室级** PoC

### 9.3 强化学习方案（学术成熟）
- **DQN / PPO / SAC** 都能做装配排序
- **门槛**：需要 3-6 个月 + GPU + 标注数据
- **本项目不采用**：ROI 太低，不如 4 步推理实用

### 9.4 制造业物料查重（业内成熟）
- 多算法融合：文本相似度 + 数值匹配 + 特征提取 + 权重可配置
- 本项目的 5 维匹配 = 业内方案的 60% 子集
- 未来可扩展（Phase 2.5 迭代）

---

## 十、补充问题记录

以下问题需在开发过程中进一步确认：

| 序号 | 问题 | 提出时间 | 优先级 |
|------|------|----------|--------|
| 1 | Excel中"零件名""零件号""需求量"三列的具体列名是什么？需要提供一份示例Excel文件 | Phase 2 | 高 |
| 2 | 模拟登录时，登录接口返回的数据结构是怎样的（如何提取Authorization和Cookie）？ | Phase 5 | 高 |
| 3 | 是否需要用户登录/权限系统？还是局域网内直接使用？ | Phase 1 | 中 |
| 4 | 饼状图中"其他"状态的明细是否需要也支持点击进入子页面？ | Phase 3 | 低 |
| 5 | 飞书共享表的具体字段结构是什么？需要确认飞书自建应用权限申请进度 | Phase 4 | 高 |
| 6 | LLM API（GPT-4o-mini）的密钥和额度是否已就绪？ | Phase 2 | 高 |
| 7 | PoC 验证用的第一个车型确定了吗？（E70 迭代 / G59 海外 / S597 MT） | Phase 1 | 高 |
| 8 | 工艺专家人选是否已确定？需提前预约每周 2h 校准时间 | Phase 3 | 中 |

---

## 十一、下一步行动建议

1. **确定 PoC 车型** — E70 迭代 / G59 海外 / S597 MT 选一个
2. **收集样本数据** — 1 份 PBOM Excel + 1 个月到货数据 + 飞书共享表结构
3. **申请飞书自建应用权限** — OA 流程 1-2 周（**现在启动，Phase 4 用到**）
4. **申请 LLM API 额度** — GPT-4o-mini 预充值，确保 Phase 2 可用
5. **组建验证小组** — 主导开发 + 1 个工艺专家 + 1 个数据人员 + 1 个 IT 协助
6. **搭建最小原型** — 先跑通 Phase 1-2（PBOM 解析 + 配置列检测），验证可行性
7. **6 周后验收** — 6 个 KPI 至少 4 项达标 → 决定是否推广到全科室

---

## 附录：版本变更记录

| 版本 | 日期 | 变更说明 |
|------|------|----------|
| V1.0 | 2026-06-15 | spiderV4 初始需求文档，含 6 个基础模块 |
| V2.0 | 2026-06-23 | 整合 PBOM智能装配路线图；新增 4 个核心模块（配置列检测、关键件识别、多源去重、装配推荐）；新增 KPI 体系；新增风险降级路径；新增人机协同闭环；补充工业参考；优化前端设计（新增 3 个页面）；优化数据库设计（新增 5 张表）；优化实施计划（6 周计划） |

---

## 关联资源

- **spiderV4 原始需求文档**：`/data/home/DFMTC~tc-jiasiyan/code/spiderV4/需求文档.md`
- **PBOM 智能装配路线图**：`/home/DFMTC~tc-jiasiyan/Downloads/PBOM智能装配路线图_合订版v2.md`
- **warehouse_sync 爬虫项目**：`/data/home/DFMTC~tc-jiasiyan/code/warehouse_sync/`
- **飞书想法池 #2**：https://acndoaymjsa1.feishu.cn/docx/M7GjdLu5eoo93uxI3RacfpprnnZ