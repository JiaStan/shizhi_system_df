# 🗺️ PBOM 智能装配路线图（合订版 v2）

> **起草**：2026-06-17  
> **范围**：思琰需求 3 条 + 工业实践补强 + 落地路径  
> **对比基础**：思琰 ROADMAP.md（实操补丁）+ stan 第一版（架构图）+ 2026 行业方案（AutoAssembler、PLM、MRP）  
> **关联**：想法池 #2 / 跟进任务「评估 PoC 可行性」

---

## 📊 三方对照（避免重复造轮子）

| 维度 | 思琰 ROADMAP | stan 第一版 | 工业实践（搜索补强） | 合并取用 |
|---|---|---|---|---|
| PBOM 样本 | ✅ **真实** E70 换代（S73f）M101/M102 宽表 | 概念假设 | PLM 多视图 2026 趋势 | **思琰打底** |
| 配置列识别 | ✅ **三层递进** 规则→AI→用户确认 | 概念架构 | 业内：列名规范不统一是普遍痛点 | **思琰三层** |
| 关键件判定 | 关键词 + LLM 评分 1-5 | 0-100 四维加权 | — | **合并**（用思琰 1-5 评分，落 4 个维度的内容） |
| 多源去重 | 5 维匹配（思琰的 5 维实际我写为 4 级） | 5 维 + 4 级 | 业内：多算法融合（文本/数值/特征） + 飞书 UNIQUE 函数 | **合并** + 加飞书 UNIQUE 兜底 |
| 装配排序 | 到货率排序 | 4 步推理 + 多目标 | AutoAssembler 工业产品 / 强化学习 PPO/DQN | **4 步推理 + 多目标** |
| 成功指标 | ❌ 缺 | ⚠️ 隐含 | 业内：首过接受率 85-90% | **补全 6 个 KPI** |
| 实施计划 | 3 周 Phase 1-3 | 4 周 PoC | 业内：PoC 1 个月窗口 | **思琰 3 周 + 1 周验收 = 4 周** |
| 数据库 DDL | ✅ 完整 DDL | 仅文字描述 | — | **思琰直接用** |
| 工业参考 | ❌ | ❌ | AutoAssembler / 鼎捷 PLM / MRP 净需求 | **补全** |

---

## 🏗️ 整体架构

```
                         ┌────────────────────────┐
                         │   PBOM Excel 上传       │
                         └──────────┬─────────────┘
                                    ↓
              ┌─────────────────────────────────────────┐
              │  层 1: PBOM 解析（思琰三层检测）          │
              │  ① 规则引擎排除法（80% 命中）             │
              │  ② LLM 补刀（5-15% 不确定）              │
              │  ③ 用户确认对话框（兜底）                │
              └──────────┬──────────────────────────────┘
                         ↓
              ┌─────────────────────────────────────────┐
              │  层 2: 关键件识别（合并版）               │
              │  关键词 + LLM 评分 1-5                   │
              │  4 维度: 安全 30% / 大件 20% / 紧缺 30%  │
              │         / 工艺 20%                       │
              └──────────┬──────────────────────────────┘
                         ↓
┌────────────┐    ┌────────────────────────┐
│ 仓库系统    │───→│                        │
│ (有订单)    │    │  层 3: 多源去重合并      │──→ 统一到货表
└────────────┘    │  5 维匹配 + 4 级分级     │    (带来源标识)
                  │  + 飞书 UNIQUE 兜底      │
┌────────────┐    │                        │
│ 飞书共享表  │───→│                        │
│ (无订单)    │    │                        │
└────────────┘    └──────────┬─────────────┘
                              ↓
                  ┌─────────────────────────────────────────┐
                  │  层 4: 装配顺序推荐（4 步推理）          │
                  │  ① 齐套优先 ② 瓶颈对齐 ③ 工位平衡       │
                  │  ④ 风险车型                                 │
                  │  多目标: 齐套 / 工时 / 风险 三者加权       │
                  │  LLM 白盒解释 "为什么这样排"              │
                  └──────────┬──────────────────────────────┘
                             ↓
                  ┌─────────────────────────────────────────┐
                  │  看板: 装配建议 + 关键件到货监控         │
                  │  🟢 ≥95% / 🟡 80-95% / 🔴 <80%          │
                  └──────────┬──────────────────────────────┘
                             ↓
                  ┌─────────────────────────────────────────┐
                  │  闭环: 装配执行反馈 → 校准 LLM 评分      │
                  └─────────────────────────────────────────┘
```

---

## 🔧 模块 1：PBOM 解析（思琰三层检测 - 直接采用）

### 思琰的 M101/M102 真实样本

```
┌─────┬──────────┬──────────────────────┬──────────────┬───────────────┬────────┬──────────┬──────┬──────┬──────────────┐
│ 序号 │ 线别      │ 安装工序              │ 零件号        │ 零件名称       │ 总消耗 │ 零件接收  │ M101 │ M102 │ 力矩及范围    │
├─────┼──────────┼──────────────────────┼──────────────┼───────────────┼────────┼──────────┼──────┼──────┼──────────────┤
│ 1   │ 底盘一   │ 安装降压DCDC转换器负极  │ Z910080S7311 │ DCDC转换器负极 │ 12     │ (空)    │ 1    │ 1    │ 10 Nm ±10%  │
│ 2   │ 底盘一   │ 安装冷却水管-连接管总成  │ Q602316-0625 │ 六角法兰面螺栓 │ 12     │ (空)    │ 1    │ 1    │              │
│ 3   │ 底盘一   │ 安装冷却水管-连接管总成  │ Q083091-27   │ 钢带式弹性环箍  │ 12     │ (空)    │ 1    │ 1    │              │
└─────┴──────────┴──────────────────────┴──────────────┴───────────────┴────────┴──────────┴──────┴──────┴──────────────┘
```

### 三层递进检测

```
层 1: 规则引擎排除法
   ↓ 命中 80%
层 2: LLM 补刀（GPT-4o-mini, ~$0.0005/次）
   ↓ 不确定时
层 3: 用户确认对话框（兜底）
```

**层 1 关键代码**（思琰版已验证，可直接用）：
```python
KNOWN_COL_PATTERNS = [r'序号', r'线别', r'工序', r'零件号', r'物料编码',
                      r'零件名称', r'总消耗', r'零件接收']
METADATA_COL_PATTERNS = [r'力矩', r'备注', r'自检', r'追溯', r'NM', r'★']
# 排除已知列 + 元数据列 + 数值 < 80% 数字 = 候选配置列
```

### 用户确认对话框（思琰设计）

```
┌──────────────────────────────────────────────┐
│ 检测到以下配置列，请确认：                      │
│ ☑ M101    (134 个零件, 值范围 1-20)           │
│ ☑ M102    (134 个零件, 值范围 1-20)           │
│ ☐ 力矩及范围  ← 已排除（元数据列）              │
│ 配置名可修改：M101 → [标准续航版]              │
│ [确认]  [跳过（无配置模式）]                    │
└──────────────────────────────────────────────┘
```

### 数据库 DDL（思琰版直接采用）

```sql
CREATE TABLE project_configs (
    id INT AUTO_INCREMENT PRIMARY KEY,
    project_id INT NOT NULL,
    config_name VARCHAR(50) NOT NULL,
    UNIQUE KEY uk_project_config (project_id, config_name),
    FOREIGN KEY (project_id) REFERENCES projects(id) ON DELETE CASCADE
);

CREATE TABLE project_part_configs (
    id INT AUTO_INCREMENT PRIMARY KEY,
    project_id INT NOT NULL,
    config_id INT NOT NULL,
    part_code VARCHAR(200) NOT NULL,
    config_qty INT DEFAULT 0,
    UNIQUE KEY uk_config_part (config_id, part_code),
    FOREIGN KEY (config_id) REFERENCES project_configs(id) ON DELETE CASCADE
);
```

---

## 🔧 模块 2：关键件识别（合并版 - 思琰评分 + 维度细化）

### 评分卡：4 维度 × 加权（1-5 分制）

| 维度 | 权重 | 5 分条件 | 1 分条件 |
|---|---|---|---|
| **安全件** | 30% | 制动/转向/碰撞安全核心 | 一般紧固件 |
| **大件/重件** | 20% | > 50kg 或 > 1m³ | < 5kg 标准件 |
| **紧缺件** | 30% | 历史到货延迟率 > 30% | 到货率 ≥ 95% |
| **关键工艺件** | 20% | 影响总装节点（首批/末批/翻转） | 通用装配位置 |

**最终评分** = 安全×0.3 + 大件×0.2 + 紧缺×0.3 + 工艺×0.2（满分 5 分）

| 评分 | 标签 | 行动 |
|---|---|---|
| 4.0+ | 关键件（红） | 优先备料 / 7 天缓冲 / 每日监控 |
| 3.0-3.9 | 次关键（黄） | 提前催料 / 备替代方案 |
| < 3.0 | 常规（绿） | 周级别监控 |

### LLM Prompt（思琰版 + 维度细化）

```
你是一个汽车零部件专家。以下是 PBOM 中的候选关键件，请按 4 维度评分（1-5）：

零件列表：
1. [Z910080S7311] 降压DCDC转换器负极
2. [Q602316-0625] 六角法兰面平端螺栓
...

请对每个零件输出：
{
  "part_code": "Z910080S7311",
  "name": "降压DCDC转换器负极",
  "safety": 4,        // 安全件维度
  "size": 2,          // 大件维度
  "scarcity": 3,      // 紧缺维度
  "process": 5,       // 工艺维度
  "is_critical": true,
  "reason": "DCDC转换器影响电动系统稳定，工艺关键路径件"
}

成本：~$0.001/件（GPT-4o-mini）
```

### 数据库字段（思琰版直接采用）

```sql
ALTER TABLE project_parts ADD COLUMN critical_level TINYINT DEFAULT 0;
ALTER TABLE project_parts ADD COLUMN critical_reason VARCHAR(200) DEFAULT '';
```

---

## 🔧 模块 3：多源去重合并（合并版 - 5 维 + 4 级 + 飞书 UNIQUE 兜底）

### 数据源
- **仓库系统 delivery_detail**（有订单）— 权威
- **飞书共享表 delivery_no_order**（无订单）— 补充

### 5 维匹配 × 4 级分级

| 匹配级别 | 判定条件 | 处理方式 | UI 标识 |
|---|---|---|---|
| 🟢 **强匹配** | 零件号 + 数量 + 批次 + 供应商 + 到货日期 **5 维完全一致** | 自动合并 | 双重登记 |
| 🟡 **弱匹配** | 零件号 + 数量 + 到货日期（±3 天） | 标黄 + 人工二次确认 | 疑似重复 |
| 🔴 **模糊匹配** | 零件号 + 数量一致但日期差异大 | 标红 + 提示差异 | 待核实 |
| ⚪ **唯一** | 仅一源登记 | 直接入库 | 仅一源 |

### 合并逻辑

```python
deliveries = query_all("""
    SELECT MATTER_CODE as part_code, 'warehouse' as source, 
           RECEIVED_QTY, RECEIVED_DATE 
    FROM delivery_detail WHERE PRO_CODE = %s AND APPLY_CODE = %s
    UNION ALL
    SELECT part_code, 'feishu' as source, received_qty, received_date 
    FROM delivery_no_order WHERE project_code = %s AND apply_code = %s
""")

# 合并: 数量累加 + 取最早到货日 + 标记来源
```

### 飞书多维表格 UNIQUE 兜底（轻量级方案）

> **如果不想重造轮子**：飞书多维表格的 `UNIQUE()` + `COUNTIF()` 函数可以直接做共享表去重。

```
公式: UNIQUE([零件号], [到货日期])
结果: 共享表内唯一到货记录
```

**两条路对比**：

| 方案 | 优势 | 劣势 | 推荐 |
|---|---|---|---|
| **A. 拉取到本地 MySQL 处理** | 可控、可追溯、可关联 PBOM | 需要 1 周开发 | ✅ 主推 |
| **B. 飞书多维表格 UNIQUE 公式** | 零开发 / 1 天上线 | 不可关联 PBOM 业务字段 | ✅ 短期/轻量 |

**建议**：**主推 A，B 作为过渡**。

### 业内参考（多算法融合）

业内制造业物料查重的成熟做法：
- **文本相似度**（Jaccard / TF-IDF）— 解决「冷凝器」vs「冷凝器总成」
- **数值匹配** — 尺寸/规格字段
- **特征提取** — 供应商/品牌/型号
- **权重可配置** — 不同字段不同权重

思琰的 5 维匹配 = 业内多算法融合的子集（已覆盖强匹配 80% 场景），未来可扩展。

### 数据库 DDL（思琰版）

```sql
CREATE TABLE delivery_no_order (
    id INT AUTO_INCREMENT PRIMARY KEY,
    project_code VARCHAR(200) NOT NULL,
    apply_code VARCHAR(200) NOT NULL,
    part_code VARCHAR(200) NOT NULL,
    part_name VARCHAR(200),
    received_qty INT DEFAULT 0,
    received_date DATETIME,
    source VARCHAR(50) DEFAULT 'feishu',
    source_row_id VARCHAR(100),
    UNIQUE KEY uk_source (source, source_row_id),
    INDEX idx_project_part (project_code, apply_code, part_code)
);
```

---

## 🔧 模块 4：装配顺序推荐（stan 4 步推理 + 多目标优化）

### 4 步推理

```
步骤 1: 齐套优先
   ↓ 关键件全部到货的车型 = 优先装配
步骤 2: 瓶颈对齐
   ↓ 关键件缺的车型 = 推后到预计到货日
步骤 3: 工位平衡
   ↓ 相似工序连续装配 = 减少换型时间
步骤 4: 风险车型
   ↓ 外委供应商排程不稳 = 标黄警示
```

### 多目标优化函数

```
目标函数 = α × 齐套率 + β × 工时均衡 + γ × 风险分散

α, β, γ 权重可调（默认 α=0.5, β=0.3, γ=0.2）
```

### MRP 净需求公式（业内标准）

> 关键件到货率 = `已到关键件数 / 应到关键件数 × 100%`
> 
> 更严格：**净需求** = 总需求 - 现有库存 - 在途到货

```
净需求 (Net Requirements) = 总需求 (Gross) - 现有库存 (On Hand) - 在途 (Scheduled Receipts)
```

> 装配顺序推荐要基于"净需求"而非"毛需求"——避免重复催料或漏催。

### LLM 白盒解释

```
推荐顺序:
1. E70-配置A（关键件齐套 100%）  🟢 可装
2. E70-配置B（关键件齐套 95%）  🟡 预警
3. G59-海外版（关键件齐套 78%）🔴 阻塞 2 项
   └─ 缺件: 侧围外板总成（预计 6/22 到货）

LLM 解释: "建议先装 E70-配置A，关键件全部到货且工艺连续；
         G59 推后到 6/22 是因为侧围外板依赖外委供应商，
         该供应商历史延迟 18%，加 2 天缓冲更稳妥。"
```

### What-if 模拟

**交互**：调整某车型优先级 → 重排全表
**价值**：让工艺主管能"试错"，而不是"听 AI 的"

---

## 📊 关键件到货率看板

| 状态 | 阈值（净需求） | 含义 | 行动 |
|---|---|---|---|
| 🟢 | ≥ 95% | 可装 | 正常推进 |
| 🟡 | 80-95% | 预警 | 催缺料 + 备替代方案 |
| 🔴 | < 80% | 阻塞 | 调整装配顺序 / 推后排程 |

**看板字段**：
- 车型 / 关键件总数 / 已到数 / 在途数 / 净需求 / 到货率 / 缺料清单 / 预计到货日

---

## 🎯 6 个关键成功指标（KPI）

| # | 指标 | 目标值 | 验证方法 |
|---|---|---|---|
| 1 | **配置识别准确率** | ≥ 95% | 134 个零件全标对 / 人工抽检 20 条 |
| 2 | **多源去重准确率** | ≥ 95% | 人工抽检 20 条重复记录 |
| 3 | **关键件判定命中率** | ≥ 80% | 与工艺专家标注对比 30 个关键件 |
| 4 | **装配建议节省工时** | ≥ 30% | AI 排 vs 人工排 / 1 周实际对比 |
| 5 | **用户确认对话框点击率** | < 5% | 95% 自动识别对了 / 不需要人手 |
| 6 | **首过接受率** | ≥ 85% | 参考 AutoAssembler 业内基准 |

**3 周后跟黄流春/陈亚磊汇报的标志**：以上 6 项有 4 项达标。

---

## 🧪 4 周 PoC 计划（思琰 3 周 + 1 周验收）

### Phase 1（Week 1：6/22-6/28）— PBOM 解析
- **输入**：1 个车型 PBOM（E70 迭代 / G59 海外 二选一）
- **输出**：结构化数据（车型/配置/零件清单）
- **KPI 1**：配置识别准确率 ≥ 95%
- **AI 工具**：规则引擎 + GPT-4o-mini

### Phase 2（Week 2：6/29-7/5）— 多源去重
- **输入**：仓库系统 + 飞书共享表（同 1 个项目）
- **输出**：去重后的统一到货表
- **KPI 2**：去重准确率 ≥ 95%
- **AI 工具**：5 维匹配 + 飞书 UNIQUE 兜底

### Phase 3（Week 3：7/6-7/12）— 关键件识别
- **输入**：PBOM + 工艺文件 + QIS 历史
- **输出**：关键件清单 + 评分卡
- **KPI 3**：关键件判定命中率 ≥ 80%
- **AI 工具**：关键词 + LLM 4 维度评分

### Phase 4（Week 4：7/13-7/19）— 装配顺序推荐
- **输入**：多车型 PBOM + 统一到货表
- **输出**：装配顺序 + 关键件到货率 + LLM 解释
- **KPI 4-6**：节省工时 ≥ 30% / 点击率 < 5% / 首过接受率 ≥ 85%
- **AI 工具**：4 步推理 + 多目标优化 + LLM 解释

### 资源需求

| 角色 | 人 | 投入 |
|---|---|---|
| 主导（你） | 思琰 | 1 人 / 全程 |
| 工艺专家 | 1 人 | 每周 2 小时（校准关键件） |
| 数据人员 | 1 人 | Phase 2-3 / 数据准备 + 验证 |
| IT 协助 | 1 人 | Phase 2 / 飞书 API + 仓库系统对接 |

---

## ⚠️ 风险与降级路径

| 风险 | 影响 | 降级路径 |
|---|---|---|
| **飞书共享表结构不统一** | 解析失败 | 轻量级：飞书 UNIQUE 公式；重量级：数据治理 + 统一字段 |
| **仓库系统数据延迟** | 实时性差 | 与 IT 协调实时 API（Phase 1 启动前并行） |
| **关键件定义主观** | 评分不准 | 工艺专家校准（每周 2h）+ 反馈闭环 |
| **外委供应商波动大** | 排程失效 | 加 7 天缓冲 + 风险预警 + What-if 重排 |
| **LLM 幻觉** | 错误建议 | **关键决策保留人工确认** + LLM 白盒解释可审 |
| **LLM API 不可用** | 评分卡失效 | 回退到纯规则引擎（关键词匹配） |
| **飞书 API 5xx** | 同步失败 | 队列重试 + 手动导出兜底 |

---

## 🔁 人机协同闭环

```
AI 出初稿 → 工艺专家校准 → 关键决策人工确认 → 装配执行
   ↑                                              ↓
   └──────── 反馈数据回流校准 LLM 评分 ←───────────┘
```

**为什么这条链子重要**：
- AI 出 100% 准确的初稿 = 不可能（业内首过接受率 85-90%）
- 工艺专家**只校准 10-15% 边界情况** = 高效
- 关键决策**必须人工** = 安全
- 反馈数据回流 = 模型越用越准

---

## 🏭 工业参考（避免闭门造车）

### AutoAssembler（Assembly Intelligence）
- **能力**：EBOM → MBOM → BOP 自动规划，AI 推理装配顺序
- **优势**：基于 CAD 几何 + 工艺约束 + 物理仿真
- **限制**：SaaS / 不在本地 / 价格未知
- **价值**：作为方案思路参考，**不直接采购**

### 鼎捷 PLM 2026 趋势
- **AI 原生 BOM 多视图管理**
- EBOM → MBOM 转换错误率 < 0.5%
- 适合**全公司级**推广，不适合**科室级** PoC

### 强化学习（学术成熟方案）
- **DQN / PPO / SAC** 都能做装配排序
- **门槛**：需要 3-6 个月 + GPU + 标注数据
- **本项目不采用**：ROI 太低，不如 4 步推理实用

### 制造业物料查重（业内成熟）
- 多算法融合：文本 + 数值 + 特征 + 权重
- 思琰的 5 维匹配 = 业内方案的 60% 子集
- 未来可扩展（Phase 2.5）

---

## 🚀 下一步行动（建议 1 周内启动）

1. **确定 PoC 车型** — E70 迭代 / G59 海外 / S597 MT 选一个
2. **收集样本数据** — 1 份 PBOM + 1 个月到货数据 + 飞书共享表
3. **申请飞书自建应用权限** — OA 流程 1-2 周（**现在启动，4 周后用到**）
4. **组建验证小组** — 你 + 1 个工艺专家 + 1 个数据人员
5. **搭建最小原型** — Python + 飞书 OpenAPI + GPT-4o-mini 跑通 Phase 1
6. **3 周后看效果** — 6 个 KPI 至少 4 项达标 → 决定是否推广

---

## 🤔 4 个待你拍板的决策

| # | 决策 | 我的建议 |
|---|---|---|
| 1 | 这份合订版要**建飞书云文档**长期跟踪吗？ | **建议建**（让黄流春/陈亚磊可见可评） |
| 2 | PoC 第一个车型？ | **E70 迭代**（数据成熟 + 工艺熟悉） |
| 3 | 截止日？ | **7/22**（4 周 PoC + 1 周缓冲 = 5 周） |
| 4 | 飞书自建应用权限申请**现在就启动**？ | **强烈建议**（OA 流程 1-2 周） |

你说一句"建+ E70 + 7/22 + 启动"我就直接动手。

---

## 📎 关联资源

- **想法池 #2**：[https://acndoaymjsa1.feishu.cn/docx/M7GjdLu5eoo93uxI3RacfpprnnZ](https://acndoaymjsa1.feishu.cn/docx/M7GjdLu5eoo93uxI3RacfpprnnZ)
- **跟进任务**：评估 PoC 可行性（待你定截止日）
- **关联想法 #1**（飞书打卡签到小程序）：仓储系统工时数据来源
- **关联想法 #3**（6/17 AI 提效想法 #1：装车日报 AI 初稿）：装配执行反馈闭环

---

**版本**：v2 合订版  
**变更说明**：
- v1（stan 概念架构版）：偏概念，缺实操
- v2（本版）：**思琰实操样本打底 + stan 架构补强 + 工业参考 + 6 个 KPI + 降级路径**
- 后续随 PoC 推进持续更新
